# Pinned PSSharedGoods workflows

`FullModule/Public/Objects/Split-Array.ps1` is unchanged source from PSSharedGoods commit `2a807a4f11ba458b7bc405ce3674d93838639af1`, with SHA-256 `fb8e80dfad72d1b0d964fbb660dce08c818707ca8e59d4138a0527f1d2ab6e9b`. `CompleteWorkflow_PinnedSplitArrayPreservesAtomicListOutput` verifies three regions on `net10.0`/PowerShell 7.6 and `net472`/Windows PowerShell 5.1: a parameter-array early return carried by a return-or-fallthrough envelope, fresh `List<object>` construction, and the terminal comma/no-enumerate return. The generated helper never enumerates the early return value. Retained PowerShell owns its output cardinality, partial-success and error continuation, downstream stopping, and enumerator cleanup. Original and generated modules match parts, fixed-size, singleton, and reused invocations. A separate six-family fixture applies the same envelope to stable-scalar vectors, `System.Array`, `ArrayList`, `List<object>`, Hashtable, and explicit no-enumerate output. Direct object/ETS returns, arbitrary enumerators, local collection ownership, and compiled mutation remain retained.

`Convert-BinaryToString.ps1` is the unchanged complete function from [PSSharedGoods commit 2a807a4](https://github.com/EvotecIT/PSSharedGoods/blob/2a807a4f11ba458b7bc405ce3674d93838639af1/Public/Converts/Convert-BinaryToString.ps1). Its MIT license is included in this directory.

The source file SHA-256 is `8d237e02921013b114a7d0af851e8369e2693bbfad5a8d5a17b8ee30e48fa332`. The compiler discovery packet pins the containing archive separately.

`CompleteWorkflow_PinnedBinaryToStringPreservesBindingAndOutput` builds the complete function through the normal artifact builder and compares the original and generated command. It covers null, empty, singleton, malformed, Unicode, nested and invalid input; named, alias, positional and pipeline binding; record types and order; error identity and capture; and a later successful invocation. This fixture exercises an in-memory conversion and performs no administration operations.

Additional probes compare traced input enumerators that fail during `MoveNext`, `Current`, or disposal. This covers parameter-binding enumeration; it does not qualify arbitrary enumeration inside compiled bodies.

Qualification of this function does not imply qualification of the rest of PSSharedGoods or of a runtime-free library target.

`FullModule/Private/Convert-IPToBinary.ps1` is another unchanged function from the same pinned commit, with SHA-256 `cd183e6262bd24eeb7adc8ce471cf60de63a214d918c0a490336e1e77f58e9e3`. `CompleteWorkflow_PinnedIpConversionUsesSingleStatementPrefixWithoutChangingBehavior` verifies that identity and exercises its one-statement guarded scalar prefix through a normal Hybrid binary-module artifact on `net10.0`/PowerShell 7.6 and `net472`/Windows PowerShell 5.1. Original and generated modules match valid, whitespace, invalid, empty, and null inputs, warning/error order, downstream stop, reuse, and removal/reimport. This qualifies statement-count-independent prefix selection; it does not qualify a second region in the same real function.

`FullModule/Private/Deprecated/Objects/Get-ObjectEnumValues.ps1` is unchanged source from the same commit, with SHA-256 `d5622ecacebcc19f16d793c1527ad2bb5233398dc78f6d6738560cef362682d3`. `CompleteWorkflow_PinnedEnumHashtablePreservesIdentityErrorsStoppingAndState` verifies the first real same-function two-region workflow: a guarded compiled prefix creates an empty Hashtable, retained PowerShell populates it, and a compiled terminal region returns that exact reference. The original and generated modules match enum key/value types and order, retained mutation visibility, new identity per invocation, continuing and stopping behavior, downstream early stop, reuse, and removal/reimport on `net10.0`/PowerShell 7.6 and `net472`/Windows PowerShell 5.1. This qualifies only fresh empty Hashtable transfer plus read-only terminal passthrough; Hashtable mutation stays retained and ordinary Hashtable parameters are not promoted by this exception.

`Convert-HexToBinary.ps1` is the unchanged function from the same commit, with SHA-256 `4ede6b109b121813150cdce0b515964127183c2feedd7e30d8d9c86737194a85`. `CompleteWorkflow_PinnedHexPreservesCaptureErrorsAndContainers` qualifies the complete Hybrid command artifact on Windows PowerShell 5.1, PowerShell 7.4.19, and PowerShell 7.6.5. It compares 132 cases per configuration: exact result containers and serialization, valid and malformed pairs, odd lengths, partial parsing errors, named/positional/pipeline binding, empty input, downstream stop, and later successful invocation. Strict command qualification remains open because the generated cmdlet binder cannot yet preserve the authored string pipeline parameter's conversion callback behavior.

The complete CLR method contains the loop, capture, numeric indexing, and parsing. Its final unqualified `Write-Output -NoEnumerate` remains one explicitly hosted command region so runtime command lookup is preserved. No whole authored function is retained. This command-artifact qualification does not establish a runtime-free library contract.

`Get-RandomCharacters.ps1` and `Get-RandomPassword.ps1` are the unchanged complete functions from `Public/Random` at the same pinned commit. Their SHA-256 hashes are `e6a8113feeeddd18398e73c7556125ff92d238610a84ac241e558cb5c14e0a5d` and `36500278fbbe5eff90b8d7a79cf05e51604e83db71a000f8e7e24511aff0edb1`, respectively.

`CompleteWorkflow_PinnedRandomClosurePreservesCallsCapturesAndFailures` builds their two-file closure through the normal input resolver and artifact builder. Both bodies compile in Hybrid mode; their command pipelines retain native provider lookup. The observer replaces only `Get-Random` inside the selected module with a deterministic provider. It compares values, record types, provider call order, conversion failures, nonterminating and terminating errors, early downstream stop, and a later successful call across 289 observations per host. Four additional observations compare explicit runspace cancellation, provider cleanup, and successful reuse. The test checks the selected module's exports and invokes its command objects so an installed module cannot silently replace the fixture.

The same observers also match in the complete pinned PSSharedGoods module and its generated Hybrid artifact on Windows PowerShell 5.1, PowerShell 7.4.19, and PowerShell 7.6.5. Windows PowerShell 5.1 requires a sufficiently short source path for its module loader. This qualifies these two command workflows, not every function in PSSharedGoods, randomness quality, or a runtime-free Strict target.

The `Registry` directory contains eight unchanged workflow files from the same commit. `Get-PSRegistry.ps1` comes from `Public/Registry`, `Script.RegistryDictionaries.ps1` from `Private/Parameters`, and the remaining files from `Private`. The parent MIT license applies. `CompleteWorkflow_PinnedRegistryPreservesModuleStateAndRetainedCalls` verifies each file's SHA-256 before building the closure.

The registry observer preserves the authored initializer, path normalization, recursive calls, local/remote dispatch, nesting counter, and cleanup. It replaces only registry read, mount, and dismount providers inside the selected module. Computer-name discovery uses loopback. No system registry is read or changed. Seven workflow functions compile, including `Resolve-PrivateRegistry`; `Get-PSSubRegistryTranslated` remains an explicit hosted function.

The eight-file closure and the complete original module match their generated Hybrid artifacts on Windows PowerShell 5.1, PowerShell 7.4.19, and PowerShell 7.6.5. Per host, 433 ordinary observations cover initialization reuse, existing and newly acquired mount state, local/remote paths, recursion, values, streams, errors, early downstream stop, and later invocation. Explicit assertions check acquired offline paths, cleanup, and `DoNotUnmount`. Six further observations cover explicit cancellation, provider cleanup, runspace reuse, and module reimport. Cancellation preserves the authored counter leak: the entrypoint has no `finally`, so an interrupted call can leave its counter nonzero and prevent later unmounting until reimport. This qualifies one stateful Hybrid workflow; it does not qualify real registry access, every function in the module, or a Strict target.

`CompleteWorkflow_PinnedRegistryInFullModule` makes the full source-module comparison repeatable from `FullModule/`. That directory contains the unchanged loader, all 283 files under `Private/` and `Public/`, and the MIT license from the pinned commit. `SHA256SUMS.txt` records all 285 files; its SHA-256 is `87f61869110c6e6950603027fe4c9304d4d7a4386008754e009710cabae37ea6`. The test verifies those bytes and discovery of all 284 source files before comparing the workflow. The original color table and argument-completer registrations initialize inside the disposable host process. The dependency manifest and assembly directory are not included or imported.
