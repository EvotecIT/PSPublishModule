param(
    [string] $ProjectPath = $PSScriptRoot
)

Import-Module PSPublishModule -Force -ErrorAction Stop

Invoke-ModuleTestSuite `
    -ProjectPath $ProjectPath `
    -AdditionalModules 'Pester', 'PSWriteColor' `
    -EnableCodeCoverage `
    -OutputFormat Detailed
