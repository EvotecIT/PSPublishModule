# PowerShell compiler: next major milestones

Updated: 2026-09-25. Active continuation: `feature/powershell-compiler`. The readiness assessment records the qualified revisions and remaining integration boundaries.

M24–M28 have completed bounded local implementation gates, including the September 22 audit corrections. M28 covers executable run/watch, Strict library NuGet packaging, grouped project diagnostics, and a module consumer quickstart on both supported Windows PowerShell hosts. **M29 has completed its pinned local coverage gate:** 30 newly emitted and differentially executed module functions across four blocker families, an offline standalone compiled workflow, and zero accepted per-function regressions. M29a now addresses ordinary PowerShell command forms that still fall back in real scripts; M30 then handles integration, distribution, providers, and platform qualification. Direct source-debugger qualification is deferred; it is not a condition for broader compiler coverage. Completion of a local milestone does not establish general PowerShell compatibility, a released package, or qualification on an untested host.

The [readiness assessment](PowerForge.PowerShellCompilation.Assessment.md) owns findings and dated validation. The [architecture roadmap](PowerForge.PowerShellCompilation.Roadmap.md) owns design and M0–M23. The [compilation guide](PowerForge.PowerShellCompilation.md) owns commands. This document owns the remaining execution order; earlier per-commit journals are available in Git history.

## Starting point

Keep the canonical parser → binder → bound IR → analysis → lowering → C# backend. Keep semantic and artifact rules in `PowerForge.PowerShell` / `PowerForge`, with thin CLI and cmdlet adapters. New syntax must not acquire a separate evaluator in the emitter, census, or wrapper.

Generated compiler artifacts accept `net10.0` and `net472`; executables accept only `net10.0`. Hosted artifact qualification targets PowerShell 7.6 and Windows PowerShell 5.1. This is distinct from the shared `PowerForge.PowerShell` host library, whose project still builds `net472;net8.0;net10.0`. Existing 7.4 observations and the host-library build target do not reopen generated-artifact net8 support. Do not remove a shared-library target without checking its consumers.

The September 22 pinned PSSharedGoods census reached **196/282** complete emitted functions, leaving 86 retained or unsupported. Its eight recent `-as` additions have since passed focused original/generated execution on both supported Windows hosts. This is one packet's result, not a language-coverage percentage or speedup. The pinned multi-module portfolio was the M29 acceptance surface: no one module's count can establish ecosystem coverage, and an emitted Hybrid method may still delegate work to PowerShell.

## Delivery order

| Priority | Milestone | Current state | Required outcome |
| --- | --- | --- | --- |
| Complete | M29 script and module coverage | Pinned local portfolio passed | Pinned Evotec and independent third-party module portfolio; measured per-module gains, safe original/generated workflows, and no accepted regressions |
| 1 | M29a everyday PowerShell forms | Planned | Common authored command shapes compile where their semantics and command identity are proved; remaining host boundaries are explicit |
| 2 | M30 distribution and platforms | Partial / separate qualification | Supported hosts, release set, public-feed lifecycle, and clean-target execution are evidenced independently |
| 3 | M31 performance | Planned | Repeatable workload benefit after semantic and deployment correctness |

Known accepted-code defects take priority over breadth or performance. Keep remediation proportional to a reachable trigger and consequence: cover the observed failure and consequential sibling paths, then stop expanding the matrix once the supported contract is demonstrated. Record every finding as reproduced, source-demonstrated, or unverified; record its owner, affected modes, test, and closure evidence. Do not mark a milestone complete because its aggregate test count increased.

## Milestone 24 — Preserve values, collections, and error continuation

**Status: Complete within the qualified value and ownership families.**

**Outcome:** data transformations preserve observable values, CLR types, identity, cardinality, order, error behavior, continued statements, and cleanup.

The completed contract separates expression casts from variable constraints; preserves bounded numeric promotion; models no-value/null/scalar/collection output; and retains PowerShell enumeration and failure behavior where the compiler cannot own it safely. Schema-6 transfer evidence covers exact atomic maps, stable-scalar vectors, read-only exact list-like references, return/fallthrough envelopes, closed scalar/vector alternatives, and one exact fresh-ArrayList local factory. Only fresh one-dimensional stable-scalar vectors receive compiled mutation ownership, ending at live-out transfer.

Hybrid native functions additionally use the active host for computed members, map-key iteration, patterns, conditional value capture, and retained-reference mutation. These capabilities do not become runtime-free merely because the surrounding method is generated C#.

**Evidence:** the conditional-value/error matrices, unchanged TimeSpan and random-name workflows, full-module registry workflow, collection transfer/return tests, and Strict stateful consumer. See the dated assessment for the checkpoint and execution limits.

**Boundaries to preserve:** borrowed compiled reference mutation, arbitrary object/ETS transfer, arbitrary enumerators, multidimensional/open arrays, unproved local ownership, and general local-call collection factories remain retained or rejected. A later qualified Hybrid slice must not silently broaden Strict admission.

**Gate for any expansion:** select three complete previously blocked workflows from at least two families, including one Strict artifact when claiming runtime-free growth. Compare original/generated behavior for the selected workflows and consequential reachable failure paths on each claimed host. Include empty, singleton, nested, null, overflow, malformed input, and acquisition/advance/current/disposal failures where the changed contract can encounter them; existing qualified enumeration owners do not require a new unrelated fault framework for every operator. Track all co-blockers and state which operations still execute in PowerShell.

## Milestone 25 — Compile complete pipelines and advanced functions

**Status: Complete within the qualified command families; stopping fixtures requalified on both hosts.**

**Outcome:** complete commands preserve every record, parameter binding, failure/continuation, downstream stop, cancellation, and cleanup.

The existing native invocation owns PowerShell command resolution, parameter sets, defaults, validation, common parameters, dynamic scope, pipeline stages, and begin/process/end/clean behavior where the host supports it. Shared output/capture owners preserve local-call records, enumeration depth, error destinations, and cleanup ordering. The statement-error bridge preserves admitted CLR invocation errors through the loaded PowerShell host; it is not a runtime-free error implementation.

**Evidence:** complete pipeline and implicit-output matrices, random-name calls, statement-error and output-unwind tests, and both supported hosted artifact lanes. Provider replacements qualify compiler composition without granting live administration or network-operation coverage.

**Boundaries to preserve:** terminal success sinks, unsupported basic-function/common-parameter collisions, unproved error-state observers, broader mutable receivers, and unregistered runtime-free adapters retain their own admission decisions. Record an unsupported shape explicitly rather than routing only its success stream.

**Gate for any expansion:** two complete commands in their original module context, including multiple output-producing local calls and a retained provider stage. Compare zero/one/many inputs, ordered success and non-success streams, partial failure, stop, cancellation, cleanup, and subsequent invocation. PowerShell 5.1 cannot be credited for syntax that its parser does not support.

## Milestone 26 — Compile stateful modules and multiple Hybrid regions

**Status: Complete for the bounded M23 state/region contract.**

**Outcome:** several safe regions share one module state owner without creating another PowerShell runtime.

The selector admits a maximal approved candidate from each non-overlapping canonical run and preserves independently safe suffixes when a larger candidate fails policy. It does not promise every arbitrary source span. Canonical graph inputs/outputs must match transfer metadata. The guarded prefix remains the fresh-local owner; additional regions cannot invent ownership. Hybrid state stays in the parent module. Strict stateful libraries use a separate runtime-free instance/lifetime contract.

**Evidence:** the same-function `Get-ObjectEnumValues` two-region workflow; synthetic four-scalar-plus-factory region composition; full pinned registry and saved-state modules; and a stateful reservation library consumed by ordinary C#. Tests cover two runspaces, nesting/reentrancy, retained mutation, initialization failure, stop/cancellation, reuse, removal/reimport, independent instances, reset, concurrency, and disposal within their selected shapes.

Registry providers are isolated replacements. Saved-state tests use in-memory providers and do not qualify real filesystem persistence, the external dependency manifest, or the public administration entrypoints. The census remained 188/282 functions and 36 promoted regions after general selection because that packet had no third eligible disjoint run.

**Boundaries to preserve:** borrowed mutable transfer, ambient initializer effects, caller-dependent inherited state, dynamic object identity, and general lifecycle/error envelopes remain separate work. Strict state qualification currently uses explicitly typed immutable scalar field declarations with controlled state updates.

**Gate for any expansion:** retain the two unrelated complete Hybrid module workflows and the clean Strict consumer. Add a minimized real workflow for the new ownership/lifetime shape; verify aliasing, failure rollback, streams, stopping, disposal, next invocation, and agreement between explain output and executed regions. Record crossing cost separately from correctness.

## Milestone 27 — Make compiled CLR libraries practical to consume

**Status: Complete for bounded local-feed CLR consumption, including September 22 corrective qualification.**

**Outcome:** PowerShell-authored libraries expose a predictable .NET API and produce packages that ordinary consumers can restore and use safely.

Implemented and previously qualified:

- [x] Static TimeSpan and stateful reservation consumers exercise different data and lifetime contracts.
- [x] Deterministic local-feed packages contain generated library, XML documentation, portable PDBs, source, locks, and provenance; an independent rebuild checks library/PDB/provider bytes.
- [x] Ordinary consumers restore generated packages for `net10.0` and `net472` without source-project references or a PowerShell runtime requirement.
- [x] Registered providers have separate output, failure, cancellation, cleanup, and closure tests.
- [x] Generated API comparison rejects removed/changed methods and module lifetime changes. Static manifests use schema 4; stateful manifests use schema 5.

Corrective closure:

- [x] **R1 / P2:** cancellation-aware snapshot/copy/hash/archive work and a final pre-commit check preserve previous output. Rebuild cleanup precedes publication; later cancellation cannot be reported as though a committed package were rolled back.
- [x] **R2 / P3:** shared NuGet ID validation and canonical `x.y.z` policy reject malformed library, provider, and dependency metadata. Real builders preserve previous output; NuGet and ordinary consumers validate valid packages.
- [x] **R3 / P2 validation:** include all nine ABI cases and six existing provider-consumer/lifecycle/dependency cases in the bounded gate. Require passing results from the necessary M27 families.
- [x] Qualify replacement failure, Windows destination contention, cleanup failure, and cancellation during/after writing. Cleanup errors retain the primary failure and diagnostic details.
- [x] Define the current consumption contract: select identity/version/TFM, restore, and rebuild the consumer. Generated ABI comparison covers API/module lifetime; binary drop-in replacement and replacing loaded assemblies are not promised. M30 qualifies any future broader upgrade contract before promotion.
- [x] Final focused suite passes 55/55 with no failures/skips, including the eight stopping fixtures on both hosts. The net472 Release build has zero warnings/errors; targeted read-only confirmation of the production fixes has no actionable findings.
- [x] Canonical compiler gate passes 1,097/1,097 with zero failures/skips; Strict corpus passes 6/6 with 24 emitted units. Revision, host, artifact, and follow-up fixture evidence is recorded in the assessment.

**Exit gate:** both ordinary consumers exercise success, invalid input, null/empty, defaults, cancellation where supported, and repeated lifetime. Intentional generated API incompatibilities and malformed package identities are rejected. Cancellation/failure cannot damage the prior destination. Runtime/dependency inspection proves the promised runtime-free closure. Signed-input rejection is not signed-package qualification; signing and public-feed upgrade/rollback remain M30.

Native shared-library exports remain deferred until a concrete non-.NET embedding consumer requires their separate ABI.

## Milestone 28 — Deliver a source-first development loop

**Status: Complete for the bounded local development workflows; source debugging is deferred.**

**Outcome:** users develop from PowerShell source without treating generated C# or temporary build paths as the main interface.

The [project CLI](../PowerForge.Cli/Program.Command.PowerShell.Project.cs) exposes `init`, `analyze`, `explain`, `recommend`, `lock`, `restore`, `build`, `run`, `watch`, `test`, `pack`, `install`, and `diagnose` through `PowerShellCompilationProjectWorkflowService`. The [development guide](PowerForge.PowerShellCompilation.Development.md) covers executable run/watch, arguments, cancellation, and reviewed-lock boundaries. Project `pack` produces a qualified ZIP by default; `pack --format nuget` delegates to the existing Strict CLR library NuGet builder. The [library packaging guide](PowerForge.PowerShellCompilation.LibraryPackages.md) covers explicit metadata, qualification, local-feed consumption, and ABI baselines.

- [x] Add executable run to the existing project owner and thin CLI, preserving declared target, argument vectors, inherited stdin/stdout/stderr, exit codes, cancellation, and locks. Run checks inputs and the authenticated artifact inventory at the launch boundary; canceled/failed builds do not execute old output.
- [x] Add executable watch and incremental rebuild using the [existing artifact cache](../PowerForge.PowerShell/Services/Compilation/PowerShellCompilationArtifactBuildCache.cs) and content fingerprints. Stop the preceding process tree before rebuilding, debounce rapid edits, recover after invalid source, and verify no-change reuse. Local script/resource bytes may change against a reviewed graph; dependency topology, providers, profiles, and target changes require explicit lock/restore. Development artifacts identify the reviewed baseline without labeling the edited source graph as fully reviewed.
- [x] Integrate the existing library NuGet builder with `project pack --format nuget` and reuse existing ABI-baseline checks. Require one tested Strict library, emitted source, explicit metadata, and the exact isolated restore lock. Preserve qualified ZIP delivery. Ordinary Windows net10.0/net472 consumers and a provider dependency consumer exercise the resulting packages.
- [x] Extend project explain/diagnose with [grouped diagnostics](PowerForge.PowerShellCompilation.ProjectDiagnostics.md) from canonical unit decisions and bound call links. Distinguish semantic, shaping, dependency, target, input, and integrity causes; preserve final rejection/retention and operation exit status. Retain exact available coordinates and mark synthetic call locations unavailable. Provider contracts flow through final explanation shaping. Diagnose still rejects missing or stale artifact evidence.
- [x] Give typed executable and Hybrid module PDBs stable `/_/src/<document-id>` paths with authored-source SHA-256 checksums. Remap Strict top-level sequence points from the compiler's synthetic entry wrapper to authored lines. `project debug-plan` verifies the current build receipt, artifact inventory, PDB, and source hashes before returning the source-file map; it does not itself prove an editor session.
- [x] Complete the [module consumer quickstart](PowerForge.PowerShellCompilation.ModuleConsumers.md) and qualify additional supported-host diagnostics. The same Hybrid sample passes project explain/test/diagnose and direct import on PowerShell 7.6.5/`net10.0` and Windows PowerShell 5.1.26100.9444/`net472`, with typed and retained commands plus manifest export filtering. The [library NuGet quickstart](PowerForge.PowerShellCompilation.LibraryPackages.md) and executable [run/watch quickstart](PowerForge.PowerShellCompilation.Development.md) remain separate. These Windows host checks do not qualify other platforms or source debugging.
- [x] Requalify the exact candidate: the canonical compiler gate passes 1,139/1,139 tests and the Strict corpus passes 6/6 Windows programs. The `net472` PowerForge.PowerShell and `net10.0` CLI Release builds have zero warnings or errors. The assessment records the run identities and limits.

**Exit gate:** the documented executable and module development paths cover init → lock → restore → run or test → edit → rebuild → diagnose → pack as applicable to each artifact. Their qualified cases cover argument quoting, redirected streams, cancellation, offline restore, stale-output prevention, cache invalidation, and diagnostic source-line accuracy. This gate does not claim an interactive source debugger.

An M29 standalone probe exposed a separate Windows long-path robustness gap: project-isolated restore found its packages, but the generated project build could not copy existing assets through 265–294-character package paths. Direct reviewed-lock build of the same source succeeded through a shorter generated path. Diagnose and fix the project-owned restore/build path before expanding the M28 development-loop claim to deep workspace locations; this does not invalidate the previously qualified bounded short-path cases.

## Milestone 29 — Broaden complete script and module coverage

**Status: Complete within the pinned local portfolio; distribution, provider, platform, and speed gates remain separate.**

**Outcome:** remove recurring semantic blockers so real functions in different modules and scripts compile and run with preserved behavior. This is a multi-wave coverage milestone; neither a PSSharedGoods-only count nor a three-workflow demonstration closes it.

- [x] Freeze the September 22 **196/282** PSSharedGoods packet and qualify its eight recent `-as` additions through original/generated module execution on PowerShell 7.6 and Windows PowerShell 5.1. These eight are part of the baseline, not new M29 gains.
- [x] Close the first Hybrid indexed-target wave without losing prior admissions: pinned PowerInfoblox moved **41/66 → 44/66** and PSSharedGoods **196/282 → 199/282** complete emitted functions. At that freeze, one new PowerInfoblox function had both-host original/generated execution; five other new methods were unexecuted. The optional AD-typed function stays hosted and its safe region survives. The corrected candidate passed 1,139/1,139 compiler tests and 6/6 Strict programs. This was the first required blocker family and **1/30** newly executed functions at that checkpoint.
- [x] Qualify the second Hybrid dynamic-format wave on two hosts. Pinned platyPS moved **20/40 → 22/40** complete emitted functions with no lost method; private `GetRange` matched original execution, while the potentially mutating `New-ExternalHelpCab` remains unexecuted. A public platyPS module workflow passed but its tested public command remains hosted. Rebuilt PowerInfoblox artifacts also qualified generated `Get-InfobloxNetwork` and `Invoke-InfobloxQuery` through an offline module workflow. The full gate passed 1,141/1,141 compiler tests and 6/6 Strict programs. Cumulative progress at that checkpoint was **4/30** newly executed functions across two module packets and two blocker families.
- [x] Qualify a pinned second Evotec full-module workflow through offline providers. Generated `Get-ComputerFirewall` and `Get-ComputerTime` matched the original PSSharedGoods module on both hosts across positive, repeated, empty and missing-data cases; `Get-WinADDSAGuid` remains unqualified after a zero-output exploratory probe. Focused tests passed 2/2 and the full gate passed 1,143/1,143 compiler tests plus 6/6 Strict programs. At that checkpoint progress was **6/30** newly executed functions across three module packets and two blocker families; this test-only wave added no language admission.
- [x] Pin a reproducible portfolio with source revision, archive/tree hash, license and redistribution disposition, loader/dependency closure, artifact kind, target framework, semantic profile, and per-function disposition. Include PSSharedGoods, PowerInfoblox, another unrelated Evotec module, at least two independent third-party modules from different scenario families, and one standalone script/application. PowerInfoblox, MIT-licensed PSScriptTools, and platyPS 0.14.2 have pinned, built, and executed packets. The unchanged external Fibonacci standalone application has functional original/generated proof on its claimed host; its repository has no identified license, so its source remains inspect-only and external. Apache-2.0 powershell-yaml 0.4.12 is pinned for read-only discovery, but its top-level enum currently blocks all **14/14** functions on both target frameworks and binary-loader closure is outstanding. The five-module, two-target [portfolio ledger](../Benchmarks/PowerShellCompilation/Corpus/m29-portfolio-ledger.net10-net472.json) now records the exact per-packet license/dependency and per-function dispositions; unknown or unpinned licenses remain explicitly non-redistributable. Existing external assessment packets remain candidates. Never import or execute third-party source during discovery.
- [x] Establish bounded source closure for the pinned PSScriptTools module-scope `Get-ChildItem -Exclude | ForEach-Object { . $_.FullName }` loader. The final two-target census discovers 73 source files and 109 functions, emits 22 complete methods, and retains fail-closed Strict behavior. Reviewed dependency locks and full-module builds on `net10.0`/`net472` support a deterministic public workflow. Eleven compiled native functions matched original execution on PowerShell 7.6 and 5.1, with positive and edge cases. The final canonical gate passed 1,143/1,143 compiler tests and 6/6 Strict programs. The [assessment](PowerForge.PowerShellCompilation.Assessment.md) records the command-resolution assumption, optional resources, exact gate evidence, and unqualified functions.
- [x] Settle the fourth-family native `@(foreach ...)` statement-array wave. Final-source offline original/generated PowerInfoblox and PSSharedGoods full-module probes match on PowerShell 7 and Windows PowerShell 5.1; the corrected five-packet, two-target census loses no previously emitted method. The first full gate failed five tests, including a real stable-vector region regression. After the selector correction, the replacement gate passed **1,145/1,145 compiler tests** and **6/6 Strict programs**. The [assessment](PowerForge.PowerShellCompilation.Assessment.md) records both runs and the exact final evidence.
- [x] Record same-input Hybrid census and canonical per-function blocker explanations for every packet, with complete emitted, retained, region-only, rejected, and input-failure counts. Keep a per-function ledger and classify co-blockers. Check each packet's previous disposition baseline, even when its aggregate count improves. The five Locksmith regression records represented one `Test-IsADAdmin` binary-surface error in the earlier baseline; its unchanged basic function now has a validated hosted disposition and the unsafe admin-check qualification probe is removed. The Scoop `Test-IsAdministrator` accounting correction retains an emitted method with no hosted command regions. The Hybrid-module binder now preserves native prefix regions and independently recovers Locksmith2 `Test-IssueExists`'s detached terminal region while leaving its unresolved authored type and whole function hosted; Hybrid executables remain without typed regions. The fresh read-only seven-workload assessment passes with zero per-unit regressions; it does not execute AD or installer paths. The historical exploratory Locksmith 31/44 versus current 13/44 gap was reproduced on the exact pinned source: 18 earlier native-bound methods had unresolved authored AD parameter types and are correctly retained under the later safety rule. The pinned baseline was 1/44. The final-source ten-census ledger records all 565 source functions on both targets, including complete, retained, region-only, rejected, input-failure, blocker and co-blocker counts; exact per-unit comparison found zero regression against the accepted packets. The canonical census owns those explanatory diagnostics, so a duplicate explain pass is not an additional acceptance gate.
- [x] Qualify recurring blocker families by the portfolio's shared semantic shapes, effects, ownership, and lifetime. The accepted local waves cover **30/30 distinct newly emitted and differentially executed complete functions** across four families: native indexed targets, dynamic format templates, bounded source closure, and native statement-array capture. The packet totals are PowerInfoblox 7, PSSharedGoods 7, platyPS 1, and PSScriptTools 15, so no module supplies more than half. The fixed same-input five-packet, two-target ledger has no lost previously emitted method. Partial regions and unexecuted emitted functions receive no count. The standalone workflow and portfolio records are documented below and in the assessment.
- [x] Prove at least **three complete module workflows**: one in PowerInfoblox, one in another Evotec module, and one in an independent third-party module, plus one standalone script/application workflow. PowerInfoblox's offline query workflow, PSSharedGoods' offline firewall/time workflow, and PSScriptTools' public compiled calls have both-host original/generated full-module evidence. A platyPS public import/metadata workflow also passed, but its tested public command is hosted and the compiled helper was invoked separately. The unchanged offline Fibonacci application now has a reviewed-lock Hybrid executable with one emitted native-bound `Get-Fibonacci` method; its original/generated success output and exit status match on the claimed generated `net10.0`/Windows x64 host. Its recursive calls take roughly 27 seconds in the generated executable versus under a second in the measured original PowerShell 7 run, so this is **functional coverage only**, with a specific M31 performance follow-up. The external source has no identified license and is not redistributed. The [assessment](PowerForge.PowerShellCompilation.Assessment.md) records exact provenance, manifest, runtime, and build limitations. The offline IPv4 candidate separately rejects nested `exit` during reviewed-lock packaging and remains unqualified; preserve the fallthrough/no-output distinction exposed by the Braille candidate. Use unchanged source, ordinary original/generated imports or launches, and deterministic offline providers for network, directory, WMI/CIM, or destructive actions. Compare values and CLR types, order/cardinality, relevant streams, state, errors, continuation, stopping, cleanup, and repeated calls on supported hosts. Do not run an untrusted or mutating administration path merely to increase a count.
- [x] Implement reusable contracts in the canonical parser → binder → bound IR → analysis → lowering → backend owners. Preserve safe Hybrid fallback and fail-closed Strict admission. Claim runtime-free growth only after a separately qualified complete Strict artifact; do not add source-name policy or emitter-side eligibility rules.
- [x] After each wave, rerun the fixed portfolio, focused differential tests, and canonical compiler gate. Show no loss of any previously qualified function or workflow, reconcile explain/diagnose with executed artifacts, and record unresolved co-blockers. Review hard-to-observe semantic changes before closure. The final-source gate passed 1,145/1,145 compiler tests plus 6/6 Strict programs; the [assessment](PowerForge.PowerShellCompilation.Assessment.md) records exact hashes, focused 5/5 proof, the independent review, and the local-only limits.

**Exit gate:** the pinned portfolio has at least 30 new, differentially executed complete functions across at least three module packets and four recurring blocker families, with one third-party module and PowerInfoblox represented; three module workflows and one standalone workflow pass on their claimed supported hosts; and every earlier pinned per-function disposition either remains valid or has a documented, validated correction. The former **226/282 PSSharedGoods** target remains a useful same-input trend, not the sole M29 completion gate. If a selected packet cannot be safely executed, replace it for execution qualification through a reviewed, re-pinned portfolio revision and show both old and new baselines. Keep inspect-only external sources external when their licenses do not permit redistribution. Counts do not imply general PowerShell compatibility, runtime-free execution, or speedup.

## Milestone 29a — Make ordinary PowerShell forms useful

**Status: In progress.** M29 proved bounded real-workload gains, but that does not mean a familiar cmdlet name works in every form. A fresh one-function Hybrid build showed that the module quickstart's `Get-Date -Format yyyy` already sits inside an emitted CLR method with a hosted command region. The new literal-format binding makes a qualified call, or an unqualified host-free Strict call, runtime-free. In a Hybrid PowerShell host, an unqualified command still respects aliases, functions, imports, and session command precedence; its name alone cannot authorize a direct CLR rewrite.

The [observed support-gap inventory](PowerForge.PowerShellCompilation.SupportGaps.md) separates the final pinned module census, concrete blocked forms, and deliberate hosted boundaries. It is the starting evidence for this milestone; its affected-unit totals are not promised unlock counts.

**Outcome:** make common, reachable script forms compile without changing their observable behavior. Track a command by its invocation shape, result type, effects, mode, and resolution context. Distinguish a runtime-free operation from a generated C# method that still invokes PowerShell, and from an entirely retained function. Do not promise a CLR rewrite of arbitrary cmdlets or treat a whole-method emission count as proof that commands inside it are native.

- [x] Build a compact support matrix from the parser/binder and same-input explanations of all five pinned modules. The [inventory](PowerForge.PowerShellCompilation.SupportGaps.md#current-pinned-portfolio-and-ordinary-script-forms) separates authored shape, command identity, cardinality/effects, Hybrid and Strict dispositions, and safe versus provider-owned paths. The current `net10.0` view covers all 565 stable function IDs with 398 complete methods versus 341 at M29; 167 remain hosted, with no lost prior method or retained region. Three ordinary standalone forms demonstrate direct Strict admission versus hosted Hybrid shaping and Strict rejection. These are analysis counts, not newly executed-function totals or a fresh full `net472` portfolio gate.
- [x] Qualify literal `Get-Date -Format` under a safe command identity. The quickstart keeps its emitted Hybrid method and hosted command lookup; original/generated type, integer conversion, culture, year-boundary, rejected bad/named formats, and session shadowing were checked on the claimed hosts. The no-argument rule is unchanged. The focused gate and exact limits are in the assessment.
- [x] Admit one non-whitespace literal string `[OutputType('Name')]` only on targets with advisory-metadata support, without seeding a return type. On pinned unchanged PSScriptTools source, both targets gain 24 complete emitted methods with zero previously emitted methods lost; a public offline function's original/generated value, CLR type, and `Get-Command` output metadata match on PowerShell 7 and 5.1. This is method emission plus one executed workflow, not 24 newly executed functions; many methods retain hosted command regions.
- [x] Complete the bounded authored `[OutputType]` metadata family for Hybrid binary modules: multiple literal string or resolved CLR type names, repeated attributes, and one literal `ParameterSetName` per attribute now survive binding, lowering, and generated cmdlets without seeding semantic return inference. Pinned PSScriptTools rose from 46/109 to 57/109 complete emitted methods on both targets with zero loss; two newly emitted functions matched original execution on both Windows PowerShell hosts. The other nine new methods remain unexecuted, and dynamic, mixed, unresolved type expressions and runtime-free advisory collections remain fail-closed. [PowerShell documents output metadata as advisory](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_functions_outputtypeattribute?view=powershell-7.6); exact proof and limits are in the assessment.
- [x] Qualify the bounded Hybrid native `[string]` parameter `switch` shape. Unchanged PSSharedGoods gained two complete methods on each target with zero lost methods/regions; `Convert-Size` matched original/generated offline values and types on both Windows PowerShell hosts. A removed-and-recreated parameter remains hosted. The result does not admit object collections or automatic-variable observation.
- [x] Qualify direct Hybrid `[Array] = foreach (...)` output capture through the existing native collector. Unchanged PSSharedGoods `ConvertFrom-ScriptBlock` gained and executed a complete method on both Windows PowerShell hosts, with exact zero/one/many and non-executed script-block cases. Same-input PSSharedGoods methods rose 207/282 → 208/282 on `net10.0` and 206/282 → 207/282 on `net472`, with no lost prior methods or regions of retained functions; CleanupMonster stayed 43/68 on each target. The promoted PSSharedGoods method replaces its former partial region. Strict remains fail-closed, and no AD path was executed.
- [x] Qualify direct Hybrid literal member assignment from conditional statement output. Unchanged PSSharedGoods `Get-ObjectPropertiesAdvanced` gained and executed a complete method on both Windows hosts, while retaining three hosted command regions. Same-input PSSharedGoods methods rose 208/282 → 209/282 on `net10.0` and 207/282 → 208/282 on `net472`, with no lost prior methods or regions of retained functions; CleanupMonster stayed 43/68. Focused zero/one/many/error probes and additional receiver-shape probes matched original/generated behavior. Strict still rejects the isolated source; local-command stream transfer remains a separate contract.
- [x] Admit one ordinary named hosted command as a hashtable or `[pscustomobject]` literal value in Hybrid native methods. Five same-input `net10.0` module explanations rose 380/565 → 389/565 complete methods with no previously emitted function or still-retained region lost. Both-target PSScriptTools builds emitted 59/109 methods, and the unchanged offline `Get-PSProfile` workflow matched original/generated output on PowerShell 7 and 5.1, including session command shadowing. The other eight new methods were not executed, and the command itself remains PowerShell-hosted. Strict and multistage command pipelines remain closed. Exact packet results and limits are in the assessment.
- [x] Qualify the bounded Hybrid command-result `switch` with literal string labels and closed clause bodies. The invocation-owned condition capture preserves zero/one/many success records, case-insensitive matching by default, `break`, and failure before clause execution; command resolution remains in the PowerShell host. The unchanged PowerInfoblox two-function packet emits both methods and matches 13 normal/shadowed-helper cases on PowerShell 7 and Windows PowerShell 5.1. The same-input full `net10.0` packet rises 51/66 → 52/66 without lost methods or retained-function regions. Strict rejects the source. This is one compiled caller with a hosted command boundary, not direct translation of its helper.
- [x] Admit literal comma-split output inside those closed switch clauses. Unchanged `Get-InfobloxDNSRecordPreferredField` now emits with one hosted command region and matches 14 original/generated offline cases on both Windows hosts, including 0/2 helper results, field lists, mixed case, and failure. The same-input full PowerInfoblox `net10.0` packet rises 52/66 → 53/66 without lost methods or retained regions; `net472` reaches 53/66 from its older 51/66 checkpoint with both command-result switch gains and no loss. Strict remains closed; other split patterns and clause effects are not implied.
- [x] Transfer an authored `return` through Hybrid native captured statement output to the enclosing function. Direct `foreach` assignment releases prior and returned records; `@(...)` discards its pending records; authored `finally` still runs and `catch` does not consume the transfer. Unchanged PSSharedGoods `Convert-UAC` gained a complete method and matched five original/generated offline cases on both Windows hosts. At that checkpoint same-input PSSharedGoods `net10.0` rose 211/282 → 212/282 with no lost methods or retained regions; `net472` reached 211/282 from an older 206/282 checkpoint with no loss. Strict remained closed. `Convert-BinaryToIP` still had a direct-range blocker at that checkpoint, resolved in the following slice.
- [x] Qualify direct Hybrid native `foreach` over the host-owned `..` range result. The native range and enumeration binders preserve ascending/descending, string-endpoint, early-break, and invalid-endpoint behavior on PowerShell 7 and 5.1. Unchanged PSSharedGoods `Convert-BinaryToIP` now emits with two hosted warning sites and matches seven offline original/generated cases per host, including the null-versus-no-record return distinction. Same-input PSSharedGoods rises 212/282 → 213/282 (`net10.0`) and 211/282 → 212/282 (`net472`) with no lost method or retained region; Strict stays closed. This affected-packet result does not refresh the five-module portfolio total or qualify unbounded ranges.
- [x] Select Hybrid native lookup for a locally constructed hashtable or ordered map only when its value use is outside the existing typed direct-return or assignment route. Five unchanged PSSharedGoods functions now emit and match 27 original/generated offline cases per host, including script-cache identity and three Exchange recipient parameter sets. Same-input complete methods rise 213/282 → 218/282 (`net10.0`) and 212/282 → 217/282 (`net472`) with no earlier method or retained region lost. Focused tests keep typed array indexing and typed dictionary direct returns non-native; Strict still rejects the five-function source. This is host-backed map behavior, not runtime-free indexing.
- [x] Qualify Hybrid invocation-owned interpolated assignment keys made from variables or direct property reads. Unchanged PSSharedGoods `ConvertTo-FlatObject` now emits and matches recursive object/map/array flattening, depth limits, exclusions, and pipeline input on PowerShell 7 and 5.1. Commands, method calls, background pipelines, and nested traps in the key remain hosted. The shared binder/runtime predicate preserves authored native assignment timing; this is not a CLR rewrite of PowerShell string conversion. `Get-ComputerPorts` also gains emission in the affected packet but has no execution claim.
- [x] Preserve known scalar branch-specific return types through the hosted module's `Object` return ABI. Unchanged PSSharedGoods `Convert-ExchangeItems` now emits and matches eight original/generated offline calls per Windows host, including parameter conversion errors. Same-input `net10.0` PSSharedGoods moves 219/282 → 220/282 with no lost emitted method or promoted region; `net472` reaches 219/282 from 218/282. Runtime-free Strict library output unions remain rejected. This is one additional executed function, not a refreshed five-module count. An object-valued `Convert-ExchangeSize` switch was separately rejected after a parameter-rebinding probe exposed different behavior.
- [ ] Qualify broader command-result switches only when a reachable workflow needs them: `continue`, observed `$_`/`$switch`, nonliteral or regex/wildcard labels, and command-bearing clause bodies require their own iteration, automatic-variable, and effect contracts. Switch-item observation is now limited to reads within the switch; the unchanged PSScriptTools packet gained no method because five affected functions expose object-valued inputs or other blockers. Reads inside a switch and regex `$Matches` after it remain guarded. Resolve the input's runtime type, cardinality, and rebinding contract before admitting these functions; do not count corrected diagnostics as coverage. Broader captured-statement-output and return-transfer shapes remain open. Candidate stream and pipeline commands such as `Write-Verbose`, `Where-Object`, `ForEach-Object`, and `Select-Object` still need resolution, stream, script-block, and provider contracts per invocation shape. Keep filesystem and external-module operations hosted until their effect/dependency contracts exist.
- [ ] For each promoted shape, run an unchanged-source original/generated module or standalone workflow on every claimed host. Check output values and types, order/cardinality, relevant streams, errors, state, and repeated invocation. Use a same-input reachability check and affected-packet no-loss comparison for a narrow command shape; run the fixed full portfolio census when shared resolution, eligibility, region ownership, source closure, or an integration candidate can affect other packets. Record stopped and partial runs as such. Count only newly executed behavior, not synthetic emission or a hosted Package launch. Keep Strict fail-closed and require a separate complete Strict artifact before calling a shape runtime-free.
- [ ] Use the focused compiler lane for routine feedback and only the affected artifact/host proofs for each wave. Run the broad lane for an integration or release candidate, or when a change reaches contracts it alone covers; do not impose a 50-minute matrix on each command-shape edit. Record elapsed time and the specific behavior each selected check protects.
- [x] Reconcile the older typed-module-state fixture with `PSB2240`. Direct returned `.Length` and simple indexed reads use Hybrid native invocation, preserving authored conversion callbacks; original/generated module results match on PowerShell 7 and 5.1. The guard still rejects these casts without native binding and does not admit derived-value escape. This synthetic fixture does not increase the pinned-module count.
- [x] Reconcile four older module-state fallback assertions that failed identically at the pushed baseline. Native-bound original/generated modules now match the local call, converted-state pipeline, local and caller-resolved command result, and deterministic effectful index on PowerShell 7 and 5.1. The old negative assertions were stale; the nested cast remains a fallback test. The selected 18-case module-state set passes. This is a test disposition correction, not four new compiler gains or a broad-lane result.
- [x] Qualify a direct string parameter passed to an unambiguous static CLR method with one required integral argument through Hybrid's existing PowerShell invocation bridge. Unchanged PSSharedGoods `Convert-ToDateTime` now emits and matches five offline original/generated cases on PowerShell 7 and 5.1, including invalid input and catch behavior. Its affected full-module packets rise 218 → 219/282 (`net10.0`) and 217 → 218/282 (`net472`) without losing a baseline emitted method or a region still owned by a hosted function. Strict remains closed; overloaded methods and unrelated CLR conversions are not implied. The last whole-portfolio count remains 398/565 pending a new five-packet census.
- [x] Identify the standalone dynamic-date blocker at the correct owner. The same command in a named Hybrid function can emit with a hosted command region, while a semantically eligible Hybrid executable script root remains hosted by artifact shaping. Final explain now reports `artifact.executable-script-root` instead of an empty causal list for this case; this is diagnostic accuracy, not new compiled execution.
- [ ] Implement and qualify the [Hybrid executable entry contract](PowerForge.PowerShellCompilation.HybridEntryContract.md) for an eligible script root. Preserve authored parameter binding/defaults and `$args`, command-resolution timing, source/dependency declaration order, success/error/other streams, `return`/`exit`, stopping and cleanup, and repeated invocation before routing any top-level statement to CLR. Begin with a safe unchanged-source standalone workflow and an explicit hosted fallback for unsupported roots; compare ordinary original/generated launches on the claimed host. Keep root compilation separate from named-function emission counts. Do not infer root support from a hosted Package launch.
- [x] Prove the first private Hybrid root path for one directly invoked, qualified `Get-Date` or `ConvertTo-Json` command with literal or prebound string arguments and no source declarations or transfers. Same-input explain/build now mark that root emitted; ordinary `pwsh -File` and generated `net10.0` launches matched omitted and explicit date formatting, omitted string binding, and the invalid-date exit/no-success result on Windows x64. The compiled root still calls a hosted PowerShell command region. Other commands, arbitrary statement forms, script blocks, richer parameter metadata/types, lifecycle transfers, other streams, dependency declarations, and other generated hosts remain on the open entry-contract item above.
- [x] Extend that private root to an ordered sequence of the same direct qualified commands. A three-command date/JSON/date script now emits one root method with three hosted command sites; same-source original/generated `net10.0` launches match omitted and explicit inputs, output order, and invalid-date continuation on Windows x64. Only the first command may read a prebound string parameter: an earlier `-OutVariable` can change it before a later command runs, and the generated frame does not yet transfer that state. A focused explanation keeps that rebinding case hosted. Explain and built disposition agree. An eligible root containing `Write-Output` remains hosted with `artifact.executable-script-root`; this does not admit arbitrary multi-statement scripts, variable transfer, pipelines, or a runtime-free command.
- [ ] Measure the CleanupMonster explain path before prioritizing another broad census: one earlier same-input `net10.0` explanation took roughly 16 minutes, and the final indexed-map five-packet check took 12m56s for CleanupMonster while other explanations and focused work also used this machine. Neither is a controlled baseline/candidate pair; compare the same source under matched load before attributing a performance regression.
- [x] Distinguish an immediate nested catch's `$_`/`$PSItem` from a switch item. Both-host original/generated artifacts preserve the caught error; post-catch item reads remain hosted. `Resolve-Encoding` now exposes its object-valued condition without a false switch-item diagnosis. This adds no pinned-module coverage count.
- [x] Qualify bounded runtime-valued exact switches through the invocation owner. Unchanged offline `Convert-ExchangeSize`, `Resolve-Encoding`, and `Convert-Color` now emit and match original/generated execution on PowerShell 7 and 5.1. Probes cover parameter removal/recreation, empty and multi-valued adapted member results, null/scalar/collection input, matching order, case sensitivity, per-clause item conversion and mutation, current-item/iterator restoration, and direct/callback transfers. Strict stays closed. Independent review found and reproduced a compound-pipeline array-record defect; the corrected binder keeps unqualified command conditions hosted rather than collapsing their success records. Same-input PSSharedGoods `net10.0` rises 222/282 → 225/282 without lost methods or retained regions. This does not refresh the full `net472` packet or five-module total.
- [x] Qualify the same runtime-value owner in an independent module. Pinned unchanged PSScriptTools `New-CustomFileName`, `New-ANSIBar`, `Format-Value`, and `Format-String` now emit and match original/generated mini-module execution on both Windows hosts, including nested switches and multiple pipeline records. The affected full `net10.0` packet rises 59/109 → 63/109 with all 121 unit IDs and no lost methods or regions. Focused checks passed 4/4 in 28 seconds. This test/fixture wave changes no compiler source, adds no broad lifecycle guarantee, and does not refresh the five-module or full `net472` census. Randomized modes and unexecuted neighboring functions are not qualified by this evidence.
- [ ] Extend switch support only for a reachable workflow beyond that bounded contract. Regex/wildcard/file forms, effectful labels, labeled transfers, broader command-result clauses and compound pipelines need separate matching, success-record capture, state, and transfer proof. Error-path and nested-switch restoration and conversion-error source positions were reviewed statically but are not new artifact execution claims. Keep those limits explicit; use the existing host owner rather than assuming a familiar member returns one string.

**Exit gate:** the support matrix and explain output agree with executed artifacts; the quickstart's formatted-date function executes as an emitted method with any hosted command boundary stated explicitly, and a separately qualified form proves runtime-free formatting if that is claimed; at least two unrelated pinned module workflows and one ordinary standalone script demonstrate the selected common forms on their claimed hosts; and same-input per-function comparison shows no unaccounted loss. If the formatted-date function remains entirely hosted, this milestone stays open with the specific blocker recorded. Remaining common gaps have named semantic or provider blockers and a priority based on real workload impact. This milestone adds no general cmdlet-compatibility or performance claim.

## Milestone 30 — Qualify distribution, providers, and platforms

**Status: Partial; public release and target qualification remain separate from local implementation.**

- [ ] Reconcile the continuation with the then-current default branch, inspect conflicts at shared owners, and run exact-candidate CI/review before integration. Do not infer readiness from the branch's earlier main merge or local test result.
- [x] Split routine compiler feedback from deliberate full qualification. The former 1,145-test run took 51m35s locally, including 51.2 serial minutes in one artifact-builder test class. With the test assembly already built, the focused lane passed 272/272 selected contract tests in 3m58s plus six Strict programs, about 4m48s end to end on the same machine. The broad artifact matrix remains available with `-Lane Full` and a manual CI dispatch. The [assessment](PowerForge.PowerShellCompilation.Assessment.md) records both evidence sets and limits.
- [ ] Measure the focused lane on the actual CI runner, including pinned-host acquisition, all required contract families, the available Windows hosts, and the six Strict programs. Check repeatability and the new 20-minute step budget. Run the full lane on the integration candidate when the broader artifact matrix is needed; do not present focused CI as equivalent to that qualification.
- [ ] Complete the compiler/core/CLI/PowerShell/provider release set and clean public-feed install, upgrade, rollback, and execution after publication authorization. Record source, package, installed version, and runtime evidence separately.
- [ ] Exercise package-version upgrades through ordinary restore/rebuild. If an actual consumer requires binary drop-in upgrades, first define assembly identity, TFM, dependency/provider binding, and running-process restart policy, then test both previously compiled and rebuilt consumers. Do not infer this guarantee from the generated ABI hash.
- [ ] Qualify PowerShell servicing updates against the native-function and statement-error bridges, which use private host APIs. Check capabilities before side effects; preserve authored fallback where supplied, otherwise fail with an actionable diagnostic. Test missing/changed contracts and a new patch candidate before changing pins.
- [ ] Partition native Linux validation from Windows-only signing, management, net472, and oracle fixtures. Run actual Linux behavior in its own output tree; cross-publishing alone is not target execution.
- [ ] Promote additional physical RID/deployment profiles only for an actual consumer need. macOS/Arm64, Windows Arm64, and additional deployment forms remain experimental until observed on target.
- [x] Correct the observed plain Strict self-contained Windows x64 closure rejection. The unoptimized, non-single-file `net10.0` probe now builds with reviewed .NET 10.0.11 runtime assets and executes `return 7` on Windows x64. Exact runtime identity/content admission, used type-forwarder resolution, explicit Windows ABI imports, and locked runtime helper inspection retain closure verification. This bounded deployment proof does not qualify optimized output, another RID, or long paths; see the assessment.
- [ ] Perform the disposable management-target reboot/reconnect gate only on a target explicitly placed under that lifecycle. Keep parser/metadata inspection separate from source execution and live provider effects.
- [ ] Exercise long/non-ASCII/space-containing paths, clean machines, offline resources, tampering, interrupted operations, runtime servicing, uninstall, and side-by-side versions. Verify packed generated projects do not acquire ambient build configuration or unintended network inputs.
- [ ] Carry explicit package authorship/license/source metadata and dependency notices through packaging; do not treat a default metadata value as evidence about the input source. Record signing authority, immutable hashes, and servicing policy for the release set.

**Exit gate:** every advertised profile has an obtainable release set, clean install/upgrade/rollback, and executed workload evidence. Missing optional profiles narrow the matrix. Private-host compatibility failures or unresolved package correctness findings block affected promotion.

## Milestone 31 — Accelerate proven workflows

**Status: Planned.**

- [ ] Select hot workflows from M24–M27 and M29 and retain startup, cheap-command, and error-path controls. Compare only modes implementing the same observable contract.
- [ ] Use canonical IR/lowering and the boundary profiler to reduce crossings and allocations. Keep support eligibility independent from a hoped-for speedup.
- [ ] Investigate the qualified Fibonacci standalone workflow's recursive native-call boundary: the generated Hybrid executable returned the correct result in 26.956 seconds, while the measured original PowerShell 7 launch took 0.577 seconds. Preserve identical output/error/exit behavior before optimizing or advertising a speedup.
- [ ] Record fresh-process startup, warm throughput, allocations, peak working set, build/import time, and artifact size with pinned source, toolchain, host, target, run ID, and validated output. Include large-function/many-region compile-time and memory budgets so safe-suffix exploration cannot grow without measurement.
- [ ] Report wins, ties, and regressions against declared budgets. Prefer another execution mode when compilation overhead dominates.
- [ ] Consider another backend only after a measured C#/Roslyn/MSBuild limitation blocks a required outcome and the alternative can consume the same lowered contracts.

**Exit gate:** three unrelated qualified workflow families have repeatable measurements and declared budgets. No language-wide speedup claim follows from a selected workload.

## Deferred developer tooling — authored-source debugging

The compiler emits stable PowerShell source paths and checksums in portable PDBs, and `project debug-plan` authenticates the map. No source breakpoint hit, step, local, stack, or exception behavior has been observed in an editor. Qualify those behaviors across a typed call and a Hybrid hosted boundary before advertising interactive source debugging. This work can resume for a consumer need or a concrete debugging defect; it is not an M29 coverage or M30 release prerequisite.

## How to choose and close the next implementation slice

1. Fix accepted-code defects first; reproduce the original/generated failure and inspect sibling paths.
2. Choose a complete blocked workflow from immutable source with every co-blocker visible. Do not import or execute external administration code merely to analyze it.
3. Implement the smallest contract in the existing owner. Group by semantic shape, ownership, effects, and lifetime rather than function name or one CLR type at a time.
4. Validate real artifacts on claimed hosts, including negative/failure paths. Report values, types, identity, streams, errors, continuation, cleanup, and actual retained boundaries.
5. For hard-to-observe changes, freeze a candidate for one independent read-only review, then resolve validated findings and run focused confirmation.
6. Record exact evidence and remaining limitations in the assessment. Update only current roadmap state, preserve user-owned work, and clean task-created heavy outputs before handoff.
