# PowerShell compiler: next major milestones

Updated: 2026-09-20.

Execution status: M24, M25, M26, and M27 are complete on the continuation branch; M28 is the next dependency-ordered product goal. The branch has completed its correctness/net8-retirement gate, general non-overlapping additional-region selection, bounded collection/value transfer and local-call closure, return/cardinality families, Hybrid-native computed members, complete hosted multi-stage pipelines with retained collection mutation, separate native range/match contracts, bounded target-compatible type literals, a Hybrid-native runtime target-conversion contract, schema-6 fresh-vector mutation lifetime evidence, and practical runtime-free CLR package consumption. Region selection follows semantic, shape, element, direction, ownership, output, mutation, error, ordering, lifecycle, and continuation contracts rather than statement-count heuristics or a presumed performance payoff. Host-bound pipelines and ETS/member behavior remain separate from runtime-free typed transfer. This tranche includes remediation of existing and newly exposed compiler defects, with complete-workflow qualification across different source styles.

The active compiler support matrix contains only `net10.0`/PowerShell 7.6 and `net472`/Windows PowerShell 5.1. [.NET 8 reaches end of support on 10 November 2026](https://dotnet.microsoft.com/en-us/platform/support/policy) and is retired from compiler targets, fixtures, corpus packets, and forward-looking gates. Dated paragraphs below may mention earlier `net8` or PowerShell 7.4 observations; those are historical evidence, not a requirement to preserve that lane.

### Continuation handoff

- [x] Re-run the complete compiler gate after the control-flow, collection-region, value-cardinality, closed scalar/vector-alternative, local collection-factory, computed-member, and complete-pipeline expansions. The canonical test phase passed 988/988 tests with zero failures or skips. Its first post-suite Strict attempt materialized only two executable artifacts; the same no-cache six-program lane then built and executed 6/6 Strict `net10.0` `win-x64` programs from a fresh isolated workspace. The runner caps xUnit collection concurrency at four and gives external compiler probes 120 seconds before killing their process tree, preventing unrelated load-sensitive false failures while retaining bounded hang detection.
- [x] `PowerForge.PowerShell` builds for `net10.0` and `net472` with zero warnings and errors. Focused transfer-contract, synthetic collection-family, and unchanged `Split-Array` tests pass on PowerShell 7.6 and Windows PowerShell 5.1.
- [x] Six-program Strict `net10.0` `win-x64` corpus passes from analyze through execution.
- [x] Real host identities are PowerShell 7.6.5 and Windows PowerShell 5.1.26100.9444.
- [x] A retained Hybrid function can promote a guarded prefix and a second detached statement-aligned region around hosted work. Stable scalar inputs retain their authored constraints and return every mutation; an exact prefix-owned Hashtable may cross read-only into a terminal return. Differential artifacts preserve authored output order, errors, cancellation/stopping, identity, state, and subsequent invocation on both supported hosts.
- [x] Remove the artificial two-statement minimum from scalar prefix and detached-region selection. One-statement regions still require the complete existing graph, error, ordering, stable-scalar, storage-ownership, and continuation contracts; no benchmark result controls admission.
- [x] Execute the newly admitted one-statement prefix from unchanged PSSharedGoods `Convert-IPToBinary` source on both supported hosts.
- [x] Replace the scalar/Hashtable-only transfer decision with schema-versioned contracts for shape, element kind, direction, ownership, output, and mutation. The closed policy admits exact atomic maps, stable-scalar vectors, and read-only exact `System.Array`/`ArrayList`/`List<T>` references; object/ETS transfer, arbitrary enumerators, and compiled collection mutation remain fail-closed.
- [x] Generalize retained definite assignment, later fresh-local ownership, and additional statement-aligned region selection. At that checkpoint the pinned PSSharedGoods census reported 48 promoted regions, 70 candidates, and 19 opportunities without source-name policy. Candidate execution is limited to real functions, and target-runspace reconstruction leaves the authored declaration installed when its optimized alternative cannot be constructed.
- [x] Qualify unchanged PSSharedGoods `Split-Array` on both supported hosts. Its parameter-array early return, fresh `List<object>` construction, and comma/no-enumerate terminal return compile as three regions.
- [x] Carry one structured return-or-fallthrough envelope through binding, bound IR, analysis, lowering, generated C#, runtime composition, and portable evidence. Retained PowerShell executes the authored return and therefore owns one-level enumeration, partial records, error continuation, stopping, and enumerator cleanup. A six-family matrix covers vectors, `System.Array`, `ArrayList`, `List<object>`, atomic maps, and explicit no-enumerate returns on both supported hosts. Direct object/ETS values, arbitrary enumerators, unproved locals, and compiled collection mutation remain fail-closed; the existing injected-enumerator matrix continues to prove `MoveNext`, `Current`, disposal, error-action, partial-success, and continuation behavior at the retained boundary.
- [x] Recover one bounded array/foreach transformation through the same ownership model. An exactly typed one-dimensional stable-scalar capture may collect implicit outputs from one closed non-nullable value-type scalar `foreach`; the generated region transfers the completed vector and final loop variable, while retained PowerShell owns the later one-level enumeration. The unchanged `Get-ADEncryptionTypes` and `Get-ADTrustAttributes` functions pass original/generated differential execution on both supported hosts. Direct object/ETS elements, arbitrary enumerables, explicit output shaping, terminating control flow, local-call collection closure, and compiled-owned collection mutation remain fail-closed. Admission depends on semantic proof rather than crossing count or benchmark speed.
- [x] Add schema-3 `NoValue` and `NullValue` terminal contracts. Bare `return` produces zero success records; `return $null` produces one null record. Both work as terminal regions and as return-or-fallthrough envelopes without treating `void` or arbitrary `object` as a transferable live value. Four authored workflow shapes preserve retained output/error continuation, downstream stopping, repeated calls, and remove/reimport on both supported hosts. The unchanged PSSharedGoods `ConvertTo-ImmutableID` terminal return raises the pinned module census from 47 to 48 promoted regions; unchanged `Find-DatesPastWeek` and CleanupMonster `Get-CloudCacheComputer` prove explicit-null one-record behavior. AutomationNull, dynamically empty subexpressions, arbitrary object returns, and general empty/nested collection cardinality remain outside this slice.
- [x] Add `ClosedValueAlternative` in schema 4 for one exact two-branch initializer whose fresh continuation local is either a supported stable scalar or its matching one-dimensional stable-scalar array; current serialized transfer evidence uses schema 6. The generated helper returns one atomic compiler-owned envelope; retained PowerShell restores the selected authored type constraint before later mutation or enumeration. Switch parameters project to Boolean only for the matched direct condition truthiness; copied switch values keep `SwitchParameter`, and any ordinary whole-function form containing the synthetic envelope remains authored. The unchanged PSSharedGoods `Get-ComputerBios` workflow passes scalar/vector selection, retained mutation, repetition, stopping, and remove/reimport on both supported hosts. Eight sibling `Get-Computer*` commands carry the same structural contract. At that checkpoint the complete census stayed at 48 promoted regions because this extended nine existing prefixes; it did not create nine additional regions. Object/ETS alternatives, object-element or multidimensional arrays, missing/extra branches, mismatched element types, prior ownership, explicit output, compiled mutation, and arbitrary enumeration remain retained.
- [x] Add schema-5 evidence for one exact zero-parameter local helper that allocates a fresh `ArrayList` and returns that reference as one comma/no-enumerate record. Regions lower the proved call to the same constructor while the retained helper keeps its ordinary public command ABI. Exact zero arguments, `ArrayList` result and transfer types, callee-fresh ownership, retained-only mutation, and no compiler enumeration are explicit in portable evidence. Dynamic, unresolved, module-qualified, recursive, closure-bearing, arbitrary-enumerator, and differently shaped helpers stay retained. Four unchanged PSSharedGoods consumers now promote this family; the complete census reports 51 promoted regions, 73 candidates, and 19 opportunities.
- [x] Add one explicit Hybrid-native computed-member contract for instance/static reads and instance assignment targets, including member/index nesting. The canonical binder records receiver, computed name, static target, effects, and host requirements in immutable IR; lowering and generated C# call PowerShell's own dynamic member binder, while authored assignment targets continue through the bounded native AST assignment owner. Receiver/name/index evaluation stays left-to-right and exactly once. Strict, typed regions, runtime-free libraries, null-conditional access, computed method names, and open object/ETS transfer remain fail-closed. Dual-host differential tests cover errors, output, mutation/aliasing, stopping, repetition, and module reimport. In the complete PSSharedGoods census, thirteen unchanged functions move from fallback to emitted, no emitted function regresses, computed-member blocker messages disappear, and the remaining generic member frontier is two occurrences in one function. Two prior partial regions become whole functions, producing 154 emitted functions, 49 promoted regions, 73 candidates, and 18 opportunities.
- [x] Select complete multi-stage pipelines for Hybrid native-function binding even when every command is already recognized as a compiler intrinsic. The existing command-region IR, lowerer, backend, and runtime execute the exact authored pipeline inside the active PowerShell invocation, so aliases, parameter binding, script-block scope, streams, lifecycle, stopping, and collection mutation keep one semantic owner. A terminal explicit success-stream sink such as `Out-Null` remains fail-closed. A compiler-owned invocation feeding a hosted pipeline keeps its narrower typed-continuation route. Strict and runtime-free compilation remain closed. `Get-MyIpAddress` and `Remove-DuplicateObjects` become whole emitted methods while `Get-ObjectEnumValues` retains two promoted regions; the census moves to 156 emitted functions, 48 promoted regions, 73 candidates, and 17 opportunities.
- [x] Close complete Hybrid native consumers of the already-proved zero-parameter fresh-`ArrayList` factory. The binder evaluates the proved factory projection through canonical IR, then assigns the new list through invocation-owned native variable storage; the fallback pass ignores the helper's native binder only for this exact projection. `New-SqlQuery`, `New-SqlQueryAlterTable`, and `New-SqlQueryCreateTable` move from fallback to complete emitted methods, while `Get-ObjectPropertiesAdvanced` keeps its narrower region path. The refreshed census reports 159 emitted functions, 45 promoted regions, 73 candidates, and 17 opportunities. Dynamic, qualified, unresolved, parameterized, recursive, state-capturing, enumerating, and differently shaped helpers remain fail-closed.
- [x] Add distinct Hybrid-native range and regex-match operators through the canonical binder, bound/lowered operator IR, C# backend, and loaded PowerShell runtime owner. PowerShell 7 uses its native range operation, while Windows PowerShell 5.1 converts the left endpoint before evaluating the right and then uses its native integer range operation with the original conversion exception. Differential matrices cover scalar and collection matching, regex objects, culture, case and negation, seeded `$Matches`, invalid regex, inclusive ascending/descending and character ranges, index slicing/cardinality, conversion, overflow, nulls, single evaluation, stream/error state, and stopping. Direct `foreach` range iteration stays hosted until a lazy range-enumerator contract exists. The pinned `Convert-IPToBinary` workflow moves from one promoted prefix to a complete native method without changing behavior on either host.
- [x] Add bounded `TypeExpressionAst` values. A statically resolved type must exist in the selected target framework reference surface, binds as one `System.Type` literal, and emits as `typeof(...)`; unresolved types and .NET 10-only types targeting net472 fail closed. At this checkpoint, direct and transitively hoisted authored type literals passed to invocations under observed native `try/catch` stayed hosted because the pinned SID conversions exposed a caught-wrapper mismatch. The narrower direct/catch-all case is closed below; hoisted values remain hosted.
- [x] Add a Hybrid-native runtime target-conversion path for authored reflection types unavailable to the generated target surface. The active host resolves the authored alias/type before evaluating the operand, then PowerShell's conversion binder owns null, scalar, collection, and error behavior. Strict remains closed. `Convert-DomainToSid`, `Convert-DomainFqdnToNetBIOS`, and `ConvertFrom-NetbiosName` become complete emitted methods. The refreshed pinned census is 171 emitted functions, 40 promoted regions, 70 candidates, and 14 opportunities, with no losses or input failures.
- [x] Select Hybrid native function binding for direct `foreach` over an authored untyped or `[object]` parameter. The existing PowerShell enumerator and invocation-owned iterator storage preserve lazy acquisition, scalar fallback, loop-variable assignment, stopping, and authored cleanup; typed collection loops and direct range iteration keep their established routes. Three unchanged PSSharedGoods workflows now emit complete methods. `Get-ProtocolDefaults` retains its authored function and one typed prefix: its ordered-map key loop is not a direct object-parameter loop, and exploratory whole-function admission exposed a separate dictionary-literal escape guard.
- [x] Preserve the authored method-invocation wrapper in catch-all `$_` for one direct type-literal argument. Native catch matching may select an inner error record; the statement-error owner restores the actual exception only for a base method-invocation record while leaving typed catches and derived records on the native matcher's path. The binder admits only the direct one-argument type-literal shape under catch-all handling. Unchanged `ConvertTo-SID` and `Convert-Identity` become complete emitted Hybrid methods and pass original/generated dual-host checks, including caught-record identity. A transitively hoisted type value remains hosted; `ConvertFrom-SID` has a separate note-property blocker, and `Test-ComputerPort`'s `[ref]` argument remains outside this admission. The pinned census is 176/282 emitted functions with 39 promoted regions and 12 analysis opportunities; the exact-candidate gate passed 1,024/1,024 tests and all six Strict programs.
- [x] Qualify direct map-key `foreach` and native map output as one M24 family. The binder selects the existing invocation-owned loop contract; bound output analysis distinguishes compiler-created `Hashtable`/`OrderedDictionary` literals from borrowed native references; the lowered C# constructor uses the selected host's literal-key comparer. The unchanged `Get-ProtocolDefaults` becomes a complete Hybrid method and passes dual-host original/generated record shape, order, known/unknown/case-varied lookup, repetition, and reimport. Synthetic ordinary/ordered map probes cover culture, mutation, borrowed identity, and collected output; non-native map escape stays hosted. The pinned census is 179/282 emitted functions, 38 promoted regions, 31 retained candidates, and 11 opportunities with no input failures. The complete compiler gate passed 1,032/1,032 tests with no skips and built/executed all six Strict `net10.0` `win-x64` programs; the `net472` Release build has zero warnings/errors.
- [x] Route `-split` and `-csplit` functions through the existing Hybrid native operation and index owners when selected. The unchanged `ConvertFrom-X500Address` now emits a complete method rather than retaining a conversion-only indexed-output residual. Dual-host original/generated comparisons cover its short/full values, empty and null inputs, multiple delimiters, non-ASCII text, repetition, and reimport; a synthetic split/index matrix covers collection input, invalid patterns, errors, and record count. The pinned census is 180/282 emitted functions and 38 promoted regions with no input failures. The complete compiler gate passed 1,036/1,036 tests with no skips and built/executed all six Strict `net10.0` `win-x64` programs; the `net472` Release build has zero warnings/errors. This does not expand Strict admission or close M24's wider exit gate.

This is the next execution tranche of the [compiler architecture roadmap](PowerForge.PowerShellCompilation.Roadmap.md). Milestones 24–30 turn its remaining product gaps into large deliverables with observable exit gates. The [readiness assessment](PowerForge.PowerShellCompilation.Assessment.md) owns dated audit results; the [compilation guide](PowerForge.PowerShellCompilation.md) owns current commands and supported behavior.

## Starting point

The compiler already has the expensive architectural foundations: canonical binding and immutable IR, a C# backend, semantic profiles and oracles, dependency locks, generated projects, CLR ABI metadata, provider contracts, Hybrid state/region support, and bounded Strict managed/NativeAOT delivery. Replacing the backend is not the next step.

The main shortfall is useful coverage. Six authored Strict programs qualify a narrow accepted subset. The broader external baseline has 196 units across seven workloads, with only six emitted units and no complete-workload execution credit. Emitted helpers, retained source, successful packaging, and analysis-only opportunities measure different outcomes. A successful Hybrid package can still execute almost entirely through PowerShell.

The 2026-09-07 audit also reproduced accepted-code errors caused by confusing an expression cast with a persistent variable constraint. Correcting that distinction can reduce emission for programs whose dynamic promotion was never implemented correctly. Coverage must not be recovered by relaxing the fix.

## Delivery order

| Milestone | User-visible outcome | Prerequisites | Relationship to existing work |
| --- | --- | --- | --- |
| 24. Values, collections, and error continuation | Ordinary data-processing functions preserve PowerShell behavior through compilation | Correctness fixes and their regression gate | Expands bounded M11/M18 and supplies M23 transfer contracts |
| 25. Complete pipelines and advanced functions | Complete commands produce every output and preserve failure, stop, and cleanup behavior | M24 contracts for the values and errors crossing each stage | Expands bounded M8/M10; does not replace their owners |
| 26. Stateful modules and multiple Hybrid regions | Real modules retain one correct state owner while substantially more of their bodies compile | M24; relevant M25 stream/lifecycle contracts | Closes the remaining M23 scope |
| 27. Practical CLR library consumption | A PowerShell-authored library is useful from an ordinary C# application | M24; M26 only for stateful libraries | Product qualification of M6/M9/M16 ABI and dependency foundations |
| 28. Source-first development loop | Diagnose, run, inspect, and debug a compiled project from its authored source | Existing project workflow; incremental integration with M24–27 | Builds on M17/M21, source maps, PDBs, and verified cache |
| 29. Reproducible distribution and qualified platforms | A new user installs a supported toolchain and runs its artifacts on a clean target | A declared supported workload/profile set; release authorization | Closes M19/M21/M22 external gates without duplicating them |
| 30. Measured acceleration of useful workloads | Qualified programs have predictable performance and deployment tradeoffs | Correctness and execution qualification of each measured workload | Extends M15/M22 optimization after semantic growth |

Start implementation with M24. Discovery and developer-tool design can proceed alongside it. Platform and public-feed qualification are independent lanes once the selected artifact contracts stabilize; they do not prevent source-level correctness work. M30 is a later optimization phase, not an admission requirement for M24–29.

Each milestone spans coherent implementation slices. A slice must carry binding, bound/lowered contracts, diagnostics, affected artifacts, and executable proof together; do not land a new emitter interpretation and promise semantic closure later.

## Milestone 24 — Preserve values, collections, and error continuation

**Outcome:** compile useful data transformations without changing what a caller observes when a value promotes, an enumerable fails, or a later statement continues.

- [x] Represent value type and variable constraint separately. Bounded unconstrained numeric promotion now preserves integral widths, Single/Double, mixed-width identity, division/remainder, narrowing, compound updates, increment/decrement, and overflow through canonical contracts. Unsupported cases remain explicitly retained/rejected; an RHS cast is not a declaration.
- [x] Close null, `AutomationNull`, scalar, singleton, nested-array, and empty-output cardinality across assignment, `@()`, comma expressions, return, indexing, and foreach. Dual-host array-copy/grouping, collected-output, return-envelope, conditional-value, and local-call matrices preserve exact enumeration depth, identity, and order.
- [x] Model collection enumeration and disposal, including partial success before acquisition, `MoveNext`, `Current`, or cleanup fails. The retained owner preserves PowerShell error identity, `ErrorAction`, prior state, partial records, stopping, cleanup, and statement continuation.
- [x] Introduce one structured transfer owner for exact atomic dictionaries, one-dimensional stable-scalar vectors, and read-only exact `System.Array`/`ArrayList`/`List<T>` references. Carry direction, provenance/ownership, output enumeration, and mutation explicitly across analysis, selection, generated metadata, and portable corpus evidence.
- [x] Add a return-or-fallthrough envelope for parameter-borrowed values from those closed families. The helper never enumerates the return value; retained PowerShell preserves its authored output, error, stopping, disposal, and statement-continuation behavior.
- [x] Add one closed stable-scalar `foreach` capture for a fresh, exactly typed one-dimensional array. Binding owns capture shape and element proof; analysis and lowering preserve internal success writes; the backend collects them into a typed vector; and the existing transfer/retained-enumeration owners carry the result across the region boundary. Integral truthiness, non-null value-type loop assignment, and nonthrowing integral bitwise operations close the two selected real workflows without granting general enumeration.
- [x] Close bare-return versus explicit-null cardinality at terminal and conditional retained boundaries. The transfer ABI records `NoValue` as zero records and `NullValue` as one atomic null record; direct-null analysis no longer routes through hosted collection enumeration. `void` live values, arbitrary object return values, AutomationNull, and dynamically empty subexpressions remain rejected or retained.
- [x] Admit one bounded local-call collection closure through the same canonical owners. A source-proved zero-parameter helper allocates one fresh `ArrayList` and returns it as one explicitly non-enumerated record; region and complete Hybrid-native consumers record exact parameter/result types and callee-fresh ownership, then lower the call to the equivalent constructor. A complete native consumer writes that constructed value through invocation-owned variable storage instead of calling the helper's PowerShell parameter binder. Four unchanged workflows share this structural contract: three SQL consumers now emit as complete methods and the ETS-heavy consumer retains its narrower region. Generic artifacts prove type, cardinality, identity, retained mutation, errors, stopping/reuse, and module reimport on both supported hosts.
- [x] Extend computed object/member/index access and alias-visible mutation inside Hybrid native-function bodies through PowerShell's own binder. Complete hosted pipelines can also mutate retained Hashtable, OrderedDictionary, ArrayList, generic List, and array references without transferring mutation ownership to generated CLR code.
- [x] Extend typed-array conversion and compiled-owned collection construction through schema-6 transfer/lifetime evidence. Only a fresh one-dimensional stable-scalar vector receives `CompiledOwned` authority, and only from region construction until live-out transfer; retained PowerShell owns later enumeration and mutation. Borrowed/live-in arrays, maps, lists, ETS, object-element or multidimensional arrays, and arbitrary enumerators remain hosted or rejected.
- [x] Select three complete data-processing workflows from the pinned discovery frontier. Record every co-blocker before implementation so isolated helper support is not mistaken for a workflow unlock.

**Exit gate:** three previously blocked complete workflows from at least two unrelated families execute original/generated differential matrices. Include empty, singleton, nested, null, overflow, malformed-input, and injected enumeration-failure cases. Prove values, CLR types, output count/order, errors, continued statements, and disposal on each claimed host. At least one workflow must pass as a Strict runtime-free artifact; Hybrid results must identify the operations that remain hosted. A higher emitted-unit count alone does not close the milestone.

**Next slice:** begin M28 from the existing project, diagnostics, cache, source-map, package, and consumer owners. Do not reopen the qualified M24-M27 semantic or package contracts to manufacture a run/watch/debug surface. The final M27 checkpoint passes 1,063/1,063 compiler-gate tests with zero failures or skips, builds and executes all 6/6 Strict `net10.0` `win-x64` programs, and builds `PowerForge.PowerShell` for `net472` Release with zero warnings/errors. A fresh same-input census remains 188/282 emitted functions with 36 promoted regions and no input failures; M27 productizes consumption without claiming a semantic coverage increase. A hosted stage or promoted region is not a runtime-free artifact; direct range iteration, borrowed compiled collection mutation, runtime-free ETS transfer, and unregistered provider-bound live dependencies remain fail-closed.

**Current owner-level implementation packet:** the full-module Hybrid explain/census before this slice reported 180/282 emitted functions, 38 promoted regions, no input failures, and 89 `syntax.unsupported` occurrences affecting 78 units. That feature ID aggregates unrelated failures; its 69 visible sole-blocker units were not an unlock forecast. The repeated diagnostics were 15 `[pscustomobject]` note-property values that are not one scalar expression, 14 native value collections containing non-pipeline statements, and 10 dictionary values that are not one side-effect-free scalar expression. The affected functions overlap. A shared statement-valued expression/capture contract now serves these consumers instead of three literal-specific binder exceptions. The refreshed identical-input census is 188/282 emitted functions and 36 promoted regions, with no emitted-function losses or input failures.

The eight newly complete methods are `Find-ServerTypes`, `Get-PSSubRegistryTranslated`, `Get-PrivateRegistryTranslated`, `Set-PrivateRegistry`, `Get-ComputerMemory`, `ConvertFrom-SID`, `Get-RandomStringName`, and `Get-PSService`. This is a census disposition, not proof that directory-, registry-, or service-provider effects are safe in an unrestricted live environment; the executable workflow fixtures below isolate provider dependencies.

- [x] Bind `if`/`elseif`/`else` RHS values once through the native collection binder, immutable conditional IR, analysis, lowering, and generated method. The branch result is a collapsed value for object/map properties and a record sequence for `@()`; this keeps zero records, explicit null, one scalar, ordered multiple records, and nested arrays distinct. Authored source positions and effects flow through the existing collector. Traps and escaping `return`/`break`/`continue`/`throw` stay retained; loops are not admitted as branch value statements.
- [x] Finish the shared Hybrid evaluation contract. The native success sink and statement-error/command-region owners preserve authored key/value order, nested-array depth, mutation visibility, direct versus nested branch and condition errors, `ErrorAction`, error-variable contents, prior destination after caught construction failure, warning/verbose/information observations, and later statements without a second evaluator.
- [x] Keep Strict admission independent: only a closed, runtime-free branch/value graph may lower to a typed CLR expression. A Hybrid-hosted conditional property value does not qualify an ETS value for runtime-free transfer or a module for Strict; a focused admission check keeps the ordinary runtime-free alternative while rejecting the hosted conditional-value shape.
- [x] Prove the shared contract with synthetic zero/one/many, empty-versus-explicit-null, nested-array, malformed-input, failing enumeration/cleanup, catch, preference, stop, and reuse matrices on `net10.0`/PowerShell 7.6 and `net472`/Windows PowerShell 5.1. Injected enumerable checks cover `GetEnumerator`, partial `MoveNext`, `Current`, and `Dispose` failures while retaining prior destination, record shape, error identity, and cleanup trace. Unchanged registry and random workflows provide the two source families; emitted methods, promoted regions, and retained functions remain separate counts.

The original hosts already show why a simple `null` conversion is incorrect: for an `if` used as a `[pscustomobject]` property value, an empty branch yields a null-looking property whose `@($value).Count` is **0**, while a branch emitting `$null` yields **1**. Scalar and two-output branches yield one Int32 and a two-element Object array respectively on both selected hosts. The compiler must preserve this cardinality before treating `ConvertFrom-SID` or the other affected functions as unlocked. The observed-catch cohort remains a parallel, separate proof lane: `[ref]` requires caller-storage mutation and argument-evaluation/error identity, while directory and mail dependencies require their own safe host fixtures; none are complete-workflow credit from an invocation-only change.

The shared conditional-value slice passes original/generated Hybrid binary-module comparisons on `net10.0`/PowerShell 7.6 and `net472`/Windows PowerShell 5.1. Synthetic functions exercise object/map/array consumers; empty, null, scalar, multiple, and nested-array branches; mutation; direct and nested errors; preferences and variables; condition-side streams; prior-destination preservation; and injected enumeration/cleanup failures. The pinned full-module census gains eight complete functions without losing an emitted method. Unchanged registry and random workflows qualify two source families, while Strict admission stays independent. The earlier conditional-value checkpoint passed 1,040/1,040 tests and all six Strict programs. Schema 6 plus the 20/20 focused dual-host closure matrix complete the remaining M24/M25 implementation and workflow evidence; the later final M26 publication gate passes 1,060/1,060 tests, all six Strict programs, and the warning-free `net472` Release build.

The discovery packet now pins SamErde/PowerShell, MicrosoftIntune, CleanupMonster, PSSharedGoods, and the algorithm library. The PSSharedGoods snapshot at `2a807a4f11ba458b7bc405ce3674d93838639af1` adds its full 286-unit module closure: the initial scan emits 14 units in Hybrid and zero in Strict. Across the packet each mode assesses 278 of 291 submitted inputs and retains all 13 source-closure failures. This is analysis-only discovery; the existing public/external acceptance baselines and their execution claims remain separate. Use the established public corpus, including its other library styles, for regression qualification as contracts expand.

The first complete-workflow qualification set is fixed below. The hex-to-binary Hybrid command matrix passes on Windows PowerShell 5.1, PowerShell 7.4.19, and PowerShell 7.6.5 using native function binding and explicit command regions. Its Strict command gate remains open: the authored string pipeline parameter cannot yet use the generated cmdlet binder without changing callback behavior. The unchanged TimeSpan formatting command also passes in Hybrid mode: 414 value/format observations and 36 binding-failure, early-stop, and reuse observations per host. The unchanged formatting workflow has a separate Strict CLR library qualification with 414 observations per host and an independently built C# consumer that does not load PowerShell. That library accepts a non-null typed input collection, including an empty collection; its declared CLR null-argument exception is separate from PowerShell binding-error continuation. Keep the authored functions intact; compiler improvements must apply to their general language contracts. Analyze both command and runtime-free targets because their blockers differ.

Command-host artifacts now support scalar `-f` with String templates and qualified numeric, String, Boolean, Char, DateTime, TimeSpan, and Guid arguments. The loaded host supplies culture-sensitive formatting and native FormatError behavior through the existing statement-error contract. Native-bound functions also admit arrays and opaque Object arguments because conversion callbacks observe their actual invocation storage; String templates remain required.

Expandable strings use parser-preserved expression slots, including escaped dollar signs, braced and repeated references, literal braces, and here-strings. Closed scalar values use invariant formatting, with PowerShell's Single/Double precision and null-to-empty behavior. The closed Int32/Double promotion contract also formats without a PowerShell runtime dependency. Functions with CLR-owned locals retain opaque interpolation, string casts, and local-call string conversions when callbacks can observe those locals or OFS. Native-bound function bodies can compile opaque interpolation because their variables remain in the actual PowerShell invocation. The loaded host performs stringification, and callbacks observe the current values and scope.

Native-bound bodies also compile `+` when its left operand is a proven non-null string, including chains such as `'[' + $Text + ']'`. Right-operand conversion observes live locals and OFS, preserves callback order, and uses the native error handler when conversion fails. The three-host differential matrix covers callback mutation, collections, parameter conversion and validation, error preferences, caller catches, continued statements, and later invocations. This contract still requires PowerShell; dynamically typed arithmetic and native loop storage remain separate implementation work.

String pipeline parameters retain their native function binding in Hybrid mode. PowerShell runs script type conversion in a later binding pass than custom cmdlet transformation attributes; changing that order changes what conversion callbacks observe. Eligible functions register a native function whose body or lifecycle clauses execute generated CLR code. Other proven terminal bodies can become typed regions while their parameter declarations remain native. The manifest distinguishes compiled bodies, promoted regions, and retained source. Strict binary output rejects this native-function binding contract; the independent CLR lifecycle collection ABI keeps its own typed-input rules.

String-bearing hosted lifecycle functions also retain native binding in Hybrid mode: rebinding the original argument dictionary loses parameter and preference changes made by conversion callbacks. The native-function route closes validated-parameter storage for its admitted bodies: callbacks may remove attributes and change a value's CLR type, so bound reads use the actual variable owner without assuming the authored constraint still holds. Simple assignments apply current native constraints and preserve prior values after failure. Differential checks cover optimized local slots, strict-mode reads, target-host AllScope variables, callback mutation, catch variables, source-file context, and implicit or explicit module exports on PowerShell 5.1, 7.4.19, and 7.6.5. The native lifecycle, loop, and declaration contracts below extend that same invocation owner.

Native-bound bodies also preserve execution-status transitions and collect admitted expression statements inside `@(...)`. Each multi-statement collection retains evaluation order, error continuation, source extents, and one level of output enumeration. Nested collections, comma arrays, ordinary null, empty-array identity, error preferences, and subsequent `$?` reads match the three qualified hosts. A single pure expression keeps PowerShell's direct evaluation behavior, including skipping assignment when it fails. Traps, redirection, commands inside collected arrays, and opaque enumerable values remain hosted until their collection contracts are implemented.

Command-host integral remainder, decimal binary arithmetic, and supported constrained numeric updates now carry their error requirement through binding, optimization, and lowering. Generated arithmetic evaluates both operands before entering its operation wrapper; divide-by-zero and overflow receive the native RuntimeException wrapper and continue through the statement-error host. Failed assignments preserve the prior value. Constrained conversion preserves the arithmetic result type independently of the destination constraint, including mixed integral widths and unsigned decrement. Differential checks cover complete exception chains, caller catches, continuation, loop headers, and decimal division updates on PowerShell 5.1, 7.4.19, and 7.6.5. Integral division updates and broader unconstrained promotion remain outside this bounded contract. Runtime-free libraries retain their separate direct-CLR exception surface.

Runtime-free libraries admit a smaller numeric-format contract: statically validated templates may interpolate String labels whose values are proven to contain no braces. Assignments, branches, ordinary loops, and repeated process blocks participate in that value proof. Supported standard formats are C, E, F, G, N, and P, plus D and X for integral arguments, with up to two precision digits and alignment widths up to 4096. Unknown fragments, custom format strings, and ambiguous nested-brace forms remain rejected. The generated helper reads the current culture after operand evaluation and retains the library's declared direct-CLR exception surface. The complete formatting workflow is qualified through the typed library lifecycle collection ABI. Mandatory reference/nullable pipeline records remain rejected until per-record validation is implemented. Authored calls to a non-null library collection contract require a statically non-null array literal; unknown array variables remain rejected to preserve PowerShell binding-error continuation.

The typed executable lifecycle now preserves additional optional switch arguments and collects homogeneous process output when `end` is absent or empty. It uses the normal local-call binder for explicit, omitted, reordered, and conflicting named arguments. Positional lifecycle arguments remain retained because command-argument binding precedes pipeline input binding. Generated executable comparisons cover PowerShell 7.4.19 and 7.6.5, including null, empty, singleton, and repeated input, both switch values, and fresh invocation state. The library lifecycle and parameter types whose null pipeline input raises a binding error remain separate open contracts.

| Workflow and pinned source | Contracts that must close together | Planned differential cases |
| --- | --- | --- |
| SamErde/PowerShell `General/Format-Timespan.ps1`, commit `786225096078651d0fcbc535136d074b4c500698` | Pipeline metadata and begin/process lifetime with an additional switch parameter; TimeSpan members; scalar `-f`; string accumulation; implicit output. Formatting and both the native command and library lifecycles are qualified. The library explicitly rejects a null collection before begin; PowerShell null-record binding-error continuation remains outside this CLR contract. | Zero, negative, sub-millisecond, component boundaries, maximum/minimum TimeSpan, full/abbreviated labels, repeated and empty pipeline input, culture, binding failure. Qualified Strict runtime-free library with a separate C# consumer. |
| PSSharedGoods `Public/Converts/Convert-BinaryToString.ps1`, commit `2a807a4f11ba458b7bc405ce3674d93838639af1` | By-value Byte-array parameter binding and alias; conditional output; non-null framework Encoding receiver; exact Unicode decoding and error continuation. Runtime-free library admission remains separate; the complete command-artifact path is now qualified. | Null/empty/singleton/even/odd byte arrays, malformed UTF-16, non-ASCII text, nested and invalid caller input, zero/one/many input records, alias binding, later commands after failure. |
| PSSharedGoods `Public/Converts/Convert-HexToBinary.ps1`, same commit | Captured loop output with zero/one/many collapse; bounded loop increments; Byte parsing with enum arguments; `Write-Output -NoEnumerate`; partial parsing errors and continued execution; pipeline binding. | Empty/singleton/multiple pairs, odd length, invalid digits at the beginning/middle/end, numeric boundaries, invalid binding, record nesting, partial output and subsequent statements. |

The respective source SHA-256 values are `2f502f153597259dda9e9520685ce31ed2a5d8c7981099c4d41b2229ff3ae028`, `8d237e02921013b114a7d0af851e8369e2693bbfad5a8d5a17b8ee30e48fa332`, and `4ede6b109b121813150cdce0b515964127183c2feedd7e30d8d9c86737194a85`. Injected enumerable failure/disposal and numeric promotion still need separate contract probes where these workflows do not naturally exercise them. Their absence from a workload is not proof of those contracts.

Binary-to-string qualification covers named, alias, positional, and pipeline binding; null, empty, malformed, nested, Unicode, and invalid input; exact record/error capture; later successful invocation; and traced input enumerators that fail during `MoveNext`, `Current`, or disposal. Both compilation modes emit the complete function with zero fallback units. These are PowerShell-hosted binary commands; runtime-free libraries have their own qualification below. Enumeration in compiled function bodies now has the separate host contract described below.
The hex-to-binary regression matrix compares 132 input/action/binding cases per host and mode. It covers exact scalar/list/array/null record containers and serialization, malformed pairs and odd lengths, partial errors, zero/one/many pipeline input, invalid middle input, downstream stop, and a later successful invocation. The Hybrid artifact emits the loop, numeric indexing, capture, and parsing into a complete CLR method, with an identified hosted region for the final unqualified `Write-Output -NoEnumerate`. Native function binding keeps the authored parameter behavior. The full Hybrid matrix passes on all three hosts; the Strict matrix continues to reject the unsupported binding contract.

String command parameters now use the loaded host's script argument converter, preserving null-to-empty normalization, advanced-function array rejection, basic-function conversion, reference arguments, invariant conversion, and error identity. The host contract is checked for the three qualified profiles and invokes no authored source. Separate comparisons cover two cultures, custom conversions, mandatory/optional/allow-null/allow-empty validation, and by-value/by-property pipeline input. Shared parameter validation observes whether a pipeline argument was actually bound: an empty input pipeline still executes an end-only body without revalidating the untouched mandatory parameter. Bound arguments and ordinary CLR-library validation retain their checks.

Module-qualified `Write-Output -NoEnumerate` now preserves positional scalar, null, collection, and captured-loop output on the three selected hosts. PowerShell 7 positional scalar arguments use the native list container; named input preserves the value. Windows PowerShell 5.1 positional arguments preserve the value directly, while named non-scalar input remains hosted because its PSObject-array binding enumerates collections. Comparisons include nested records, reference identity, and serialization. Output-free argument expressions remain hosted until their side effects and null-input binding are qualified; the shared value-consumption pass checks nested operands and resolved local-call return types before lowering, including region-opportunity analysis and its transitive local-call closure. It preserves side-effect-only statements, discards, and returns. Identity conversion optimization keeps provisional local-call casts until their type is resolved. Unqualified commands retain runtime lookup so session shadowing still works; this support does not remove the pinned hex workflow's final hosted command region.

Generated binary command wrappers preserve the authored function name in native parameter-binding errors through a build-owned CLR metadata finalization step. The generated project carries that step and its locked build-only dependencies, so independent rebuilds retain the same command identity. Native stream-variable registration follows function-clause lifetime, including pipeline binding failures, append capture, stop, and later invocations. The separate compiled method container remains the CLR callable surface. Metadata validation rejects overlapping string storage and signing/native-image inputs before modification; finalization precedes artifact hashing and signing.

The first closed-value implementation accepts inferred empty `@()` arrays and ordinary null elements in Object arrays. Generated empty arrays have distinct identities, matching authored allocations. Typed null elements that require unsupported numeric or other element conversion remain retained/rejected, including comma literals. Inferred scalar-string foreach now preserves zero iterations for null and one iteration for an empty or nonempty string, without restoring the incorrect persistent constraint on RHS casts. Focused original/generated module checks cover PowerShell 5.1 and 7. These cases do not close the collection milestone: enumerable failures, nested collection grouping, numeric promotion, and the three complete workflow gates remain open.

The loop-state sweep also removes stale value facts across loop writes, iterator assignment, backedges, and exits, including `continue` paths into a `for` iterator. A receiver that starts non-null but can become null no longer receives an unsafe Strict CLR call; an assignment inside the body can establish a fresh non-null fact before use. Possible module-state origin propagates across loop-carried aliases and local-function returns so later iterations cannot bypass the live-state boundary. Hybrid nullable-loop checks preserve the original error identity and later statements through retained PowerShell execution. This is correctness protection, not compiled-workflow credit.

Authored array expressions now preserve one enumeration level inside `@()`: `@((1,2))` collects two numbers, while `@((1,2),(3,4))` collects two nested arrays. A comma wrapper such as `@(,$Value)` preserves the wrapped object's identity without enumerating it. Original/generated Strict binary-module checks cover both PowerShell hosts, including nested and empty arrays, ordinary null, typed scalar records, and a wrapped enumerable that throws if enumerated. Arbitrary unwrapped enumerables and records requiring unsupported element conversions still retain PowerShell behavior. String array elements and explicit scalar string casts use a canonical null-to-empty conversion, including missing string-dictionary values. Strict original/generated checks preserve missing, null, empty, and nonempty values on both hosts. Empty collected Object arrays also follow the selected host profile: PowerShell 7 shares the result of collecting no records, while Windows PowerShell and typed array destinations allocate. An authored empty `@()` continues to allocate on both hosts. Complete workflow qualification remains open.

Single-vector collection now follows the selected profile. PowerShell 7 copies a known CLR vector into Object records, preserving nested element identities and collecting null as one record. Windows PowerShell 5.1 preserves a constrained array parameter or local through `@()`, including its identity, null value, and element type; an inferred local still takes the collection path. Generated method entry also preserves an already typed `string[]` argument and its null elements instead of copying and reconverting it. Both-host Strict artifact comparisons cover these cases. Arbitrary enumerators, mixed dynamic collection statements, and unsupported destination conversions remain open.

Unconstrained Int32 locals now retain their actual boxed Int32 or promoted Double representation through assignment, addition, subtraction, multiplication, and standalone increment/decrement. Copies preserve the value without inheriting the source variable's type constraint. Comparisons and arithmetic with a known Double operand preserve fractional, NaN, and infinity behavior. Original/generated libraries and sequential command-output checks cover Windows PowerShell 5.1, PowerShell 7.4.18, and PowerShell 7.6.5; a separate C# consumer executes the generated library without loading PowerShell. Integer division and remainder, including compound updates, now preserve exact Int32 results and promoted Double values when the existing range proof establishes a nonzero Int32 divisor. This includes descending loop counters; divisors that can be zero remain outside this contract. Differential library checks cover fractional quotients, minimum-value division by negative one, signed remainders, and already-promoted values on Windows PowerShell 5.1, PowerShell 7.4.19, and PowerShell 7.6.5. Later variable-constraint transitions, other numeric widths, and value-producing increments remain explicit boundaries. This closes a bounded numeric contract, not the complete M24 exit gate.

Hosted commands now preserve a bounded reference-local initialization contract for direct CLR calls. A failed first assignment leaves a null slot; a failed reassignment preserves its previous value. Error-edge value facts prevent a successful constructor's non-null result from hiding the failure path. Original/generated checks cover continuation, stop, typed catch/finally, loop iterations, null-seeded locals, and subsequent method calls on the three qualified PowerShell hosts. Value-type and string initialization, wider expression shapes, basic-function host behavior, and runtime-free error continuation still need their own contracts.

Captured loop output now has a bound statement and a lowered temporary success sink. Qualified loops collect records once, preserve explicit null and nested arrays, collapse zero records to AutomationNull, and restore the enclosing sink before later output. Comparisons cover zero/one/many records, nested captures, local calls, and loop flow control on PowerShell 7; scalar-result capture also compiles on Windows PowerShell 5.1. Collection-valued streamed records retain PowerShell execution on 5.1 because native cmdlet wrapping changes observable serialization.

Hashtable literals preserve their authored Hashtable or OrderedDictionary identity. String-valued lookup precision requires a closed proof across assignments, branches, direct aliases, reference casts, and mutations. Non-string writes, unknown calls, and collection escapes widen the value contract to Object before lookup; branch joins cannot restore a narrower contract. Original/generated comparisons cover direct, indexed, member, loop, interface-alias, and stored-alias mutation on the three qualified hosts. Closed string reads and string writes remain available to typed libraries and binary modules. Open Object success-stream output remains a separate admission boundary, so a workflow returning an unqualified mutated value can still require Hybrid fallback.

Direct CLR errors inside captured loops preserve partial-output collection when the body continues. Escaping PowerShell RuntimeExceptions discard collected output and skip the final assignment, preserving the prior destination or its initial null slot. A raw CLR exception from an enumerator instead flushes partial records to the enclosing success stream before error handling, while still preserving the prior destination. Nested captures, caught failures, cleanup, error-variable entries, and downstream stop match original execution on the selected hosts. Statement discards inside these loops remain discards. Return transfers remain unsupported inside captures. Non-native captures also retain their boundaries for constrained or runtime-owned targets, hosted command regions, and error-producing local calls; native-bound Hybrid support is described under M25.

PowerShell-hosted commands can pass a closed Int32/Double value to an Int32 CLR parameter when one overload shape accepts the authored argument count. Bound conversion metadata preserves evaluation of all arguments before conversion, null-receiver precedence, numeric rounding, and native argument-conversion errors. Constructor, static, and instance calls match the selected hosts, including range errors and non-finite values. Competing overloads, wider numeric conversions, and runtime-independent argument conversion remain unqualified. The complete hexadecimal workflow now emits its loop but still retains the NoEnumerate command boundary.

Qualified command hosts now compile `foreach` over statically known IEnumerable and IEnumerator sources. The loop preserves raw element storage, scalar fallback, the final loop-variable value, failure continuation, and authored cleanup. It follows native PowerShell by leaving enumerator disposal to authored code and checks the active host for stopping even when the body emits no output. Captured-loop qualification includes both raw CLR and PowerShell RuntimeException failures during enumerator acquisition, advancement, and value access. This contract requires the PowerShell statement-error host and is not admitted to runtime-free libraries or executables.

Hosted `for`, `while`, `do`, and array/string `foreach` loops also observe stopping before the authored loop body. Conditions and enumerator advancement run first, so empty or completed enumeration skips the body check and a failing MoveNext retains its exception precedence. Original/generated comparisons cover stopping before and during iteration, continue paths, null and empty inputs, current-item reads, captured output, and finally cleanup on the three qualified hosts. Interrupt requirements travel through binding, optimization, and lowering. Supported library and executable loops retain their independent runtime-free contract.

Hosted `finally` blocks suspend pipeline stopping while cleanup runs and restore it afterward, including nested cleanup and inner-loop break/continue. This stopping dependency is distinct from ordinary statement-error writes. Module-local error-state reads remain under qualification after exposing a difference between native script-module and binary-cmdlet session state.

CLR Object arguments now use the loaded host's wrapper conversion after all arguments have been evaluated. Homogeneous hashtable literals retain Hashtable identity through loop input, aliases, and interface conversions. Calls on an open Object receiver remain hosted because their method lookup, wrapper handling, and overload selection are not closed by a static Object signature; statically typed receivers and the existing closed numeric value contract retain their separate support.

Native-bound Hybrid functions now compile `-replace`, `-ireplace`, and `-creplace` through the existing PowerShell pattern-operation owner. Collection results, conversion callbacks, supplied script-block replacement callbacks, culture, enumeration failures, error preferences, and failed-assignment state match the selected hosts. The operator token's source extent and complete authored lines travel through binding, optimization, and lowering so native diagnostics preserve its spelling, location, and line endings. The differential matrix executes 417 observations for each LF/CRLF variant per host, including downstream stop and later invocation. Wildcard and split checks cover the same source-metadata boundary. Strict targets retain their separate bounded scalar Regex contract.

Native-bound Hybrid functions compile expression and command-pipeline statements inside `$()` through the shared collection owner. Empty `$()` preserves ordinary null; nonempty captures preserve zero/one/many records, nested arrays, object identity, mutation suppression, and `[void]` side effects. Direct pipeline records remain distinct from parenthesized expression output, including `-NoEnumerate` values. The three-host differential matrix covers enumeration failures, error preferences and variables, continuation, downstream stop, and later invocation. Control-flow statements and traps inside subexpressions remain unsupported, and this host-dependent contract does not expand runtime-free Strict admission.

Native-bound Hybrid membership operators use the installed host's comparison and enumeration semantics. Differential checks cover all case and negation forms, collection-before-candidate evaluation, conversion, enumeration failures, error continuation, and downstream stop on the three selected hosts. Typed membership preserves the same operand order. Collected-array inference follows emitted element types, and command array returns use the shared success-stream owner to preserve nested records on Windows PowerShell 5.1. These bounded checks do not close the complete-workflow gates or expand runtime-free Strict admission.

Typed binary commands can consume local function output in expressions, assignments, and arguments. The call contract preserves empty output, explicit null, single records, multiple records, and nested arrays independently of the generated method's CLR return type. Parameter binding converts the empty-output sentinel to null; ordinary capture retains it. Direct calls retain streaming behavior, and outer error redirection keeps its original destination while a child call captures success records. Differential checks cover suppression, nested calls, error preferences, catches, and previous assignment values on all three selected hosts. Typed command membership now shares the host's comparison and enumeration owner for object-valued collections. Calls containing hosted command regions remain outside this local-capture contract, and runtime-free Strict admission remains separate.

## Milestone 25 — Compile complete pipelines and advanced functions

**Outcome:** compile an entire command workflow, including multiple success outputs, instead of restricting useful work to a scalar terminal return.

- [x] Preserve sequential success output across local calls, branches, loops, try/catch/finally, and terminal returns. Dual-host complete artifacts retain earlier records, cleanup output, early returns, nested calls, and loop order without prematurely ending the caller.
- [x] Compose typed and hosted pipeline stages with canonical input/output/stream/error contracts. Existing native command regions own host-dependent stages; generated output and capture owners preserve enumeration boundaries, partial output, downstream early stop, cancellation, and disposal without inventing a second pipeline engine.
- [x] Compile one complete authored multi-stage pipeline as a single native command region inside an emitted Hybrid method. The active PowerShell invocation remains the owner of command resolution, parameter binding, script-block scope, success/error/warning/information ordering, begin/process/end/clean scheduling where present, stopping, disposal, aliases, and retained mutation. Generated code owns only the surrounding supported statements and the immutable region contract. This does not grant a runtime-free pipeline ABI.
- [x] Extend advanced-function binding and lifecycle for selected real shapes: pipeline input by value/property, parameter sets, default-versus-bound state, validation, common parameters, begin/process/end, and host-qualified clean behavior.
- [x] Preserve local-call binding, dynamic-scope observations, streams, nonterminating errors, and caller continuation through each admitted reachable call graph. The selected random workflow executes five local calls, while complete pipeline and implicit-output matrices cover retained stages, captures, preferences, errors, and later caller statements. Unknown observers still force a truthful boundary.
- [x] Add two complete pipeline-heavy commands to executable acceptance, including a command with more than one output-producing local call and one with a retained provider stage.

**Exit gate:** both commands run in their original module context and generated artifacts with record-for-record output/stream comparison. Exercise zero/one/many inputs, partial failure, downstream stop, cancellation, and cleanup on PowerShell 5.1 and selected PowerShell 7 profiles where the authored feature exists. Strict admission uses runtime-free adapters only; unsupported host features remain explicit.

Eligible Hybrid advanced functions compile begin, process, end, and clean blocks into separate native callbacks over one generated CLR body. The native invocation owns parameter binding, defaults, validation, common parameters, variables, and clause scheduling. The manifest reports `CompiledNativeCallbacks`, the authored source hash, and the required host version. Comparisons cover shared storage, binding callbacks, returns, errors, downstream stop, cancellation, finally cleanup, and later invocations on PowerShell 5.1, 7.4.19, and 7.6.5; clean behavior is qualified on the PowerShell 7 hosts. Dynamic parameters and lifecycle traps retain their explicit boundaries.

The pinned PSSharedGoods `Get-RandomCharacters` and `Get-RandomPassword` workflows now satisfy the selected command-acceptance step in Hybrid mode. Both complete bodies compile, including the password command's five local calls; random-provider pipelines remain explicitly hosted. The two-file acceptance fixture and the complete original module match their generated artifacts on PowerShell 5.1, 7.4.19, and 7.6.5: 289 ordinary observations and four cancellation/reuse observations per host. The provider is replaced only inside the selected module to make values, call order, injected failures, and cleanup deterministic. This evidence does not qualify the rest of the module or close the broader composition items above.

Captured command pipelines select native invocation storage directly. Binary and unary `-join` use the same native invocation's conversion and enumeration rules, with operands evaluated before separator conversion. Their bound effects preserve callback mutation and statement-error handling. Differential checks cover null, scalar, nested and character collections, conversion callbacks, enumerator failures, partial results, and continuation on the three selected hosts. Strict typed joins retain their separate direct-CLR contract.

Native-bound Hybrid functions can interleave generated statements with explicit command pipelines in the same invocation scope. Assignments and command callbacks share live variables, command lookup uses the parent module, and statically named local callees retain their own native binding and dynamic-scope observations. Pipeline captures preserve partial output, empty-output identity, and array collection. Original/generated checks cover errors, preferences, output-variable capture, downstream stop, source metadata, and redirection on the three selected hosts; PowerShell 7 checks also cover background pipelines. Each region compiles only its selected pipeline, with authored source metadata retained for diagnostics. Dynamic call-graph closure and runtime-free Strict pipelines remain open.

Complete multi-stage pipelines now select that same native owner even when their commands are individually known intrinsics. This removes the prior false split in which each stage was recognized but the authored pipeline statement had no complete owner. A compiler-owned invocation feeding a hosted pipeline retains its narrower typed-continuation route. A dual-host matrix covers empty/single/many/null input, all success and non-success streams, begin/process/end scheduling, aliases, error actions, early stop, repeated invocation, removal/reimport, and retained collection mutation plus duplicate-key and bounds failures. Pinned `Get-ObjectEnumValues` and `Remove-DuplicateObjects` execute differentially; `Get-MyIpAddress` is pinned and structurally emitted without making a network call. The established fail-closed contract keeps terminal `Out-Null` success-stream consumption retained. The implementation adds no source-name policy, collection-type whitelist, or second pipeline engine.

Background pipelines and redirection select native binding in eligible Hybrid functions, including functions without parameter-binding callbacks. Targets without the required operator contract reject or retain the authored function.

Assigned script-block literals in eligible Hybrid functions now bind and lower as child bodies through the same compiler pipeline. Native invocation preserves dynamic variable reads, child-local writes, parameter metadata, and the authored block text. Artifact comparisons on PowerShell 5.1 and 7.6.5 cover nested and escaped blocks, `GetNewClosure()`, errors, lifecycle clauses, downstream stop, cancellation, and runspace reuse. Unsupported child bodies retain the owning function. The pinned complete `Get-ComputerLookupCandidates` workflow compiles with six explicit command boundaries, including one in its nested block; nested region graphs report those boundaries separately from parent execution order. Native `-like`, `-notlike`, and `-split` preserve collection results and error continuation. These remain PowerShell-dependent contracts and do not qualify runtime-free script blocks or the M26 stateful module exit gate.

Native-bound Hybrid loop captures also collect records from explicit command pipelines. Nested captures restore the enclosing native output pipe before destination assignment and later output. The shared temporary-pipe owner also serves partial command captures. Captured assignments support ordinary, scoped, typed, and validated variable targets through the same native assignment owner as expression assignments. Compound assignments read the destination before the compiled loop runs. Original/generated comparisons on the three selected hosts cover nested compiled calls, output/error/warning/information variables, merged errors, preferences, loop transfers, failed constraints, downstream stop, cancellation, and later invocation. Non-native captures retain their hosted-command boundary.

Native-bound Hybrid functions now compile supported local type declarations and validation attributes. Assignment targets use the invocation's native storage and constraints while their values remain compiled expressions. This preserves constraint installation and replacement, compound-assignment evaluation order, and the previous value after a failed assignment. Ordinary scoped and read-only assignments use the same operation, including arithmetic errors on a `$null` compound target. Original/generated comparisons cover the three selected hosts, error preferences, source positions, and later statements. Runtime-free constraint transitions retain their explicit boundary.

Native-bound `foreach` also accepts dynamic collections and assigns each item through native variable storage. The loop saves `$foreach` before evaluating its collection, keeps its cursor independent of authored `$foreach` reassignment, and restores the previous iterator during transfer or failure cleanup. Authored code retains responsibility for disposing its enumerator. Comparisons cover scalar/null/empty collections, nested iterator identity, constrained and read-only targets, acquisition/advancement/current failures, source positions, downstream stop, cancellation, and captured output on PowerShell 5.1, 7.4.19, and 7.6.5. The body and control flow remain compiled IR; this host-dependent contract does not change runtime-free Strict admission.

Supported arithmetic, bitwise, and comparison operators in native-bound functions use the native operation contract even when both operands are literals. Differential checks cover integer promotion, division and remainder errors, filtered array comparisons, preferences, and source positions on the same three hosts. This does not extend runtime-free numeric admission.

Generated command methods admit non-terminal scalar and enumerable output through a shared native success-output operation. Returned records are written before unwinding `finally`, then the CLR method exits. The operation serves both native-bound functions and typed command methods with statement-error context; it does not require native function binding. Comparisons on PowerShell 5.1, 7.4.19, and 7.6.5 cover partial enumeration failures, continuation preferences, nested compiled calls, captured records, downstream stop, cancellation, and later invocation. Command output metadata is inferred separately from the callable CLR return type, including scalar and collection provider results. Compiler-owned dictionary literals retain their escape boundary. Basic-function command binding and the runtime-free multi-record library ABI remain open; qualified local-call captures are described below. Non-success output from `finally` can bypass generated-host preferences and variable capture after stop, so those shapes retain PowerShell execution. Hosted-region grouping keeps cleanup visible to control-flow analysis instead of hiding it inside a region.

The return contract also propagates through callers and applies to command returns protected by `finally`, so cleanup cannot delay or lose an already-produced record. Generated command catches exclude PowerShell pipeline-stop and flow-control exceptions while continuing to handle ordinary exceptions. Both-host comparisons cover broad catches around direct and local-call output, throwing cleanup, record observation before cleanup mutation, and repeated calls through an intermediate helper. A success-output attempt during stop unwinding interrupts the remainder of `finally` on both tested hosts; the compiled result preserves that behavior. The selected workflows above also cover cancellation and provider cleanup; broader lifecycle composition remains open.

Generated command artifacts now use the statement-error host for qualified CLR method and constructor calls, typed catches, explicit CLR throws, and rethrows. Bound statement boundaries preserve assignment and failed-return continuation before output rewriting. Local calls propagate the host contract and preserve the failing command's identity through nested callers. The bridge invokes the loaded host's error machinery without executing source expressions and checks its private host contract before invocation work. Runtime-independent libraries and executables do not receive this capability.

Qualified CLR cleanup errors also use that bridge during `finally`. Their conservative error-stream effect no longer forces retention of an otherwise qualified function. Direct warning/error sinks and hosted command regions still retain PowerShell execution when cleanup can run after downstream stop, including through local calls. Original/generated cleanup comparisons cover preferences, error-variable capture, subsequent cleanup statements, normal completion, and downstream stop on all three selected hosts.

Original/generated comparisons cover Windows PowerShell 5.1, PowerShell 7.4.18, and PowerShell 7.6.5: continuation preferences, null receivers, receiver/argument evaluation order, constructor failures, mutable value-type parameter storage, typed catches, finally output, and nested command/error-variable behavior. The normal artifact builder also produces and executes these methods without retained script functions. Existing host fixtures cover read-only caller preferences and error-variable cleanup after normal completion or downstream stop. Wider mutable receiver storage, basic-function error surfaces, and live error-state reads remain explicit boundaries. In particular, a function that can change error state cannot read an invocation-entry `$Error` or `ErrorActionPreference` snapshot through its call closure. General unsupported operator/conversion and runtime-free error semantics remain fail-closed. The admitted M24/M25 workflow gates are covered by the grouped closure matrix; broader language coverage is not implied.

Typed local command calls can now capture hosted-region output together with generated output and a terminal CLR return. Invocation-owned pipes preserve empty-output identity, explicit nulls, nested collections, inherited error destinations, output/error variables, and downstream early termination. Hosted regions use the existing module session-state owner; they no longer register a separate dispatcher. Original/generated comparisons on PowerShell 5.1, 7.4.19, and 7.6.5 cover record shape, continuation, terminating errors, capture, module lookup, and authored error locations. Grouped regions and captured assignments reconstruct only their selected statements with original source extents. Hosted type tests preserve PowerShell wrappers and extended properties through native type semantics. These contracts do not qualify runtime-free command pipelines or close the full dynamic-scope and multiple-region workflow gates.

Conditional assignments now collect zero, one, or multiple success records through the existing capture owner. Native unary arithmetic preserves PowerShell numeric promotion, and native member/index assignments preserve aliases and target evaluation order. Assignment statements inside `$()` and `@()` execute without emitting their assigned value. These contracts qualify the pinned [CleanupMonster saved-state closure](../Benchmarks/PowerShellCompilation/Corpus/ExternalWorkflows/CleanupMonster/README.md) on all three selected hosts. Native captures whose `break` or `continue` targets an enclosing loop remain hosted until an enclosing-transfer contract is qualified; transfers to loops inside the capture still compile. This closure proof does not close the full-module lifecycle gate.

## Milestone 26 — Compile stateful modules and multiple Hybrid regions

**Outcome:** finish M23 with meaningful module-scale compilation while preserving one state owner and one semantic pipeline.

- [x] Admit one second detached statement-aligned region after a guarded prefix. Canonical binding and region graphs require exact function-parameter and local live-ins, return every mutated stable-scalar local, and preserve authored order around retained operations. A terminal read-only region may consume an exact guarded-prefix-owned Hashtable reference.
- [x] Apply the bounded two-region shape to unchanged real module source on both supported hosts, including output order, error stopping, downstream early stop, reuse, retained mutation, reference identity, and removal/reimport.
- [x] Generalize the bounded two-region shape using canonical liveness, definite assignment, effects, error routes, and transfer contracts. The selector now preserves one maximal approved candidate from every non-overlapping canonical run and exposes safe statement-aligned suffixes when a larger structurally valid span is rejected by policy. The existing guarded prefix remains the only fresh-local owner; additional regions cannot introduce new ownership.
- [x] Admit the safe local-call closures and collection/value contracts qualified in M24. The exact zero-parameter fresh-`ArrayList` factory can participate in the same retained function as multiple scalar regions. An entire candidate remains hosted when any live input, output consumer, alias, exception route, source identity, or local-call contract is unproved.
- [x] Keep the parent PowerShell module authoritative for Hybrid state. Prove nested/reentrant calls, two runspaces, retained-code mutation, initialization failure, removal/reimport, and authored cleanup.
- [x] Implement Strict module initialization and lifetime independently: deterministic initializers, instance/state ABI, explicit concurrency policy, initialization failure rollback, disposal/reset, and separate library instances. No hidden runspace or generated copy of Hybrid state is allowed. The qualified initial state contract uses explicitly typed immutable scalar fields.
- [x] Execute two unrelated real module workflows that cross compiled and retained code, and one small complete stateful Strict library/application.

**Exit gate:** the two Hybrid workflows promote more than a single terminal scalar helper and preserve state, streams, exceptions, cancellation, and cleanup in their full module context. The Strict workflow runs from a clean C# consumer or EXE with independent-instance tests and certified runtime-free closure. Explain output and the final ledger agree with the executed boundaries. Performance is recorded separately.

The pinned PSSharedGoods registry workflow qualifies one of the two required Hybrid module workflows. Its full-module artifact executes eight compiled workflow functions, now including `Get-PSSubRegistryTranslated` through the shared conditional-value owner, with recursive registry calls, shared dictionaries, acquired mount state, the nesting counter, and authored cleanup. Source/artifact comparisons match on PowerShell 5.1 and 7.6.5 for the current compiler target matrix: 433 ordinary observations and six cancellation/reuse/reimport observations per host. Earlier PowerShell 7.4.19 evidence is historical. The repeatable full-module test verifies the pinned loader and 284 resolved source files, including color-table and completer initialization. Registry read, mount, and dismount providers are isolated replacements; no real registry operation is qualified. Interrupted calls preserve the source's nonzero counter because its entrypoint has no `finally`.

The pinned CleanupMonster saved-state workflow now runs inside its unchanged source-module loader and all 67 function files. Both saved-state functions compile and call retained in-memory providers. On PowerShell 5.1, 7.4.19, and 7.6.5, the artifact matches 18 ordinary observations and eight cancellation/reuse/reimport observations per host. The comparisons preserve shared dictionary/history identity, partial key conversion, diagnostics, provider cleanup, and host-specific retry behavior. Explain output agrees with the full module's delivered disposition ledger and region graphs. This validates the saved-state lifecycle in the full source context; the dependency manifest, filesystem providers, public administration entrypoints, and broader multiple-region workflow gate are not exercised.

The reservation-budget fixture historically qualified a complete stateful Strict library on .NET 8 and .NET 10. The .NET 8 result is retained only as dated evidence; current requalification uses the supported `net10.0`/`net472` policy. A separate C# consumer references only the built artifact and exercises reservation, release, capacity rejection, validation without state mutation, concurrent operations, reset, independent instances, and disposal. Dependency certification and the consumer's loaded-assembly check confirm the runtime-free closure; explain and build region graphs agree. The library uses the existing direct-CLR validation and conversion exception surface. Integral expressions that can promote expose an Object return carrying Int32 or Double; OutputType metadata does not override the analyzed return representation.

That workflow also qualifies plain Int32 addition, subtraction, and multiplication through the existing boxed numeric-promotion implementation. Local callers are rebound when canonical return analysis changes the provisional signature, including a numeric union hidden by OutputType metadata. Runtime-free casts and constrained assignments accept the closed Int32/Double-to-Int32 conversion with midpoint-to-even rounding and CLR overflow exceptions. Boundary comparisons check actual result types and Double bits across the three PowerShell hosts. Wider integral types, arbitrary object conversion, and command-host conversion-error continuation remain separate contracts.

Strict libraries expose an instance API with deterministic initialization, serialized calls, transactional reset, idempotent disposal, and independent state per instance. Constructor and reset overloads preserve omitted defaults and explicit values; validation or initialization failure leaves the previous state intact. Current generated-artifact checks target the supported framework policy and run an ordinary C# consumer, including concurrent updates and rejection of calls after disposal. Original-module comparisons cover the same scalar state transitions. Mutable state declarations, ambient initialization effects, and caller-dependent inherited reads remain rejected until their contracts are qualified; this does not close wider additional-region and mutable-transfer work.

Native-bound Hybrid commands preserve the parent module's state through generated-to-retained-to-generated calls. Differential artifact checks on PowerShell 5.1, 7.4.19, and 7.6.5 cover nested invocation, retained mutations, separate runspaces, failure and downstream stop, initialization failure, removal/reimport, and authored removal callbacks. Explicit cancellation inside a retained stage preserves generated finally/clean state updates and allows later invocation in the same runspace. The tests assert which bodies compile and which remain retained. This qualifies the shared state owner; wider additional-region selection and mutable transfer remain separate gates.

Promoted scalar loop regions now receive an explicit stopping callback from their retained PowerShell invocation. Lowering propagates that callback through compiled local calls independently of statement-error state; native handlers and finally cleanup retain their fuller host context. Region metadata and disposition explanations report the dependency. Differential checks on PowerShell 5.1, 7.4.19, and 7.6.5 stop an entered generated helper, observe outer finally cleanup, and execute a later call through the normal rewritten module. These checks qualify the callback boundary; general additional-region loop and error envelopes remain open.

Guarded initialization prefixes now check their invocation's local storage before running a detached helper. Fresh targets use the compiled prefix; existing targets keep every authored assignment, including inherited read-only and validated variables. Stable scalars and a fresh empty Hashtable transfer in authored order, and an independently selected terminal suffix still runs when the prefix stays native. The function retains its parameter sets, aliases, output metadata, comment help, and authored error locations. Current `net472`/Windows PowerShell 5.1 and `net10.0`/PowerShell 7.6 artifact comparisons cover one- and multiple-statement prefixes, scalar and Hashtable transfer, all four error preferences, stopping inside the compiled prefix through the exported function, and subsequent reuse; the earlier PowerShell 7.4 observation is historical. Namespace imports and inline parameters retain their metadata; files with hosted class identities or runtime-bearing imports keep those function owners native.

The completed multiple-region owner selects one maximal policy-approved candidate from every non-overlapping canonical bound run. It also preserves structurally valid statement-aligned suffix candidates, allowing the existing promotion policy to reject an unsafe larger span without hiding a safe suffix; the reconciliation owner suppresses an enclosed suffix when a larger approved candidate already covers it. Helper ABIs record function parameters separately from local inputs. A local input must have an authored stable constraint or match the one guarded-prefix output owner in the same retained function. Mutated outputs remain stable scalars and return through ordered continuation transfer; collection/value contracts retain their M24 ownership and output behavior. Canonical graph inputs and outputs must exactly match every transfer contract before the source rewriter composes the calls. A synthetic retained module executes four scalar regions plus one exact fresh-`ArrayList` local-factory region and preserves collection identity, caller mutation, early pipeline stop, reuse, and removal/reimport on `net10.0`/PowerShell 7.6 and `net472`/Windows PowerShell 5.1. A same-run CLR-exception candidate stays hosted while its safe suffix remains independently eligible. The final compiler gate passes 1,060/1,060 tests and all six Strict programs; the `net472` Release build completes with zero warnings/errors. New-local ownership, borrowed compiled collection mutation, arbitrary enumeration, dynamic object/ETS transfer, and unproved local calls remain fail-closed.

The unchanged pinned PSSharedGoods `Convert-IPToBinary` function now qualifies one real one-statement guarded prefix. Its exact source hash is verified before the normal Hybrid artifact build; original and generated modules match valid, whitespace, invalid, empty, and null cases, warning/error order, downstream stop, reuse, and removal/reimport on both supported hosts. This is the real-workflow proof for statement-count-independent region admission.

The unchanged pinned PSSharedGoods `Get-ObjectEnumValues` function supplied the first real same-function two-region proof. A guarded compiled prefix creates an empty Hashtable, retained PowerShell fills that exact object, and a compiled terminal region returns the same reference. The fixture verifies source hash `d5622ecacebcc19f16d793c1527ad2bb5233398dc78f6d6738560cef362682d3`, enum key/value types and order, retained mutation visibility, per-call isolation, continuing and stopping behavior, downstream early stop, reuse, and removal/reimport on both supported hosts. The complete-pipeline slice deliberately preserves those two narrower regions because the compiler owns the invocation that supplies the hosted pipeline. Ordinary Hashtable parameters still do not receive a typed-region mutation exception.

Guarded functions and wholly compiled native-bound functions use PowerShell declarations for registration, then install their prepared bodies. Declaration scope, aliases, prior success status, and failures when a function or alias is read-only retain native behavior. WhatIf, Confirm, and cmdlet default parameters do not suppress a function declaration. The generated declaration preserves escaped function names and inline parameter metadata.

## Milestone 27 — Make compiled CLR libraries practical to consume

**Outcome:** publishable PowerShell-authored DLLs expose a useful, predictable .NET API rather than merely proving that a generated assembly can load.

- [x] Qualify two library use cases with different data and lifetime shapes: the static typed-collection `Format-TimeSpan` transformation and the stateful `ReservationBudget` service. Derive their callable API, null/empty/default, reset, serialization, disposal, and repeated-use contracts from ordinary consumers.
- [x] Carry typed collections, nullability, defaults, generated-name collisions, provider streams/cancellation, ownership, and disposal through the existing versioned ABI. Add an ABI compatibility owner that accepts additive methods and rejects removed methods, changed parameter type/nullability/defaults, public state-contract changes, unsupported future schemas, and malformed nested collections.
- [x] Package generated libraries with XML documentation, deterministic portable PDBs, generated source, dependency/provider locks, provenance, and runtime-free support assets through PowerForge. Rebuild and consume the emitted package without source-project shortcuts.
- [x] Qualify independently built provider packages through their existing SDK/ABI with exact versions, signer policy, transitive dependency closure, output/stream/error behavior, cooperative cancellation, cleanup, and cleanup-failure behavior. Arbitrary cmdlets remain outside registered adapters.
- [x] Restore and execute ordinary C# consumers from temporary local feeds for `net10.0` and `net472`; the consumers reference only the generated package and do not load PowerShell or compiler internals. Public-feed consumption remains M29 work.

Completion evidence (2026-09-20): the focused M27 matrix passed 25/25 after the final diagnostic-source portability correction. The authoritative gate passed 1,063/1,063 tests and all 6/6 Strict programs; the `net472` Release build completed with zero warnings/errors. Package checks cover repeatable bytes, signed/tampered-input rejection, portable source identity, XML docs, symbols, dependency locks, normal static and stateful C# consumers, provider success/failure/cancellation/cleanup, and intentional ABI incompatibilities. The fresh pinned PSSharedGoods census remains 188/282 emitted functions and 36 promoted regions with no input failures. M27 changes product consumption, not language coverage.

**Exit gate:** both consumer applications compile and execute success, invalid-input, null/empty, cancellation where supported, and repeated-lifetime cases. An API compatibility check catches an intentionally incompatible sample change. Runtime/dependency inspection confirms the promised CLR-library contract. Native shared-library exports remain deferred until a specific non-.NET embedding consumer requires that separate ABI.

## Milestone 28 — Deliver a source-first development loop

**Outcome:** a user can understand and develop a compiled project without treating generated C# and temporary build directories as the primary interface.

- [ ] Add a coherent run workflow to the existing project service and thin CLI/cmdlet surfaces. Select the declared target, handle arguments/stdin/stdout/exit codes, and reuse validated build outputs and locks.
- [ ] Extend incremental rebuild/watch behavior using existing source/dependency fingerprints. A changed callee, resource, provider, semantic profile, or dependency must invalidate the right artifact; a no-change run must reuse verified output.
- [ ] Present blockers grouped by complete function/workflow and explain the causal dependency chain, supported alternatives, and exact source locations. Keep semantic, shaping, dependency, and target failures distinct without making users read the full internal ledger.
- [ ] Prove source-level debugging through existing generated C#, PDBs, and source maps: breakpoints, stepping, locals, call stacks, and exceptions across a typed call and a Hybrid boundary. Explicitly identify any boundary that cannot yet be stepped through.
- [ ] Add a short contributor-independent quickstart for one EXE and one library/module project, with runnable examples and a supported-host diagnostic when prerequisites are missing.

**Exit gate:** from a clean workspace, complete init → lock → restore → run/test → edit → rebuild → diagnose/debug → pack using documented entrypoints. Observe the debugger and any editor UI directly. Verify argument quoting, redirected streams, cancellation, offline restore, cache invalidation, and source-line accuracy. Do not claim debugger integration from the presence of PDB files alone.

## Milestone 29 — Qualify distribution, providers, and platforms

**Outcome:** turn a source-proven preview into a declared, reproducibly installable supported product.

- [ ] Complete M21's compiler/core/CLI/PowerShell/provider package release set and clean public-feed install, upgrade, rollback, and consumer execution after explicit publication authorization. Local feeds and source builds remain separate evidence.
- [ ] Close M22's native-Linux test capability partition. Keep Windows-only signing, management, net472, and oracle fixtures in correctly labeled lanes; test actual Linux behavior in its own checkout/output tree.
- [ ] Qualify the next selected physical RID/deployment profiles one at a time, prioritizing an actual consumer need. macOS/Arm64, Windows Arm64, and extra deployment forms remain experimental until executed on their target.
- [ ] Close M19's disposable management-target reboot/reconnect gate only on a target explicitly placed under the test lifecycle. Preserve the separate qualified authentication/protocol/provider profiles.
- [ ] Exercise long/non-ASCII/space-containing paths, clean machine install, resource discovery, offline operation, package tampering, runtime servicing, uninstall, and side-by-side versions. Carry the real Windows path limitation into qualification instead of hiding it with an unusually short test path.
- [ ] Publish the support matrix and compatibility/security servicing policy with the actual released package versions and their immutable evidence.

**Exit gate:** the declared release set is publicly obtainable, survives clean install/upgrade/rollback, and runs the qualified workload packet on every advertised profile. CI, signed artifacts, public availability, installed versions, and target-host execution are independently recorded. Missing optional platforms narrow the supported matrix; they must not be reported as tested.

## Milestone 30 — Accelerate proven workflows

**Outcome:** make performance predictable after the selected programs are correct and useful.

- [ ] Select measured hot workloads from M24–27 and retain cold-start, short/cheap, and error-path controls. Compare original PowerShell, Hybrid, Strict managed, and NativeAOT only where each implements the same contract.
- [ ] Use the existing profiler and region graph to reduce crossing overhead, specialize loops, fuse compatible pipeline stages, and eliminate proven allocations through canonical IR/lowering passes.
- [ ] Record fresh-process startup, warm throughput, allocations, peak working set, build time, artifact size, and import cost with pinned compiler/toolchain/host identities and validated outputs.
- [ ] Make recommendations explain why a profile helps or loses for the measured workload. Keep tiny commands on an appropriate path when compilation overhead dominates.
- [ ] Consider another backend only after a measured C#/Roslyn/MSBuild limitation blocks a required result and the alternative can consume the same lowered IR and source/ABI contracts.

**Exit gate:** at least three unrelated qualified workflow families have repeatable comparisons, declared budgets, and honest wins/ties/regressions. A promotion must meet its predeclared workload budget without semantic or deployment regressions. No universal speedup or language-wide coverage claim follows from this gate.

## How to choose the next implementation slice

1. Capture a complete blocked workflow from an immutable source packet, its license/selection policy, and every blocker along its reachable execution path.
2. Minimize the semantic failure into a generic regression without importing or executing external administration code during analysis.
3. Fix accepted-code defects first. Preserve the failing original/generated example and sweep sibling assignment, call, collection, or lifetime paths.
4. Choose the smallest reusable contract that removes the workflow's decisive blockers. Keep other missing contracts visible instead of refactoring the source to manufacture a success.
5. Prove the generated artifact and original behavior on the claimed hosts, update the unchanged census comparison, and add the complete workflow to acceptance.

The next tranche succeeds through complete workflow unlocks and reproducible consumption. Completed architecture checkboxes remain useful foundations, but they do not substitute for those outcomes.
