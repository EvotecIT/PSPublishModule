# PowerShell compiler: observed support gaps

Updated: 2026-09-26. This is the M29a discovery inventory, not a list of cmdlets that never work. The first five-module table is the frozen M29 baseline; the current M29a matrix below shows later changes on the same pinned source. The [next milestones](PowerForge.PowerShellCompilation.NextMilestones.md#milestone-29a--make-ordinary-powershell-forms-useful) own the work and exit gate; the [assessment](PowerForge.PowerShellCompilation.Assessment.md) owns execution evidence.

The five unchanged M29 module packets contain 565 source functions. The final same-input Hybrid census emitted 341 complete functions on `net10.0` and 340 on `net472`; 224 and 225 respectively remained hosted. The [per-function ledger](../Benchmarks/PowerShellCompilation/Corpus/m29-portfolio-ledger.net10-net472.json) records exactly which ones. An emitted method may itself call PowerShell. A retained function may contain a promoted typed region. Neither count is a percentage of the PowerShell language.

| Pinned module | Complete methods, net10.0 | Complete methods, net472 | Retained, net10.0 |
| --- | ---: | ---: | ---: |
| PSSharedGoods | 205/282 | 204/282 | 77 |
| PowerInfoblox | 49/66 | 49/66 | 17 |
| PSScriptTools | 22/109 | 22/109 | 87 |
| platyPS | 22/40 | 22/40 | 18 |
| CleanupMonster | 43/68 | 43/68 | 25 |

The final-source five-packet, two-target census summary has SHA-256 `250abe61cdc546b8f8ac91258e243249fe761abff7ec6ba16db9bfd8900e9982`. Its `net10.0` feature-impact counts below are **affected source units**, not distinct functions unlocked by one fix; categories overlap, and a diagnostic can also occur inside an emitted Hybrid method. The reported “sole” count is only a visible single-feature explanation, not proof that emission and execution would follow if that feature were relaxed.

| Reported gap family | Affected units / visible sole, net10.0 | Concrete observed form |
| --- | ---: | --- |
| Broad `syntax.unsupported` | 99 / 83 | An umbrella for different control-flow, collection, member, and capture rules; it is not one implementation task. |
| Parameter metadata | 66 / 60 | Mostly `[OutputType(...)]` whose CLR type cannot be resolved to the current single-type metadata contract; 64 units are in PSScriptTools. |
| Parameter types | 21 / 10 | Target-unavailable or unresolved authored types, including platyPS `PSSession` and external AD types. |
| Nested function definitions | 19 / 6 | Functions declared inside a function or statement body need closure, scope, and lifetime behavior. |
| Method-invocation expressions | 12 / 12 | Eleven reported diagnostics are calls inside observed `catch` boundaries; argument effects and caught-error identity are not yet preserved there. |
| `Register-ArgumentCompleter` | 9 / 5 | PSScriptTools registration has a PowerShell host/session effect and no admitted typed equivalent. |
| `begin`/`process` lifecycle | 9 / 0 | Advanced function lifecycle and its missing lowering contract occur together. |
| File-scoped class/enum declarations | 8 / 3 | PSScriptTools functions sharing a file with authored types remain hosted. The separate pinned powershell-yaml packet has 0/14 complete methods because of a top-level enum and still needs loader qualification. |
| Typed nested script blocks | 6 / 3 | A delegate or explicit hosted boundary is needed for the observed captures. |
| Parameter defaults | 5 / 0 | Runtime-evaluated defaults, including platyPS `$Encoding`, are outside the current typed parameter contract; PSScriptTools also reports `ValidateScript` metadata separately. |
| Unqualified `Write-Verbose` | 6 / 0 | The host can resolve a different command than the built-in provider; this often co-occurs with other blockers. |
| Runtime scope | 5 / 0 | `$PSBoundParameters` and `$PSCmdlet` uses require their PowerShell invocation context. |
| Unqualified `ForEach-Object` | 4 / 0 | Some PSScriptTools/platyPS pipelines retain runtime command resolution. The special bounded PSScriptTools file loader is already recognized for source discovery; that does not compile every `ForEach-Object` invocation. |

The broad syntax group contains these **specific observed gaps**. Counts here are diagnostic occurrences in the five `net10.0` census packets, so they are useful for finding examples but must not be added to the affected-unit table:

| Authored form or effect | Observed occurrences | Current boundary |
| --- | ---: | --- |
| Capturing statement output into a typed variable or return | 14 plus 3 enclosing-return diagnostics | Requires a qualified success-stream host and a safe target/return transfer; `Get-ObjectPropertiesAdvanced` is one retained PSSharedGoods example. |
| Dictionary or `[pscustomobject]` values assembled from statements or conditionals | 11 dictionary and 3 note-property diagnostics | This frozen count predates bounded native-hosted conditional and single-command values. Broader statement values remain unqualified; `Convert-FolderEncoding` is one PSSharedGoods example. |
| Member invocation within `catch` | 11 | Error identity and argument side effects must survive the boundary; `Get-LocalComputerSid` is one retained PSSharedGoods example, not an approved execution probe. |
| Nested definitions | 39 combined syntax diagnostics | A single authored definition can produce multiple diagnostics; `ConvertTo-JsonLiteral` is one PSSharedGoods example. |
| `begin`/`process` blocks | 9 diagnostics for each block | Pipeline lifecycle, stream behavior, stop, and cleanup remain jointly hosted. |
| Object-valued `switch` and observed `$_`/`$switch` state | 7 object-type and 8 automatic-variable diagnostics | Scalar switch lowering does not cover these runtime-dependent shapes; `Convert-Size` is one retained example. |
| Indexed mutation through an unsupported receiver | 5 target and 5 direct-return diagnostics | The current typed rule handles narrower arrays, lists, and dictionaries; `Get-ComputerPorts` is one retained example. |
| Subexpressions and nested script blocks | 10 diagnostics for each | Several PSScriptTools forms need explicit statement-output and closure rules. |
| Range operator `..` in the observed expression shape | 6 | PSScriptTools reached an unbound `DotDot` binary operator; other range shapes must be checked independently. |
| Labeled `break`/`continue` | 4 labeled-break diagnostics plus labeled-continue cases | CleanupMonster control transfer is not represented by the current typed loops. |
| Bare `throw` outside a supported catch and `throw` of a string | 3 PSSharedGoods throw-statement diagnostics plus PowerInfoblox examples | `New-PSRegistry` is one retained example. Preserve PowerShell error construction and continuation rather than treating bare `throw` as an ordinary CLR rethrow. |

The next syntax work is ranked by *reachable functions*, not diagnostic count. The M29 build ledgers show the following sole visible `syntax.unsupported` candidates; these are leads for qualification, not promised method gains:

| Family | Observed functions | Useful offline path | Qualification boundary |
| --- | --- | --- | --- |
| Object-valued `switch` | Five PSSharedGoods and four PowerInfoblox retained functions at the frozen baseline | `Convert-Size`, `Get-InfobloxDNSRecordValueField`, and three runtime-valued conversion workflows are offline probes | Direct string-parameter, bounded command-result, and invocation-owned runtime-value forms are qualified separately. Regex/wildcard/file forms, effectful labels, and broader command-pipeline capture remain hosted. |
| Captured statement output | Five PSSharedGoods and three PowerInfoblox retained functions at the frozen baseline | `ConvertFrom-ScriptBlock` inspects an AST without invoking the passed script block | Direct `[Array] = foreach` now has bounded Hybrid proof. Other target types, enclosing returns, AD, logging-file mutation, and WAPI calls need separate semantics or safe offline boundaries. |
| Range `..` | Six diagnostics, all in PSScriptTools `Show-ANSISequence` | No complete offline workflow unlocked by range alone | That function also has lifecycle, nested-definition, command, and runtime-scope blockers; range is deferred until a representative reachable use appears. |

The first bounded `switch` slice recovered a direct, top-level exact switch over the same authored `[string]` parameter in a Hybrid native function. Its scalar type guarantee rejects preceding commands, member calls, and rebinding. On unchanged PSSharedGoods, `Convert-Size` and the deprecated `Set-EmailBodyReplacement` gained complete methods on both targets; only `Convert-Size` was executed offline. The later runtime-valued slice qualifies `Convert-ExchangeSize` through host-owned variable lookup and enumeration, preserving a parameter removed and recreated as an array by a preceding `Select-String` callback. It does not relax the scalar guarantee. The PowerInfoblox command-result condition uses a separate success-record capture contract.

A second bounded Hybrid slice collects success records from direct `[Array] $target = foreach (...) { ... }` assignments through the existing native output collector. Unchanged PSSharedGoods `ConvertFrom-ScriptBlock` gained a complete method on both targets and matched four offline original/generated cases on PowerShell 7 and 5.1. A later bounded slice admits direct literal member assignment from conditional statement output, such as `$Data.Properties = if (...) { ... }`. The unchanged PSSharedGoods `Get-ObjectPropertiesAdvanced` gained a complete method and matched offline original/generated cases on both hosts, with its hosted command regions still explicit. Its method replaces a former partial region. The current affected-packet count is **209/282** (`net10.0`) and **208/282** (`net472`); same-input CleanupMonster remains **43/68** on each, with no lost retained-function regions. These figures update only those packets, not the frozen five-module baseline table above. Other typed captures, return transfers, and command-result success streams still need their own contracts. The [assessment](PowerForge.PowerShellCompilation.Assessment.md#milestone-29a-sixth-slice--direct-member-conditional-output-capture) records exact evidence and limits.

The PowerInfoblox local-command switch is a separate success-stream case. Its earlier read-only two-function packet emitted the helper but retained `Get-InfobloxDNSRecordValueField` (**1/2** complete methods), and original-only probes established zero/many/error behavior under module-session helper rebinding. The bounded Hybrid native rule now captures the authored condition command's success records before executing any clause, then compares each record against literal string labels in source order. The helper remains resolved and invoked by the PowerShell host. On the same unchanged two-function source, both methods now emit and original/generated offline probes match 13 normal and shadowed-helper cases on PowerShell 7 and Windows PowerShell 5.1, including zero, two, mixed-case, and partial-output-then-throw behavior. A same-input full PowerInfoblox `net10.0` explanation rose **51/66 → 52/66** complete methods without losing a previously emitted method or a region of a still-retained function. Strict rejects the source. Broader switches with observed automatic variables, nonliteral labels, or command-bearing clause bodies stay hosted; no full-module generated execution or fresh full-portfolio claim follows from this slice. See the [assessment](PowerForge.PowerShellCompilation.Assessment.md#milestone-29a-eighth-slice--native-command-result-switch-iteration) for exact evidence.

The next bounded clause-output form is a literal string split on a literal comma, as in unchanged PowerInfoblox `Get-InfobloxDNSRecordPreferredField`. It uses the existing PowerShell-owned split operation and success-output writer; the command condition remains hosted. The three-function mini module now emits all three methods on both targets, and 14 offline original/generated cases match on PowerShell 7 and 5.1, including field order and cardinality. The full PowerInfoblox packet reaches **53/66** complete methods on each target, with no previously emitted method or still-retained region lost. The `net10.0` comparison is **52 → 53** for this slice; `net472` compares with an older **51/66** checkpoint and includes both qualified command-result switch functions. See the [assessment](PowerForge.PowerShellCompilation.Assessment.md#milestone-29a-ninth-slice--literal-comma-split-switch-output). Arbitrary split patterns and effectful clause bodies remain outside this admission.

### Recorded PSSharedGoods triage before hosted literal values

A same-input Hybrid `net10.0` explanation at branch head `158fe3438` reported **209 complete methods and 73 retained functions** among 282 authored functions (JSON SHA-256 `2aa53f6f2769f4e5622ea43a5e948150018e301237980902b9271be480389044`). Of the retained functions, 52 had one reported cause. This predates the hosted literal-value, captured-return, direct-range, and native indexed-value slices; the pre-interpolation affected PSSharedGoods packet emitted **220/282** on `net10.0` and **219/282** on `net472`. These older blocker counts have not been recomputed. A sole diagnostic is not a promise that changing that rule alone produces equivalent generated execution: authored state, command effects, and packaging still need artifact proof. The following were distinct retained functions in that earlier packet, unlike the frozen five-packet diagnostic totals above.

| Sole reported blocker at the earlier checkpoint | Retained functions then | Next proof or reason to keep hosted |
| --- | ---: | --- |
| Indexed value use outside the direct-return contract | 4, including the three country-conversion functions | Those three country functions now emit through Hybrid native binding and match offline original/generated cache, map, and repeated-call behavior on both Windows hosts. Other indexed shapes still need separate proof. |
| Native method invocation inside `catch` | 4, including `Get-PSSubRegistry` and `Test-ComputerPort` | Preserve caught-error identity and argument side effects; registry and network operations are not offline execution probes. |
| Indexed mutation with a receiver outside the current typed set | 3, including `ConvertTo-FlatObject` | Bounded variable/property interpolation now uses the invocation-owned assignment operation. `ConvertTo-FlatObject` emits and matches unchanged-source recursive offline flattening on both Windows hosts. `Get-ComputerPorts` also emits but remains unexecuted; other machine/file-facing examples and computed effectful keys need separate proof. |
| `-is`/`-isnot` against a type not statically resolved on the target | 3, including `Convert-Office365License` | Resolve the authored type in the selected artifact; do not substitute a different CLR type or execute tenant-dependent paths. |
| Captured output into a still-unqualified target | 3, two AD functions and `Set-LoggingCapabilities` | Keep provider/file effects hosted until a safe target and offline effect boundary exist. |
| Return inside captured statement output | 2, `Convert-BinaryToIP` and `Convert-UAC` | Both now emit and match bounded original/generated offline execution on both Windows hosts. `Convert-BinaryToIP` also required direct native `foreach` range admission; its warning commands remain hosted. |
| Switch observing `$_`, `$PSItem`, or `$switch` | 2 earlier diagnostics, `Resolve-Encoding` and `Convert-Color` | The ownership diagnostic correction alone gained no methods. Both functions now emit and execute through the separately qualified runtime-valued exact-switch owner. Broader matching, deferred blocks, and command-condition forms still need their own contracts. |

The qualified Hybrid return transfer lets a direct statement-output assignment release earlier success records and the returned value while exiting the enclosing function. An `@(...)` capture instead discards its pending records on that transfer. Both still run authored `finally` blocks. The unchanged `Convert-UAC` function proves the direct form in an offline mini module on PowerShell 7 and Windows PowerShell 5.1; the captured command pipeline inside it remains PowerShell-hosted. Direct native `foreach` can now consume the existing host-owned range result through the host enumeration binder. Unchanged `Convert-BinaryToIP`, which combines that range with `@(...)` capture, gained a complete method and matched seven offline original/generated cases on each Windows host. Its two `Write-Warning` sites remain hosted, Strict rejects it, and this does not establish arbitrary range sizes, other platforms, or full-module generated import. The [assessment](PowerForge.PowerShellCompilation.Assessment.md#milestone-29a-eleventh-slice--native-range-in-direct-foreach) records the evidence and limits. Native indexed-value binding subsequently qualified five in-memory PSSharedGoods functions, including the three country conversions; their script caches and PowerShell invocation remain host-owned.

### Current pinned portfolio and ordinary script forms

The latest complete same-input five-packet Hybrid `net10.0` explanation covers **565 stable function IDs** and emits **453 complete methods**; **112 remain hosted**. Exact-switch predicates add one complete CleanupMonster method with both-host offline selection proof. Nested filters add three methods, all with both-host offline original/generated execution proof: `ConvertTo-PrettyObject`, `ConvertTo-JsonLiteral`, and `ConvertFrom-ObjectToString`. Nested declarations add nine methods; unchanged CleanupMonster report merging has both-host offline execution proof, while eight other gains remain emission-only. Literal method-argument script blocks add 11 complete methods. `Get-LastModifiedFile` has both-host local-file execution proof, and `Get-ComputerSMB` matches an offline provider; nine other gains remain emission-only. Earlier assigned try-array capture qualifies `Get-SqlQueryColumnInformation` under an offline provider. The earlier report workflow has both-host offline execution proof; folder inspection is qualified on PowerShell 7/net10.0; email remains emission-only. Earlier typed-array and SDK workflows retain their bounded proofs. No formerly emitted method or still-retained region was lost. The [current comparison](../Benchmarks/PowerShellCompilation/Corpus/m29a-switch-predicate-comparison.net10.json) records changed dispositions and links the complete per-function baseline; the [retained-function inventory](../Benchmarks/PowerShellCompilation/Corpus/m29a-retained-functions.net10.json) records current causes and regions for all 112 retained functions. PSScriptTools, PSSharedGoods, and CleanupMonster use their qualified `CompleteModule` source modes. Earlier checkpoints remain historical; these counts do not imply execution of every emitted function or a refreshed full `net472` portfolio.

| Pinned module | M29 → current complete methods, `net10.0` | Current retained | Highest-value reachable shape or justified boundary |
| --- | ---: | ---: | --- |
| PSSharedGoods | 205 → 242 / 282 | 40 | SQL metadata and SMB projection match offline providers on both hosts; actual SQL/SMB/CIM access stays hosted and unexecuted. Closed typed-array capture qualifies the two integer flag decoders. SDK data contracts, folder analysis/conversion, registry reads, and logging defaults retain their bounded proofs. `Send-Email` is emission-only; no SMTP execution is claimed. The conversion wrapper's authored Converted status limitation is preserved. Three filter-dependent object/JSON/host-formatting workflows now have both-host offline proof. |
| PowerInfoblox | 49 → 55 / 66 | 11 | Offline DNS helpers retain their prior execution proof. This fresh packet refreshes the older 53-method table entry; this access-mutation slice adds no method. WAPI-facing paths were not executed. |
| PSScriptTools | 22 → 78 / 109 | 31 | `Get-LastModifiedFile` has both-host offline file-selection proof. Nine other callback gains are emission-only. `Test-EmptyFolder` has offline net10.0 proof; net472 retains its unavailable EnumerationOptions type. `Get-PSProfile` and four filename/ANSI/value/string workflows retain both-host proof. `Get-PSSessionInfo` and other emitted methods remain unexecuted. |
| platyPS | 22 → 24 / 40 | 16 | PSSession and MAML renderer/parser types need their own target/dependency contract; nested definitions and pipeline lifecycle also block complete methods. |
| CleanupMonster | 43 → 54 / 68 | 14 | `Export-ADComputerReportData` matches both-host offline serialization and failure cleanup on owned files and in-memory rows. SDK data contracts qualify the pure `Test-ADQueryConfigurationError` classifier. `Invoke-ADComputerInventoryQuery` and `New-ADComputersStatistics` remain execution-unqualified. `Merge-CloudDeviceReportInventory` now matches offline deduplication and input preservation on both hosts. `Get-CloudDevicesToProcess` now matches offline compliance selection, processed-record exclusions, and protection rules on both hosts. No AD/cloud operation was run. |

The current retained inventory reports these **distinct affected retained functions**. Categories overlap; a diagnostic can be a conservative co-blocker rather than the sole missing capability. These counts guide workflow selection, not predicted compiler gains.

| Current observed family | Retained functions | Work or explicit boundary |
| --- | ---: | --- |
| Unsupported syntax umbrella | 60 | Split the authored forms before implementation; do not relax a generic syntax check. |
| Unresolved or unavailable parameter types | 26 | Preserve authored identity and target/dependency availability. |
| Remaining scriptblock.typed diagnostics | 4 | Qualify scope, capture, invocation lifetime, and effects through a shared owner. |
| Remaining nested declarations/body failures | 2 | Ordinary functions, filters, and header parameters preserve native declaration/metadata ownership. Remaining child co-blockers, workflows, and dynamic parameters stay hosted. |
| Pipeline lifecycle and lowering | 2 each | Qualify the joint begin/process/stream/stop contract; counts refer to overlapping functions. |
| Unqualified Write-Verbose | 4 | Keep session resolution and stream preferences; fix reachable co-blockers first. |
| Member invocation expressions | 7 | Inspect the specific argument/catch/receiver contract; ordinary member calls already work. |
| Runtime scope; Write-Warning | 2 each | Keep invocation context and stream/command identity explicit. |
| Authored types; ForEach-Object | 5; 2 | Separate source closure, pipeline script-block behavior, and value-capture forms. |
| Subexpressions; runtime parameter defaults; conversions | 1; 1; 4 | Preserve evaluation timing and authored type constraints. |

The later historical table still describes the earlier 380/565 checkpoint. Its sole-cause counts are not current residual counts.

Literal script blocks passed directly to a method now use the same compiled callback factory as assigned blocks. The method remains PowerShell-bound; fully eligible callback bodies become private CLR methods. Dynamic free-variable reads, `$_` for collection methods, `$args` for delegate conversion, creation timing, and child closure eligibility retain their existing owners. Unsupported child bodies or lowering failures retain the surrounding function. Metadata blocks and blocks inside hosted command pipelines do not enter this value route. The drop in scriptblock.typed diagnostics is not a claim that every block works: some residual child/owner failures now appear in other diagnostic families. Ordinary nested named definitions now use private compiled callbacks and statement-time native registration. Filters preserve their process clauses, and function-header parameters use the canonical parameter view with their original metadata. Unsupported child bodies, workflows, and dynamic parameters remain hosted; Strict does not admit these native declarations.

All five module rows above use the same fresh **453/565** explanation. Earlier values remain historical. The string-to-numeric static call in unchanged `Convert-ToDateTime` retains its original/generated offline proof on both hosts; it remains PowerShell-runtime-backed, and other static overload families remain unqualified.

Literal script-block clauses in an unlabeled exact switch now use fully compiled private callback methods with native predicate invocation, local scope, captured-record Boolean conversion, current-item state, and module-owned constant identity. Unchanged CleanupMonster compliance selection is qualified offline on both Windows hosts. Unsupported child bodies, escaping predicate `break`/`continue`, command-result predicates, regex/wildcard/file/labeled forms, and runtime-free Strict remain outside this extension. This is Hybrid compilation with PowerShell runtime semantics, not a dependency-free C# switch.

Assigned native `@(try { ... } catch { ... } finally { ... })` now uses the existing output collector and assignment owner. Both-host proof covers handled records, explicit null, escaping errors retaining previous storage, typed conversion failure, return through finally, and repeated calls. Escaping break/continue remain hosted; runtime-free Strict remains rejected. The unchanged SQL metadata workflow normalizes its query arguments and error text under a rebound offline provider; this does not qualify a live database, arbitrary nested capture bodies, or SQL query safety.

Hybrid native typed `@(...)` capture uses the existing collector and authored assignment owner, including element conversion. The previously observed exception-type difference was caused by the catch bridge always using the general typed-handler matcher. It now preserves the native single-untyped-catch wrapper and uses the general matcher only for typed/mixed handlers. The temporary literal-only `PSB2940` guard has been removed after both-host original/generated proof of conversion failures, callbacks, previous storage, typed/mixed catches, source positions, and finally behavior. Persistent local/parameter constraints and equivalent `local:` spellings are covered. This is not runtime-free Strict support. Guarded-vector fallback remains available for co-blockers, and capture-exiting loop transfers still fail closed. Provider operations and arbitrary nested bodies are not qualified by these probes. The [catch-identity comparison](../Benchmarks/PowerShellCompilation/Corpus/m29a-catch-identity-comparison.net10.json) proves all five explanations are byte-identical to the accepted 425/565 ledger, so this correction adds behavior proof without claiming more pinned methods.

Direct catch-owned reads of unqualified `$_` and `$PSItem` now select the existing native invocation owner in Hybrid mode. The simple CLR parse/catch observer no longer requires pipeline parameter metadata to compile. Both-host probes preserve nested error restoration, alias identity, rethrow, finally, and previous automatic state, and an unchanged offline `Convert-IPToBinary` workflow preserves warnings and its authored catch under a rebound helper. Deferred blocks and inner switches do not qualify through the enclosing catch; the whole-function binder still enforces its other contracts. Runtime-free Strict remains closed. The [catch-state comparison](../Benchmarks/PowerShellCompilation/Corpus/m29a-catch-state-comparison.net10.json) preserves all five module dispositions at 425/565; no additional pinned emitted function is claimed.

Resolved constructor receivers now remain distinct from authored Type values in observed-catch arguments. A whole `::new(...)` expression recursively checks its arguments instead of treating the constructor's type receiver as the resulting value. Transformed or collected Type arguments retain their guards. The new report and folder workflows exposed a second correctness gap: the authored `[pscustomobject]` alias consumes `PSTypeName` as type-name metadata, not a note property. These literals now select the existing native custom-object converter after ordered value evaluation; ordinary custom-object literals retain their prior path. String, empty, null, numeric, and array metadata agree with original execution on both hosts. Runtime-free Strict and explicit PSObject-cast semantics are not broadened by this metadata path.

The literal-enum and indexed-conditional slice recorded **228/282** PSSharedGoods methods, up from **225/282**; that checkpoint reached 230/282 after access increments. `Get-PSSubRegistry` and `Get-PSSubRegistryComplete` have unchanged-source original/generated execution proof on both Windows hosts. A later fixture-only wave qualifies `Set-LoggingCapabilities` without a log path: caller-map and module-default mutation, empty cleanup, switch presence, identity, and repeated invocation match on both hosts; filesystem creation/rotation remains unqualified. The earlier three runtime-switch gains remain qualified. No previously emitted method or region of a still-hosted function was lost. The last full `net472` packet remains the earlier **219/282** observation; both-host builds and execution do not refresh that full packet. Earlier packet counts in the triage narrative remain historical checkpoints.

Literal static enum-field arguments and their simple aliases now use the existing native invocation owner without being mistaken for authored `System.Type` values. Enum transformations such as `.GetType()` retain the type-argument guard. Simple indexed assignment from `if` output uses the existing native capture/assignment owners; complex index effects and runtime-free Strict capture remain unqualified. The registry probe covers local authored reads and caught conversion errors, not remote registry administration. `Get-FolderEncoding` is now qualified: its generic pipeline diagnoses actually referred to member/index increments. The native compiler owns those increments, including nested indexed storage; method/command-bearing mutation targets and runtime-free Strict access mutation remain outside admission.

The new switch owner preserves runtime cardinality and PowerShell item conversion instead of inferring scalar input from a member name. The encoding workflow matches zero/many results from an isolated `ToUpperInvariant` type-data override; the size workflow matches a parameter removed and recreated as an array by a shadowed command. Synthetic artifact probes cover item mutation, case sensitivity, repeated stringification, direct and callback `break`/`continue`, `return`, and normal state restoration. An independently found compound-pipeline array-record mismatch was reproduced on both hosts and corrected by keeping that condition hosted. Strict, regex/wildcard/file forms, effectful labels, and labeled runtime switches remain outside the new admission. See the [assessment](PowerForge.PowerShellCompilation.Assessment.md#milestone-29a--invocation-owned-runtime-valued-exact-switches).

Direct returned `[string]$script:Value` `.Length` and `[string[]]$script:Values` indexing now have a narrow Hybrid native-invocation route, proven on both Windows PowerShell hosts in a seven-function fixture. The conversion can run a PowerShell callback, so it is not a runtime-free string operation. Derived values and effectful indices remain outside this new selection. No direct matching cast was found in the tracked PSSharedGoods or CleanupMonster full-module packets, and the external three-packet census has not been refreshed for this slice; the five-module method totals above are unchanged claims, not a new census result. Four older negative fixture assertions were separately reconciled against original/generated native-bound execution, including caller command resolution; they were stale typed-only expectations, not four compiler gains.

| Authored form and examples | Retained / sole reported | Result, effects, and current disposition |
| --- | ---: | --- |
| Scalar `switch` whose condition binds as `Object` | 6 / 6 at the recorded baseline | These are distinct shapes. The two PowerInfoblox command-result forms use a success-record collector. `Convert-ExchangeSize` and PSScriptTools `Format-Value`/`Format-String` now emit through the runtime-value owner and match offline both-host execution, including nested switches and multiple pipeline records. CleanupMonster's member-call shape needs its own safe workflow proof. |
| `switch` with observed `$_`, `$PSItem`, or `$switch` (`ConvertTo-InfobloxDNSRecordCreateBody`, `New-ANSIBar` at the recorded baseline) | 11 / 8 | Reads inside the switch need runtime-owned item and iterator state. The observer distinguishes unrelated function reads. `New-ANSIBar` now emits and matches standard/custom characters, gradient modes, spacing, aliases, and verbose output on both hosts. Other shapes remain separately qualified or hosted; these residual diagnostic counts have not been recomputed. |
| Conditional/statement values in dictionaries or `[pscustomobject]` note properties (`Get-FolderEncoding`, `Get-PSProfile`) | 11 / 6 dictionary; 7 / 5 note-property at the recorded baseline | One ordinary named command can now supply a literal value through a hosted Hybrid capture with PowerShell's zero/one/many collapse. This does not translate the command to CLR, admit multistage pipelines, or qualify arbitrary statement values; current residual counts require a fresh breakdown. |
| Statement-output assignment or return (`Add-InfobloxNetwork`, `Convert-BinaryToIP`) | 7 / 3 target; 3 / 2 enclosing return | These are frozen baseline counts. Direct Hybrid captured-return transfer and direct native range iteration now promote `Convert-UAC` and `Convert-BinaryToIP`; other targets remain separately qualified or hosted. |
| Nested script blocks and nested function definitions (`Set-PSAnsiFileMap`, `New-ExternalHelp`) | 20 / 9 blocks; 22 / 4 definitions | Closure, invocation scope, automatic variables, lifetime, and host command identity need explicit contracts. Hybrid retains the affected functions; Strict has no blanket admission. |
| Unavailable or unresolved authored parameter/CLR types (`New-MarkdownHelp`, `Convert-HashtableString`) | 27 / 14 parameter types | Generated signatures must preserve authored type identity and package availability. External MAML/PowerShell/AD/WPF dependencies remain hosted or fail closed until target-specific proof. |
| Member invocation inside `catch` (`Get-PSSubRegistry`, `Test-EmptyFolder`) | 13 / 12 at the recorded baseline | This is not a blanket current catch restriction. The native guard concerns authored Type-valued arguments under observed catches; direct type/closed alias catch-all shapes have bounded proof. Remaining transformed/effectful type arguments and typed-catch variants need their own contracts. Registry, file, and network effects require scoped execution evidence. |
| `begin`/`process` lifecycle (`Merge-MarkdownHelp`) | 12 / 0 | Pipeline cardinality, streams, stop, and cleanup co-occur with other blockers. This is a hosted lifecycle, not twelve ready method gains. |

Three small standalone sources established the original `net10.0` distinction: `Get-Date -Format yyyy` had one typed Strict executable unit and original/generated Windows x64 execution proof; dynamic-format date and `ForEach-Object` script-block roots were hosted in Hybrid and rejected in Strict. The private Hybrid root path now emits a sequence of direct dynamic-format date and `ConvertTo-Json` commands with hosted command regions, with original/generated execution proof on `net10.0`/Windows x64. Only the first command can consume a prebound string parameter; later ones use literals. The `ForEach-Object` script-block root remains hosted. A direct dynamic-format `Get-Date` inside an authored advanced Hybrid function can also emit, while moving the same body into a basic function retains it for `binary-module.cmdlet-shape`; those function binding contracts differ. Other eligible Hybrid roots still report `artifact.executable-script-root` when retained. Earlier explanation hashes in the [assessment](PowerForge.PowerShellCompilation.Assessment.md#milestone-29a-current-portfolio-explanations) are historical snapshots. The [Hybrid entry contract](PowerForge.PowerShellCompilation.HybridEntryContract.md) records the admitted boundary and remaining proof requirements.

The earlier PSScriptTools observer correction kept **59/109** methods and corrected causes without new execution. The runtime-valued switch owner now raises the same pinned `net10.0` packet to **63/109**: `New-CustomFileName`, `New-ANSIBar`, `Format-Value`, and `Format-String` all gain complete methods and match original/generated offline execution on PowerShell 7 and 5.1. The packet retains all 121 unit IDs and loses no prior method or region. `New-PSFormatXML`, `Write-ANSIProgress`, and `Get-LastModifiedFile` remain separately blocked. The filename workflow preserves hosted command resolution through a fixed session clock; formatting probes exercise nested switches, multiple pipeline records, aliases, value types, and verbose streams. This is mini-module execution, not a full generated PSScriptTools import or a new full `net472`/five-module census. See the [assessment](PowerForge.PowerShellCompilation.Assessment.md#milestone-29a--cross-module-runtime-switch-qualification).

**Ordinary command shapes also need attention.** The [module quickstart](PowerForge.PowerShellCompilation.ModuleConsumers.md) uses `Get-Date -Format yyyy`. A fresh one-function Hybrid build on both Windows hosts emits its complete CLR method with one hosted command region; the original and generated imports return the current integer year, and both honor a session function shadowing `Get-Date`. The earlier description of the whole function as retained was too broad. The new bounded intrinsic binds a literal `-Format` to a runtime-free string result only when command identity is safe: a canonical module-qualified call, or an unqualified call in a host-free Strict target without a local shadow. Dynamic formats, PowerShell's named `FileDate*` formats, other parameters, and an unqualified Hybrid call keep runtime resolution. The no-argument `Get-Date` rule remains. PSScriptTools also reports unqualified `Write-Debug`, `Write-Host`, `Write-Warning`, `New-Object`, and `ForEach-Object`; PSSharedGoods reports `Write-Host` and `Select-Object`; `Out-String` is hosted in PSScriptTools. These are observed invocation forms, not claims that every use of each named cmdlet is unsupported.

| Representative source shape | Result or effect to preserve | Current qualified route and remaining gap |
| --- | --- | --- |
| `Get-Date` with no arguments | One local `DateTime` value | Direct runtime-state binding with safe command identity; an unqualified Hybrid call remains session-resolved. |
| `Get-Date -Format yyyy` or a literal .NET pattern | One culture-formatted String | Direct binding with safe identity in this M29a slice; unqualified Hybrid invocation stays a hosted command inside an emitted method when the surrounding function is admissible. Named `FileDate*`, variable formats, and other parameter sets remain outside the rule. |
| Hybrid executable top-level statement | Script parameter binding, command lookup, success/error streams, and exit behavior | A sequence of direct `Get-Date` or `ConvertTo-Json` calls can enter one private compiled root method while each command stays hosted. Only the first may read prebound string arguments; later calls use literals because an earlier hosted command can rebind those parameters. Other commands, statement forms, and richer script shapes remain in the packaged PowerShell entry point pending differential proof. |
| PSScriptTools `[OutputType('Name')]`, multiple types, or `ParameterSetName` | Advisory `Get-Command` output metadata, not a value guarantee | Non-whitespace literal string names and resolvable CLR type lists now propagate in source order, including repeated attributes with one literal `ParameterSetName` each, on advisory-metadata-capable Hybrid binary modules. They do not become return-type guarantees. Dynamic, mixed, unresolved CLR type expressions and whitespace-only names remain hosted; runtime-free Strict targets reject advisory-only collections. The 66/60 diagnostic baseline above predates these bounded implementations. |
| Unqualified `Write-Verbose` and related stream commands | Stream records, preferences, command lookup, error behavior | The host owns runtime resolution and stream routing. A reusable hosted command region may permit surrounding CLR code, but its presence is not direct translation of the command. |
| `ForEach-Object` or `Select-Object` with a script block or provider-bound input | Pipeline order/cardinality, `$_`, script-block scope, downstream streams | Bounded native forms need separate proof. Arbitrary unqualified pipeline commands and their session effects remain hosted. |
| Statement output captured into an assignment or return | PowerShell success-stream enumeration and no-output distinction | Direct `[Array] = foreach` and direct literal member `= if` are qualified in Hybrid on both Windows hosts. The 14 syntax diagnostics and three enclosing returns are the frozen baseline, not a fresh residual count; other targets and returns remain outside the proven contract. |

The separate inspect-only external assessment reports **Locksmith 13/44** and **Locksmith2 21/100** complete emitted functions on `net10.0`, with no complete workload execution. Locksmith's unresolved authored AD parameter types must not be admitted by pretending they are CLR types in the generated artifact. Locksmith2 prominently hits unresolved `[OutputType]`, member calls in `catch`, and unavailable `ErrorRecord` reference shapes. `Test-IsADAdmin` and `Test-IssueExists` have documented hosted dispositions in the [assessment](PowerForge.PowerShellCompilation.Assessment.md); they are not safe targets for an AD execution probe. Provider calls such as `Get-AD*`, network requests, prompts, and installer operations remain explicit host/effect boundaries until separately qualified. The external assessment evidence SHA-256 is `5e409b06e136b463caa5dd1c44b47ca852cd2deee579653529e61d0a091ab682`.

Unchanged PSSharedGoods `Convert-ExchangeItems` emits on both Windows targets through a hosted-module `Object` return for known scalar string/value-type branches. Eight original/generated calls match per host, and a separate fixture reaches both branch types. That slice's packet counts were **220/282** on `net10.0` and **219/282** on `net472`; they are historical checkpoints. Runtime-free Strict libraries still reject this heterogeneous return. Its attempted scalar admission of `Convert-ExchangeSize` was withdrawn after a rebinding mismatch; the later runtime-valued contract above now preserves that behavior and qualifies the function.

For the next implementation selection, split each retained function's co-blockers and identify a safe unchanged-source workflow. The formatted-date quickstart, bounded `[OutputType]` forms, direct string-parameter `switch`, direct `[Array] = foreach` capture, and direct member conditional capture have qualified slices above; their unqualified variants remain explicit. The largest frozen diagnostic group was parameter metadata, but its 66 affected units were **not** 66 ready-to-compile functions. A support claim should name the exact form, mode, host, command-resolution assumption, and remaining PowerShell boundary, then show original/generated behavior and same-input no-loss results. The [M29a checklist](PowerForge.PowerShellCompilation.NextMilestones.md#milestone-29a--make-ordinary-powershell-forms-useful) defines that proof without requiring the broad artifact matrix on every edit.
