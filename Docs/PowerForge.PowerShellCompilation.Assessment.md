# PowerShell compiler readiness assessment

Updated: 2026-09-20. The active continuation remains on `fix/compiler-audit-milestones` without a pull request. The forward-looking support matrix is `net10.0`/PowerShell 7.6 and `net472`/Windows PowerShell 5.1; `net8.0` is retired because its Microsoft support ends on 10 November 2026. Admission is semantic, not benchmark-driven: a generated method or region must preserve values, ownership, output, mutation, errors, ordering, and continuation. Transfer schema 6 admits compiler-owned mutation only for fresh one-dimensional stable-scalar vector construction until live-out transfer; it still rejects direct `System.Object`/ETS values, arbitrary enumerators, unproved collection ownership, borrowed collection mutation, maps/lists, and open or multidimensional arrays.

Hybrid native functions select the existing PowerShell-owned lazy `foreach` contract for direct iteration over an authored untyped or `[object]` parameter and for a direct map-key collection. The active host still owns enumerator acquisition, loop-variable storage, stopping, and authored cleanup; this does not make arbitrary enumeration runtime-free. Typed collection loops keep their established route, and direct range iteration remains hosted pending its distinct lazy-range proof. Bounded native `Hashtable` and `OrderedDictionary` literals now use the selected host's key comparer, and compiler-created map output passes the native escape guard while borrowed native references retain their identity. The unchanged PSSharedGoods `Get-ProtocolDefaults` emits a complete Hybrid method and matches original execution on both supported hosts; its former promoted prefix is absorbed into the whole method. Native function selection also routes authored `-split`/`-csplit` through the existing host operation, allowing the unchanged `ConvertFrom-X500Address` split-and-index result to emit as a complete Hybrid method. Before the conditional-value slice below, the pinned module census reported **180/282 emitted functions** and **38 promoted regions** across 286 units, with no input failures. Two pinned SID conversions remain complete through the direct type-literal/catch-all contract.

The pinned frontier moved from **180/282 to 188/282 complete emitted functions** with no lost emitted functions or input failures; promoted regions moved from 38 to 36 as partial regions became complete methods. The conditional-value contract now covers zero/null/one/many/nested records, branch and condition errors, warning/verbose/information observations, prior-destination preservation, and injected enumerable acquisition/advance/current/disposal failures with partial output and cleanup traces. Later bounded observed-catch and `[ref]` slices preserve their separate owner contracts without changing the census. Transfer schema 6 closes the remaining fresh typed-vector construction lifetime: compiler mutation is exclusive until live-out transfer, then retained PowerShell owns enumeration and later mutation.

M26 generalizes retained-function composition from one detached continuation to one maximal approved candidate from every non-overlapping canonical run. Structurally valid suffix candidates remain visible to the shared promotion policy, so a modeled CLR-exception route can reject an unsafe larger span without hiding a safe inner region; final reconciliation removes enclosed approved suffixes. The guarded prefix remains the only fresh-local owner. A synthetic retained module executes four scalar regions and one exact fresh-`ArrayList` local-factory region on both supported hosts, preserving collection identity, caller mutation, early pipeline stop, reuse, and remove/reimport. The same-input PSSharedGoods census remains **188/282 emitted functions and 36 promoted regions** because that module contains no third eligible disjoint run; M26 makes no artificial coverage-gain claim. An independent read-only review found one same-run suffix omission, which was fixed and confirmed with no remaining actionable finding.

The preceding M24/M25 checkpoint passed **1,056/1,056** compiler-gate tests with zero failures/skips. The final M26 checkpoint passes **1,060/1,060** compiler-gate tests with zero failures/skips, builds and executes **6/6 Strict `net10.0` `win-x64` programs**, and builds `PowerForge.PowerShell` for `net472` Release with zero warnings/errors. The separate totals keep the historical checkpoint distinguishable from the additional-region change. Live UDP and directory/mail dependencies remain separate provider-boundary work, not M24-M26 claims.

Other qualified Hybrid paths remain narrow. Complete authored pipelines use native command regions so PowerShell owns discovery, binding, script-block scope, streams, lifecycle, errors, stopping, aliases, and retained collection mutation. Explicit success-stream sinks such as terminal `Out-Null` still fail closed, and compiler-owned invocations feeding hosted pipelines retain their typed-continuation route. An exact zero-parameter fresh-`ArrayList` factory is admitted through canonical IR and invocation-owned storage. Range and regex-match operations delegate to the active PowerShell host; target-compatible type literals lower to `System.Type`, and Hybrid can resolve selected target-incompatible conversion types in that host. A direct type-literal argument under a catch-all can retain the original method-invocation error record through the native statement-error owner. A separate trailing direct-local `[ref]` slice writes a static CLR by-reference result back after a successful native call. Focused dual-host artifacts and the complete gate pass, but unchanged-workflow proof remains open. Nonlocal, member, multiple, early-argument, and by-value reference shapes stay hosted. Region reconstruction also leaves authored declarations installed when target-runspace import cannot construct an optimized alternative. The dated 2026-09-06 and 2026-09-07 sections below are historical evidence, not current package or release claims.

## Current audit: foundations exist, useful coverage is still narrow

The architecture is substantially ahead of the workload coverage. Source inspection confirms canonical parser/binder/IR/lowering owners, generated C#, profile/oracle contracts, dependency and provider locks, CLR ABI metadata, generated projects, Hybrid state/regions, and target-qualified Strict delivery policies. The next work should expand correct programs through these owners rather than introduce another backend.

| Area | What exists in current source | Remaining product gap |
| --- | --- | --- |
| Artifact modes | Package, Hybrid, Strict module/library, managed EXE, and NativeAOT paths with explicit runtime/dependency contracts | Arbitrary dynamic PowerShell remains hosted; an EXE/DLL extension alone proves no semantic compilation |
| Language semantics | Bounded typed values, calls, control flow, closed collection transfers, zero-record versus explicit-null return contracts, conditional return and scalar/vector-alternative envelopes, hosted commands and lifecycle, complete Hybrid-native multi-stage pipelines, closed stable-scalar `foreach` array captures, and host-bound computed member access through canonical IR | AutomationNull/dynamic empty-output closure, runtime-free dynamic objects/ETS or pipelines, general enumeration, and broader multiple-success-output contracts |
| Module-scale compilation | Live parent-owned Hybrid state, every independently safe non-overlapping statement-aligned region, structured scalar/map/vector/list transfer contracts, one bounded local collection-factory call, complete hosted pipelines with retained collection mutation, return-or-fallthrough continuation, real module workflows, and an independent runtime-free Strict state/lifetime owner | Borrowed compiled reference mutation, dynamic object/ETS transfer, arbitrary enumeration, provider-bound live dependencies, and additional complex lifecycle families remain fail-closed or later work |
| .NET consumption | ABI v4, generated projects, source maps, runtime-free provider adapters, lock/provenance verification | A broader set of ordinary C# consumer libraries and versioned API/lifetime qualification |
| Development experience | Project init/analyze/explain/recommend/lock/restore/build/test/pack/install/diagnose, verified cache, generated C#/PDB mapping | Coherent run/watch and demonstrated source-debugger workflows; source maps alone do not prove debugger usability |
| Distribution/platforms | Explicit preview support matrix and selected Windows/Linux Strict profiles | Public-feed install/upgrade/rollback, additional physical profiles, and the remaining disposable management-target qualification |

### Confirmed defect and correction

`ResolveAssignmentType` treated an RHS conversion as `Explicit` local provenance. Mutation binding interprets that provenance as a persistent variable constraint. PowerShell instead distinguishes `$Value = [int]2147483647` from `[int]$Value = 2147483647`.

The audit reproduced these observable failures in generated Hybrid modules on both PowerShell 7 and Windows PowerShell 5.1:

- An untyped local initialized with an Int32 cast and then incremented or updated with `+= 1` produced a conversion error instead of the original `Double` value `2147483648`.
- An untyped local initialized with a Single cast and updated with a Single operand retained Single precision/type instead of the original Double result.

The canonical local binder now infers the local representation from an RHS cast without inventing a variable constraint. The same distinction applies to an ordered-dictionary initializer. Expression-level casts still carry their authored conversion; LHS variable and parameter constraints retain their existing semantics. Unsupported unconstrained numeric promotion is retained under Hybrid and rejected by Strict. String-to-null reassignment is checked separately from constrained-string normalization, and unchanged casts and genuine constrained numeric updates remain compiled.

Regression coverage is in [PowerShellCompilationAssignmentConstraintTests.cs](../PowerForge.Tests/PowerShellCompilationAssignmentConstraintTests.cs); the owner change is in [PowerShellSemanticBinder.Locals.cs](../PowerForge.PowerShell/Services/Compilation/Binding/PowerShellSemanticBinder.Locals.cs). The pre-fix packet failed five of its six cases, including both generated-module executions; the extra string type-change case already retained source and was not a new defect.

One conservative coverage consequence remains explicit: scalar-string foreach currently requires `Explicit` or `Literal` provenance. A stable `$s = [string]'abc'` read through its inferred local can therefore lose typed eligibility. Hybrid retains the function and Strict may reject it. Recover that support with a closed scalar/null enumeration contract under M24, rather than labeling the local as constrained again.

### Fresh validation and limits

- The split/index composition passes original/generated comparisons on PowerShell 7.6/net10 and Windows PowerShell 5.1/net472 for the unchanged `ConvertFrom-X500Address`, including full/short output, multiple delimiters, empty and null inputs, non-ASCII text, repetition, and module reimport. A separate synthetic matrix checks both split operators with scalar, null, collection, and invalid-pattern inputs through indexed return, including error identity and cardinality. The pinned census rises from 179 to 180 emitted functions, with 38 promoted regions and zero input failures. The complete compiler gate passed **1,036/1,036 tests** with no failures or skips and built/executed **6/6 Strict `net10.0` `win-x64` programs**; the `net472` Release build passed with zero warnings/errors. This Hybrid slice does not expand Strict admission or close M24's exit gate.
- The ordered-map/hashtable slice passes dual-host original/generated tests for the unchanged `Get-ProtocolDefaults` known, case-varied, unknown, and 26-record list paths; ordered key/property order, output types and count, repeated invocation, and remove/reimport are compared. Synthetic probes cover host-specific key comparison under Turkish culture, ordinary hashtable construction and mutation, borrowed-reference identity, and collected-map cardinality. Non-native map escape remains hosted. The pinned PSSharedGoods census has 284 source files, 286 units, 282 functions, 179 emitted functions, 38 promoted regions, 31 retained region candidates, 11 analysis opportunities, and zero input failures. The complete compiler gate passed **1,032/1,032 tests** with no failures or skips and built/executed **6/6 Strict `net10.0` `win-x64` programs**; `PowerForge.PowerShell` builds for `net472` with zero warnings/errors. Independent read-only review found no actionable issue. Dynamic duplicate keys and deeper nested-array map output are not claimed by these focused probes.
- The direct type-literal/catch-all slice raises the complete pinned PSSharedGoods census from **174 to 176 emitted functions** with **39 promoted regions**, **30 retained region candidates**, and **12 analysis opportunities**. Unchanged `ConvertTo-SID` and `Convert-Identity` pass original/generated missing-identity, repeated-call, and module-reimport comparisons on PowerShell 7.6 and Windows PowerShell 5.1. A separate dual-host probe checks caught `$_` exception type, inner exception, error ID, and category; the wrapper correction applies only to the base method-invocation record, preserving the native matcher result for derived records such as remoting errors. A hoisted type-literal case stays retained and a typed-catch case has separate dual-host selection checks. `ConvertFrom-SID` still stops on a nested note-property expression, while `Test-ComputerPort`'s `[ref]` invocation has no claimed execution gain. The exact-candidate compiler gate passed **1,024/1,024 tests** with no skips and built/executed **6/6 Strict `net10.0` `win-x64` programs**; the `net472` Release build passed with zero warnings and errors. One earlier gate run had an intermittent mismatch in an unchanged dynamic-static invocation test's Windows PowerShell 5.1 `$Error` message; two isolated runs and the complete recheck passed without a code change. Its cause is unconfirmed and remains a watch item for M25, not evidence that the catch change fixed it.
- The 2026-09-19 compiler gate passed **1,014/1,014 tests** with no skips and built/executed **6/6 Strict `net10.0` `win-x64` programs**. Two newly added custom-enumerator differential cases were compiled and passed separately in the final Release run, which passed **5/5** focused cases across the two supported hosts. The `net472` Release build passed with zero warnings and errors. Independent read-only review found no actionable defect; its stated enumeration-failure proof gap was closed by those two cases.
- The complete bounded compiler gate passed **988/988 tests** with zero failures or skips, then built and executed **6/6 Strict `net10.0` `win-x64` programs**. The first post-suite Strict attempt materialized only two executables; a fresh isolated no-cache workspace built and executed all six, distinguishing the missing-artifact condition from a compiler-semantic failure.
- Focused original/generated collection and value-cardinality workflows pass on `net10.0`/PowerShell 7.6 and `net472`/Windows PowerShell 5.1. They cover atomic maps, stable-scalar vectors, `System.Array`, `ArrayList`, `List<object>`, zero-record bare returns, one-record explicit null returns, conditional return/fallthrough, branch-dependent `string`/`string[]` continuation constraints, ordinary and comma/no-enumerate output, downstream stopping, retained output/error continuation, remove/reimport, `Split-Array` cardinality, stable-scalar `foreach` capture in `Get-ADEncryptionTypes` and `Get-ADTrustAttributes`, and repeated invocation. The unchanged `Get-ComputerBios` workflow matches original execution for scalar and vector property selection on both hosts, and structural evidence covers the same reusable selector family in eight sibling commands. Pinned `Find-DatesPastWeek` and `Get-CloudCacheComputer` explicit-null paths match original execution on both hosts. Existing injected-enumerator tests cover partial records plus `MoveNext`, `Current`, disposal, and error-action continuation at the retained PowerShell boundary.
- Focused computed-member artifacts match original PowerShell on both supported hosts with StrictMode off and v2. The matrix covers PSCustomObject and Hashtable reads/writes, static computed members, member/index nesting, receiver and name evaluation order, exactly-once mutation, null/missing/throwing members, alias visibility, all four error-action preferences, downstream stopping, repeated calls, and remove/reimport. Strict analysis continues to reject the same syntax as runtime-bound.
- Focused complete-pipeline artifacts match original PowerShell on both supported hosts. The matrix covers empty, singleton, multiple, and null input; success, warning, information, and error ordering; begin/process/end lifecycle; aliases; all four error actions; downstream early stop; repeated invocation; removal/reimport; and retained mutation of Hashtable, OrderedDictionary, ArrayList, generic List, and array references, including duplicate-key and bounds failures. Pinned `Get-ObjectEnumValues` and `Remove-DuplicateObjects` execution is compared directly; `Get-MyIpAddress` is pinned and structurally emitted without performing a network query. Strict keeps the multi-stage pipeline rejected.
- Release builds pass for the active compiler targets **net10.0 and net472** with zero warnings and errors. `net8.0` is intentionally absent from this forward-looking gate.
- The pinned complete PSSharedGoods census passed without input failures: **284 source files**, **286 units**, **282 functions**, **174 emitted functions**, **39 promoted regions**, **69 candidates**, and **12 analysis opportunities**. The three foreach gains are `Find-MyProgramData`, `Get-Types`, and `Show-DataInVerbose`; dual-host original/generated tests exercise their unchanged source and module reimport. A separate dual-host lazy-enumerator comparison covers zero/multiple items, acquisition, `MoveNext`, and `Current` failures, early break, both error actions, and authored cleanup. `Get-ProtocolDefaults` remains hosted with its map-key loop and one typed prefix. The net emission change from the previous 171-function checkpoint is three; one formerly promoted region is now part of a complete method. `ConvertFrom-X500Address`, `Get-ComputerSMBInfo`, and the type-consuming observed-catch invocation wrappers retain their separate blockers. These counts are neither a language-coverage percentage nor a performance or public-package claim.

This audit did not rerun Linux/NativeAOT target-host qualification, the separate 290-input discovery packet, destructive management tests, hosted CI, or public-package lifecycle tests. Their earlier evidence remains dated; a passing local gate is not CI, publication, or installed-product proof. The [major milestones 24–30](PowerForge.PowerShellCompilation.NextMilestones.md) prioritize values/collections, complete pipelines, module state/regions, practical libraries, developer tooling, distribution, and later optimization.

## Earlier assessment: 2026-09-06

PowerForge has a semantic compiler with useful bounded DLL and EXE paths. This corrective slice repairs accepted numeric behavior, makes coverage assessment describe the requested artifact, and adds repeatable compiler validation. It also adds a small generic remainder/data-flow slice with complete programs from two unrelated computation families. Broad administration scripts still depend heavily on retained PowerShell and external services.

## Corrected contracts

- [x] **Single arithmetic.** The binder widens operands before floating arithmetic and selects `Double`, including unary arithmetic. `([single]16777216) + ([single]1)` now returns `System.Double` with value `16777217`. This fixes both precision and the generated CLR return type.
- [x] **Typed numeric updates.** Bound and lowered mutation contracts preserve constrained integer conversion, promotion, failure state, and authored CLR catch selection. Overflowing `[int]` addition and `[byte]` increment select `InvalidCastException`; exceptions thrown while evaluating the right-hand expression retain their original identity. The sweep covers all eight integral widths, compound updates, increment/decrement, loop iterators, floating operands, and promoted 64-bit products.
- [x] **Remainder.** Same-type integral `%` and constrained `%=` use canonical numeric semantics, including signed minimum remainder negative one. Euclidean GCD, prime counting, and Gregorian leap-year calculations now qualify as complete Strict programs. Mixed integral binary operations and dynamically promoting unconstrained locals remain outside this bounded contract.
- [x] **Artifact-aware census.** `--kind`, `--mode`, and `--semantic-profile` use the shared final shaping owner. A Strict standalone entrypoint is assessed as an EXE, including script-unit emission. Baselines reject different artifact/mode/profile/recursion contracts. The legacy API remains callable; explicit options use `RunWithOptions`.
- [x] **Resilient input assessment.** Unsupported source closure and malformed paths produce `InputFailures` while successful products remain available. Incomplete results retain a failure exit code and cannot satisfy a baseline. Empty and declarations-only Strict EXEs also build and exit without output; their synthetic entrypoint does not inflate authored-unit coverage.
- [x] **Bounded PR gate.** `Build/Invoke-PowerShellCompilerGate.ps1` selects semantic, census, state, artifact, and fuzz contracts and executes all six Strict corpus programs. The Windows product workflow supplies a hash-pinned PowerShell 7.6.5 host, .NET 8 artifact references, a timeout, and uploaded TRX/JSON evidence. Hosted CI remains a separate gate after publication.

PowerShell-specific exception wrappers remain a distinct boundary. Numeric forms inside catches requiring exact `RuntimeException` or PowerShell conversion-wrapper behavior are retained or rejected when that identity cannot be represented. Named local-call observation follows the complete transitive source closure, including callers retained as authored PowerShell, and checks bound numeric operations before rejecting a callee. An unresolved target, including a dynamic call or alias, conservatively retains potentially observed numeric callees while allowing unrelated safe helpers to compile. This prevents a retained caller from observing an incorrectly compiled helper. These fixes preserve the tested CLR catch and state contracts; they do not claim to reproduce every PowerShell error record.

## Measured execution coverage

The Strict acceptance packet expanded from four programs and 18 authored units to **six programs and 24 authored units**. All six original/generated executions passed on Windows x64 and Linux x64, with **24/24 emitted units and zero runtime fallback**. The added number-theory program returns `21|25`; the calendar program returns `1900-2100|49|False|True`.

The existing four-file Strict application still checks delivered dependency/resource identity, success and controlled-failure streams, execution from a clean directory, and six alternating fresh-process samples after excluded warmups. The packet enforces its existing per-target budgets. This is bounded workload qualification, not a PowerShell-language percentage or a broad speed claim.

Numeric artifact tests compare original and generated values, CLR types, catch selection, and state through `net8.0`, `net10.0`, and Windows `net472` binary-module and runtime-free library surfaces. The Linux selection excludes Windows PowerShell 5.1 cases and reports that unavailable capability explicitly. An additional 39 hosted-command and transpiler cases passed on Windows. These focused gates do not replace the full compiler suite or historical NativeAOT/physical-platform certification.

The final bounded gate passed **198 Windows cases** and **194 Linux cases**, with zero failures. The unchanged seven-workload external acceptance baseline also passed: 155 source files, 6/196 emitted units, 6/183 emitted functions, two promoted regions, 59 analysis-only opportunities, zero regressions, and zero complete-workload executions. Direct CLI validation confirmed a failed input returns exit code 1 while preserving the successful product in JSON. Workflow syntax passed `actionlint`.

The public Hybrid ten-module baseline retains its separately dated evidence and was not rerun by this Strict-only gate. Public-feed install, upgrade, rollback, provider acquisition, macOS/Arm64, and the disposable management-target reboot/reconnect case remain separate release or target qualification work.

## Pinned repository discovery

`compiler-discovery.net10.json` is separate from the acceptance baselines. It pins immutable revisions, SHA-256 archives, license status, source selection, artifact kind, semantic profile, and analysis-only execution policy. The runner reuses verified acquisition and canonical census owners. It does not import or execute external scripts.

| Source | Revision | Artifact | Assessed / submitted inputs | Strict emitted units | Hybrid emitted units |
| --- | --- | --- | ---: | ---: | ---: |
| [SamErde/PowerShell](https://github.com/SamErde/PowerShell) | `786225096078651d0fcbc535136d074b4c500698` | EXE | 230 / 232 | 1 / 412 | 8 / 412 |
| [CleanupMonster](https://github.com/EvotecIT/CleanupMonster) | `f215b32ca86b38d9150629c08f6c0c232bfafb27` | Binary module | 1 / 1 | 0 / 69 | 2 / 69 |
| [MicrosoftIntune](https://github.com/EvotecIT/MicrosoftIntune) | `876f8c8602ed3f31d5d5c36ad33818d42b5e9638` | EXE | 20 / 20 | 0 / 64 | 1 / 64 |
| [PowerShell algorithms](https://github.com/dfinke/powershell-algorithms) | `5e1f95b3111adb8c788786f773e63f087ac3d07b` | CLR library | 26 / 37 | 1 / 29 | 1 / 29 |

Each mode submitted **290 inputs**, assessed **277**, and retained **13 failures**. CleanupMonster's one manifest input expands to its real module closure; it is not one source file. Unit denominators describe assessed source only. Failed inputs remain in the submitted-input denominator, and none of these repositories has complete-workload execution credit.

Two SamErde inputs have nonliteral dot-source expressions: `General/Install-PowerShellAsDotNetTool.ps1` and `General/Show-BuildMermaid.ps1`. Eleven algorithm inputs have source-closure failures under the standalone-file selection, including paths outside that selected closure. They remain useful discovery evidence, not newly accepted dependencies. All assessed inputs reported zero parser-error files.

The initial audit's module-shaped script numbers are superseded by this artifact-aware scan. Low emission is still the principal product limit: a helper emitted inside a retained script does not mean the script is compiled. Repository discovery and authored acceptance programs answer different questions and must keep separate denominators.

The algorithm snapshot records MIT licensing; the other three snapshots record an unrecognized SPDX license. No external source was vendored. The new acceptance programs are locally authored generic computation examples.

## Scalar continuation candidate

The continuation candidate adds a bounded prefix that transfers stable scalar locals back to the retained PowerShell function in declaration order. The canonical binder, optimizer, semantic analysis, lowerer, and region graph still decide eligibility. Whole-function selection takes precedence over overlapping regions. Authored nullable constraints survive transfer on both Windows PowerShell 5.1 and PowerShell 7; modeled CLR errors, host effects, unsupported scope, and incomplete value contracts retain the source.

At source revision `dba6175aa`, the Windows gate passed **247 compiler cases and all six Strict programs**. The compiler also built for `net472`, `net8.0`, and `net10.0` without warnings. These are local candidate results, not a merged or published release.

Two complete command workloads passed original/generated output checks but **did not meet the former performance exit gate**. The report workload used the unchanged `CreateColorLegenedReportHTA` function from the pinned SamErde snapshot above, including its file write; the surrounding administration application was not executed. The diagnostic workload called Pester 6.1.0's unchanged `Format-Hashtable2` inside the complete original and generated module contexts, including nested formatting and sorting.

| Complete workload, 300 calls per sample | Original median, CCD0 / CCD1 | Hybrid median, CCD0 / CCD1 | Interpretation |
| --- | ---: | ---: | --- |
| Report generation and file output | 151.74 / 156.98 ms | 153.04 / 153.82 ms | Within the 5% tie tolerance; no demonstrated benefit |
| Nested diagnostic formatting | 69.17 / 65.40 ms | 76.46 / 74.01 ms | 10.5% / 13.2% slower |

The canonical benchmark runner used five warmup and twenty measured samples per lane, rotated engine order, and no outlier removal. Both processor cache domains were measured independently on a Ryzen 9 9950X3D2, Windows build 26200, PowerShell 7.6.5, with matching affinity per comparison and normal process priority. Each report sample checked file-byte identity and absent success output; every formatting result matched the original. Thread allocations were slightly worse for the report and about 0.5% lower for formatting. Neither result demonstrates a useful allocation benefit. These constant-heavy prefixes do not establish that the crossing cost is amortized. That performance result does not block subsequent correctness or coverage work.

Qualification also exposed two independent correctness defects. Project restore now derives SDK properties from the actual artifact template, so single-file executable restore includes the same implicit package closure as build. Strict and Hybrid single-file projects passed lock, online restore, offline restore, build, and execution. Runtime-free targets now reject hosted command tails even when stream-provider capability is enabled. An unchanged algorithm that previously emitted a dispatcher-dependent library while claiming no PowerShell runtime now fails analysis; the old emitted-unit count is not native execution evidence.

## Numeric computation candidate

A bounded numeric representation can keep an inferred Int32 counter in Double storage when literal additive updates preserve its numeric value and every read combines it with an existing Double operand. Direct type observation, boxing, interpolation, wider mutations, opaque observers, and transfer back into retained PowerShell remain rejected. Complete typed bodies can also use the existing region ABI when binary cmdlet shaping requires retaining the authored function header. Neither change introduces another backend.

The unchanged `trialDivision` function from the pinned algorithm snapshot now emits one complete-body helper in a Hybrid binary module while retaining its original function name and parameter header. Original/generated parity covers sixteen inputs, including fractional values, NaN, and infinities, and checks Boolean type and scalar cardinality.

| Complete command, 100 calls per sample | Original median, CCD0 / CCD1 | Hybrid median, CCD0 / CCD1 | Interpretation |
| --- | ---: | ---: | --- |
| Prime input `1000000007` | 49.82 / 46.95 ms | 11.35 / 11.10 ms | 4.39 / 4.23 times faster |
| Composite input `1001` | 4.74 / 4.73 ms | 5.09 / 5.25 ms | 7.4% / 11.1% slower |

Both domains used five warmups, twenty measured samples, rotated order, no outlier removal, and the same host settings described above. Every measured result was validated. The computation-heavy case amortizes the helper call; the cheap case does not. These are workstation timings for one workload family, with no allocation-benefit or general speedup claim. Further coverage and continuation work is governed by semantic correctness; a two-family speedup is no longer required.

The binder also proves bounded Int32 addition and subtraction from operand intervals. A descending loop can refine its counter after a proven initializer and positive guard when the body cannot write or indirectly expose the counter. The fact stays within the loop's cloned symbol state; unproved arithmetic still retains PowerShell execution.

The next workload exposed a separate collection defect: an untyped `@($InputObject)` could wrap an incoming array as one element. Potentially enumerable collection expressions now retain PowerShell execution. A runtime-collection experiment recovered the shuffle's ordinary output shape and showed substantial potential benefit, but failed to preserve continuation after an enumeration error. That experiment was removed and provides no accepted second-family performance result. Broader collection compilation needs a complete value and error contract before qualification.

Final local validation passed **278 compiler cases and all six Strict programs**, including generated-artifact parity on Windows PowerShell 5.1 and PowerShell 7.6, overflow promotion, preceding success output, bounded counters, retained array shape, and enumeration-error continuation. All three compiler frameworks built without warnings. Separate independent read-only reviews covered numeric projection and the later integer-range/collection-retention contracts without actionable findings. These are local candidate results; publication and current-head CI remain separate checks.

## Detection executable qualification

The complete unchanged `Scripts/LocalSecurityAuthority/Detect-LsaProtection.ps1` from the pinned MicrosoftIntune snapshot passed project initialization, lock, restore, build, test, and pack as a framework-dependent Hybrid `net10.0` `win-x64` executable. Its source SHA-256 is `f3c152538dad482adc91a2df860d9eca9ee38981207a433060d476e5e87fe369`. The original PowerShell process and generated executable both exited zero and printed the same enabled-state message, with empty stderr. The observed `RunAsPPL` registry value was 1 before and after execution; qualification did not change the setting.

This proves the enabled-state path on Windows build 26200. It does not qualify disabled, missing-value, denied-access, reboot, or other-platform paths. The executable retains PowerShell execution and has no native computation claim. A compact temporary project directory was required on this Windows host because long-path support was disabled; the earlier deeper package location exceeded the effective path limit. This is a deployment limitation, not a reason to change machine policy automatically.

## Next implementation gate

Correctness and useful coverage take priority over speed. Preserve how the author designed the program, including errors and continuation, and unlock broader scripts/modules through reusable compiler contracts. The earlier two-family performance requirement has been retired; its measurements remain historical evidence, not a prerequisite for adding support.

- [ ] Begin M27 by qualifying ordinary C# consumers, useful typed APIs, package closure, and ABI compatibility through the existing generated-project and dependency owners. Do not reopen Hybrid-only state or region contracts in the runtime-free surface.
- [x] Implement a bounded scalar continuation and compare two unrelated complete commands. Output parity passed; performance did not.
- [x] Apply the bounded two-region shape to unchanged real module source. The PSSharedGoods enum-table workflow preserves the same Hashtable reference across retained mutation and a compiled terminal return on both supported hosts.
- [x] Replace the scalar/Hashtable special case with a closed collection-region ABI and return-or-fallthrough envelope. `Split-Array` now supplies three real regions, including its control-flow-dependent parameter-array return, while retained PowerShell remains the output-enumeration and failure-continuation owner.
- [x] Recover one bounded array/foreach transformation through that same owner. A closed non-nullable value-type scalar loop can emit stable-scalar values into a fresh typed vector in generated code, while retained PowerShell owns its later enumeration. The unchanged `Get-ADEncryptionTypes` and `Get-ADTrustAttributes` functions pass on both supported hosts. Dynamic objects, ETS, arbitrary enumerators, explicit output shaping, general local-call collection closure, and compiled collection mutation remain fail-closed until their complete contracts exist.
- [x] Add a coherent return-cardinality family rather than another scalar type exception. `NoValue` and `NullValue` flow through transfer metadata, terminal and conditional region selection, bound/lowered control flow, generated C#, runtime composition, and retained invocation. AutomationNull, dynamically empty subexpressions, and arbitrary object returns remain fail-closed.
- [x] Add one bounded local collection-factory closure shared by four unchanged workflows. The exact helper has zero parameters, creates one fresh `ArrayList`, and returns it as one comma/no-enumerate record. Portable evidence records exact empty parameter types, exact result type, callee-fresh ownership, retained-only mutation, and no compiler-owned enumeration. The same proved projection now closes complete Hybrid native consumers by writing the constructed value through invocation-owned variable storage; three SQL consumers become complete methods while the ETS-heavy consumer retains a region. Dynamic, module-qualified, unresolved, recursive, stateful, parameterized, and differently shaped helpers remain retained.
- [x] Complete M26 additional-region composition through the existing binder, promotion policy, reconciler, and immutable source rewriter. Every non-overlapping canonical run can contribute one maximal approved region; unsafe maximal candidates do not hide safe statement-aligned suffixes, and enclosed approved suffixes do not reach the runtime rewriter. The one guarded prefix remains the sole fresh-local owner. Dual-host module artifacts preserve scalar transfer, exact local-factory collection identity, caller mutation, early stop, reuse, and reimport; same-run modeled exception routes remain hosted.
- [x] Carry computed instance/static member reads and computed instance-member assignment through one explicit Hybrid native-function contract. PowerShell's adapter binder remains the semantic owner; Strict and typed/runtime-free regions stay closed. Thirteen unchanged PSSharedGoods functions become emitted, while `Send-Email` demonstrates that clearing this family does not erase unrelated blockers.
- [x] Carry retained collection mutation and complete pipeline flow through one existing canonical owner. Complete multi-stage pipelines execute as native command regions inside emitted Hybrid bodies, so PowerShell retains stream, lifecycle, scope, error, and mutation authority without a second pipeline engine or a collection-type whitelist.
- [x] Assess bounded `foreach`/enumeration, range-and-match control flow, type/conversion expressions, and local-function closure together. Select the closed fresh-collection factory projection because it closes three complete consumers through an existing owner without widening general local calls.
- [x] Take range-and-match as one grouped control-flow goal while keeping distinct canonical operator contracts. Dual-host differential proof covers `$Matches`, scalar/collection matching, regex conversion/errors, case/negation, inclusive ascending/descending ranges, endpoint conversion/overflow, character ranges, nulls, single evaluation, streams, errors, and stopping. Nine unchanged functions become complete emitted methods because the same contracts recur outside the initial seven-function frontier.
- [x] Add one bounded type-literal expression family rather than a CLR-type whitelist. Target-compatible literals produce `System.Type` values in runtime-free and hosted artifacts; unresolved and target-missing types fail closed. Differential proof keeps the pinned SID workflow hosted when its native `try/catch` would otherwise observe a different method-invocation wrapper.
- [x] Add one Hybrid-native runtime target-conversion contract rather than a target-type whitelist. The active PowerShell host resolves the authored type name before operand evaluation and applies its conversion binder; Strict remains closed. Synthetic dual-host proof covers null, scalar, array, error-action, repeat, and reimport behavior. Three unchanged directory-conversion workflows become complete emitted methods, while direct and transitively hoisted type literals passed to invocations under observed catches remain hosted.
- [x] Qualify the complete detection executable's enabled-state path on a controlled Windows target without modifying the registry.
- [ ] Extend detection qualification to absent, disabled, and failure states on a disposable target. Qualify remediation separately with before/after state and cleanup.
- [ ] Keep broader target-host and public-package lifecycle qualification explicit before making a stable toolchain claim.

Retain generated C# as the backend and semantic decisions in the binder, immutable IR, analysis, and lowering. No finding requires another backend, named-repository special cases, or a second eligibility owner.

## Reproduction

Build the CLI and tests from the same checkout with the repository's .NET 10 SDK. Artifact validation also needs the selected PowerShell 7.6 host; on Windows, the `net472` matrix uses Windows PowerShell 5.1.

```powershell
./Build/Invoke-PowerShellCompilerGate.ps1 `
    -EvidencePath ./assessment/compiler-gate -RuntimeIdentifier win-x64

powerforge powershell census ./Main.ps1 --kind exe --mode Strict `
    --framework net10.0 --semantic-profile PowerForge.Oracle.PowerShell/7.6 --output json

./Benchmarks/PowerShellCompilation/Corpus/Invoke-CompilerDiscovery.ps1 `
    -CliAssemblyPath ./PowerForge.Cli/bin/Release/net10.0/PowerForge.Cli.dll `
    -WorkspacePath ./assessment/discovery
```

Run the gate on the actual Linux target with `-RuntimeIdentifier linux-x64`; cross-publishing alone is not execution evidence. Discovery can rerun with `-Offline` after verified acquisition. Its JSON records successful products and per-input failures; a completed discovery scan is not an acceptance pass.
